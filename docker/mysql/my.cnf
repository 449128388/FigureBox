[mysqld]
# 排序缓冲区大小 - 增加以支持 ORDER BY 排序操作
# 默认值 256KB，调至 8MB 避免 Out of sort memory 错误
# 注意：Windows 挂载文件可能被 MySQL 忽略（World-writable config warning）
# 因此在 docker-compose.yml 中通过 command: --sort-buffer-size=8388608 传递
sort_buffer_size = 8M
