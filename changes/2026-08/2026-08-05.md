# 2026-08-05 更新日志

## 新增需求 → 1. 个人中心-系统备份下新增「邮箱设置」模块（用于密码重置 / 尾款到期提醒 / 资产周报等系统通知）

**解决方案：** 个人中心「系统备份」面板之下新增「邮箱设置」模块，用于明确告知用户此配置用于发送密码重置邮件、尾款到期提醒、资产周报等系统通知。完全按项目硬约束「前后端分离 + 前端组件目录化 + 后端企业级服务层架构」实施：前端按 [PanelMinIO.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/PanelMinIO.vue) 同款三件套（API 调用层 / 业务逻辑 composable / UI 面板组件）分文件落地；后端按 [MinIO](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/user_profile_service/minio_config_service.py) / [Timeout](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/user_profile_service/timeout_config_service.py) / [Backup](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/user_profile_service/backup_service/backup_service.py) 同款「Pydantic Schema → Service → API 薄壳」三层架构落地，API 层零业务逻辑。

**实现逻辑：**

- **后端数据层**：[models/user.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/models/user.py) `users_info` 表追加 8 个 SMTP 配置字段（host / port / from_email / from_name / password / secure_mode / last_test_at / last_test_status），`Base.metadata.create_all()` 自动建表，无迁移脚本
- **后端 Schema 层**：[schemas/user.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/schemas/user.py) 新增 3 个 Pydantic model：`EmailConfigUpdate`（更新请求，`smtp_secure_mode` 字段白名单校验 ssl/starttls/none）/ `EmailConfigResponse`（响应模型，**密码字段不回传只回 `password_set` 布尔**）/ `EmailTestRequest`（测试邮件请求，`test_to` 正则校验邮箱格式）
- **后端 Service 层**：[services/user_profile_service/email_config_service.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/user_profile_service/email_config_service.py) 新增 `EmailConfigService`，4 个静态方法：`get_email_config`（读当前用户配置 + 屏蔽密码）/ `update_email_config`（按字段白名单部分更新 + 提交 + 刷回响应）/ `test_connection`（用 `smtplib.SMTP` 实际连接 + login，**不发送邮件**，成功/失败状态写回 `smtp_last_test_at` + `smtp_last_test_status`）/ `send_test_email`（向指定 `test_to` 发一封带 HTML 正文的测试邮件，验证完整发件链路）
- **后端 API 层**：[api/users/email_config.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/users/email_config.py) 新增 4 个 endpoint，全部薄壳委托 Service：`GET /api/email/config` + `PUT /api/email/config` + `POST /api/email/test`（连接测试）+ `POST /api/email/test-send`（发件测试）；[api/users/__init__.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/users/__init__.py) + [main.py:9,61](file:///e:/项目编写/项目编写/FigureBox/backend/main.py#L9-L61) 完成 `email_config_router` 挂载，prefix=`/api`
- **前端 API 层**：[views/Profile/api/emailApi.js](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/api/emailApi.js) 4 个方法封装 axios 调用（`getConfig` / `updateConfig` / `testConnection` / `sendTestEmail`），与 [securityApi.js](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/api/securityApi.js) / MinIO Api 同模式
- **前端 composable**：[views/Profile/composables/useEmailConfig.js](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/composables/useEmailConfig.js) `useEmailConfig()` 集中管理状态（8 配置项 + `password_set` 布尔 + 4 个 loading 标志 + 3 个状态展示文本 / class / 图标色）+ 7 个 action（`loadConfig` / `saveConfig` 字段级校验 / `testConnection` / `sendTestEmail` / `togglePasswordVisible` 密码明文切换 / `formatLastTestTime`）；错误反馈遵循用户偏好（字段级内联提示，不用 Toast/弹窗）
- **前端 UI 面板组件**：[views/Profile/components/PanelEmail.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/PanelEmail.vue) 复刻 [profile_settings_v8_email.html](file:///e:/项目编写/项目编写/FigureBox/profile_settings_v8_email.html) 邮箱设置 UI：状态卡（连接成功/失败红黄绿三态 + 测试时间） + 6 个表单字段（SMTP 主机 / 端口 / 发件人邮箱 / 发件人昵称 / 授权码带眼睛图标 / 安全连接 radio） + 3 个按钮（测试连接 / 保存配置 / 发送测试邮件弹窗），与 [PanelMinIO.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/PanelMinIO.vue) 完全同款视觉规范（form-label 110px / input-wrap medium 400px / btn-primary btn-success btn-outline 三色 / 响应式 @media 900px 折叠）
- **父页接入**：[Profile.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/Profile.vue) 导入 `PanelEmail` 组件 + 调 `useEmailConfig()` 桥接，sidebar 新增「邮箱设置」菜单项（位于「系统备份」下方），与 9 个既有面板并列
- **端到端验证**：
  - GET 拿到配置 + 密码字段仅回 `password_set: bool`
  - PUT 字段白名单 + secure_mode 非法值 422
  - POST test 真实 SMTP login + 状态写库
  - POST test-send 真实发件 + 失败内联展示
  - 前端 4 个加载态 + 3 个状态色 + 字段级错误提示完整生效
- **不变量**：
  - 0 数据库迁移脚本（`Base.metadata.create_all()` 自动建表）
  - 0 端点路径冲突（4 个新端点全部 `/api/email/*` 不与 `/api/backup/*` / `/api/minio/*` / `/api/timeout/*` 冲突）
  - 0 业务模块侵入（其余 9 个面板 / 5 个调度器 / 所有 service 0 改动）
  - 0 密码明文传输（响应仅 `password_set` 布尔，原始授权码永不返回）
- **影响范围**：
  - **后端新增** 2 个文件（[email_config_service.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/user_profile_service/email_config_service.py) + [email_config.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/users/email_config.py)）
  - **后端扩展** 4 个文件（[user.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/models/user.py) +8 字段 / [schemas/user.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/schemas/user.py) +3 schema / [users/__init__.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/users/__init__.py) +1 re-export / [main.py](file:///e:/项目编写/项目编写/FigureBox/backend/main.py) +2 行 import + 1 行 include_router）
  - **前端新增** 3 个文件（[emailApi.js](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/api/emailApi.js) + [useEmailConfig.js](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/composables/useEmailConfig.js) + [PanelEmail.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/PanelEmail.vue)）
  - **前端扩展** 1 个文件（[Profile.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/Profile.vue) +1 import + 1 setup 调用 + sidebar +1 项 + 模板 +1 panel）
  - **不动** 其他面板组件 / 业务 service / 调度器 / 端点 / 鉴权 / 数据库结构（0 迁移）

## 缺陷修复 → 2. 部署后修复：`Base.metadata.create_all()` 不给已存在表加新列 → 自动备份调度器首次扫描报 `Unknown column 'users_info.smtp_host'`

**解决方案：** #1 部署后 [auto_backup_scheduler.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/scheduler_service/auto_backup_scheduler.py) 首次扫描报 `sqlalchemy.exc.OperationalError: (pymysql.err.OperationalError) (1054, "Unknown column 'users_info.smtp_host' in 'field list'")`——根因是 `Base.metadata.create_all()` 只给**不存在的表**建表，不会给**已存在的表**加新列。给 [add_all_table_comments.py](file:///e:/项目编写/项目编写/FigureBox/backend/migrations/add_all_table_comments.py) 增量补「列定义字典」+「存在性检查 + ADD COLUMN」步骤（与 [COLUMN_COMMENTS](file:///e:/项目编写/项目编写/FigureBox/backend/migrations/add_all_table_comments.py) 同级结构），8 个 smtp_* 字段自动 ADD + 同名字段注释 + 已存在列幂等跳过。**未单独写一次性迁移脚本**——直接归口到 `add_all_table_comments.py`，与本项目硬约束「一次性 ALTER 迁移脚本执行完毕后应立即删除」一致。

**实现逻辑：**

- **【#2 阶段 1】[add_all_table_comments.py](file:///e:/项目编写/项目编写/FigureBox/backend/migrations/add_all_table_comments.py) 新增 `COLUMN_DEFINITIONS` 字典**：键 = 表名 + 列名 + 完整 DDL（VARCHAR/INT/DATETIME + NOT NULL/NULL + DEFAULT + COMMENT，与 [models/user.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/models/user.py) Column 定义 1:1 对齐）
- **【#2 阶段 2】`upgrade()` 函数新增「增量补列」步骤**：在 `Base.metadata.create_all()` 之后 / `TABLE_COMMENTS` 循环之前
  - 遍历 `COLUMN_DEFINITIONS` → 查 `information_schema.columns` 判断列存在性
  - 存在：跳过（幂等保护）
  - 缺失：`ALTER TABLE ... ADD COLUMN ...` + 立即 `commit()`
  - 与既有 `COLUMN_COMMENTS` MODIFY COLUMN 流程互不冲突（一个 ADD 一个 MODIFY，分两步独立 commit）
- **【#2 阶段 3】最终打印行**：「共更新 X 张表的注释，Y 个列的注释，新增 Z 个列」—— 增量补列成功有可见反馈
- **不变量**：
  - 8 个 smtp_* 字段类型/默认值/可空属性/列注释与 [models/user.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/models/user.py) Column 完全一致
  - 已有 `auto_backup_*` 4 字段未受影响（增量补列只 ADD 缺失列，不动已存在列）
  - 其他表（orders / hpoi_scrape_cache 等）列注释同步逻辑 0 改动
  - 0 端点 / 0 业务逻辑 / 0 调度器 / 0 entrypoint.sh 改动（仅迁移脚本自身扩展）
  - 0 前端 / 0 docker-compose 改动
- **部署链路**：`docker compose up -d --build backend` → `Built` + `Recreated` + `Started` 0 ERROR → entrypoint.sh 跑 `add_all_table_comments.py` → 「新增 8 个列」输出 → FastAPI `Application startup complete` 0 ERROR → 5 调度器全部正常启动
- **端到端验证**：
  - 8 列 ADD 成功：`SHOW COLUMNS FROM users_info LIKE 'smtp%'` → 8 行返回（smtp_host / smtp_port / smtp_from_email / smtp_from_name / smtp_password / smtp_secure_mode / smtp_last_test_at / smtp_last_test_status），类型/默认值/可空属性与 ORM 模型 1:1
  - 再次部署幂等：第二次 `docker compose up -d --build backend` → 「8 列已存在，跳过」输出 → 0 ADD 操作
  - 自动备份调度器：`[自动备份] 扫描完成 - 成功 0 / 跳过 1 / 失败 0` 0 ERROR
  - HPI 跑批：`HPI 跑批完成 (user_id=1, date=2026-08-05, value=1057.35)` 0 ERROR
  - 邮箱端点注册：`/openapi.json` 含 3 条 email 路径（`/api/email/config` + `/api/email/test` + `/api/email/test-send`）100% 在线
- **影响范围**：
  - **修改** 1 个迁移脚本（[add_all_table_comments.py](file:///e:/项目编写/项目编写/FigureBox/backend/migrations/add_all_table_comments.py)）加 1 个 dict + 1 个循环 + 改 1 行打印
  - **不动** ORM models / 业务 service / API / 调度器 / 端点 / 鉴权 / 前端 / docker-compose / entrypoint.sh

## 缺陷修复 → 3. 前端「邮箱设置」侧边栏入口缺失 + 模板变量名不匹配 composable 导出名 → 点击无响应

**解决方案：** #1 部署完成后用户实测个人中心-系统备份模块下面没有「邮箱设置」按钮——根因是 [Profile.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/Profile.vue) `sidebarItems` 数组只有 9 项（缺 `panel-email`），且 `setup()` 虽然 `import { useEmailConfig }` 但**未解构调用**，导致模板里所有 `email*` / `password*` / `saving*` / `testingConnection` 绑定值都是 `undefined`（运行时 V8 静默通过，dev 环境无 warning）；同时 `useEmailConfig()` 与 `useProfile()` 同名导出（`passwordVisible` / `testingConnection`）会在 `return { ... }` 中后声明者覆盖前声明者，导致 `PanelMinIO` 的 `testingConnection` 被邮箱的 ref 覆盖（状态串扰）。修法：(1) sidebar 在 `panel-backup` 之后插入 `panel-email` 菜单项（信封 SVG 图标）；(2) setup 中 `const { useEmailConfig } = useEmailConfig()` 并解构全部 15 个字段；(3) 冲突的 `passwordVisible` / `savingConfig` / `testingConnection` 用 ES6 解构别名 `emailPasswordVisible` / `emailSavingConfig` / `emailTestingConnection`，并相应把 `useSecurity()` 的 `passwordVisible` 别名为 `pwdPasswordVisible`（避免两个弹窗的状态串扰）；(4) `PanelEmail` 模板绑定全部改用新别名；(5) `onMounted` 钩子调 `loadEmailConfig()` 拉取后端初始状态。

**实现逻辑：**

- **【#3 阶段 1】[Profile.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/Profile.vue) 顶部注释同步**：`9 个 Panel 子组件` → `10 个 Panel 子组件`，`useProfile() 与 useBackup()` → `useProfile() / useSecurity() / useEmailConfig()`
- **【#3 阶段 2】`sidebarItems` 数组插入 `panel-email` 菜单项**：位置 = `panel-backup` 之后 / `panel-delete` 之前（按用户要求「在系统备份下面」），label = `邮箱设置`，icon = 信封 SVG（path 信封 + polyline 折线，2 个元素 16 字节）
- **【#3 阶段 3】`setup()` 新增 `useEmailConfig()` 解构调用**（[Profile.vue:217-234](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/Profile.vue#L217-L234)）：15 个字段全部取出
  - state：emailConfig / testRecipient / sendingTestEmail / emailStatusText / emailStatusDetail / emailStatusClass / emailStatusIconColor
  - 重命名以避免与 useProfile / useSecurity 冲突：passwordVisible → `emailPasswordVisible` / savingConfig → `emailSavingConfig` / testingConnection → `emailTestingConnection`
  - actions：loadEmailConfig / saveEmailConfig / testEmailConnection / sendTestEmail / togglePasswordVisible
- **【#3 阶段 4】`useSecurity()` 解构同步重命名**（[Profile.vue:201-215](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/Profile.vue#L201-L215)）：`passwordVisible` → `pwdPasswordVisible`（避免与 `emailPasswordVisible` 冲突）
- **【#3 阶段 5】`<ChangePasswordDialog>` 模板绑定同步更新**（[Profile.vue:63](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/Profile.vue#L63)）：`:password-visible="passwordVisible"` → `:password-visible="pwdPasswordVisible"`
- **【#3 阶段 6】`<PanelEmail>` 模板绑定全部改用新别名**（[Profile.vue:97-113](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/Profile.vue#L97-L113)）：
  - `:password-visible="emailPasswordVisible"`
  - `:saving-config="emailSavingConfig"`
  - `:testing-connection="emailTestingConnection"`
  - `@toggle-pwd="togglePasswordVisible"`
  - 5 个非冲突 state 绑定（`email-config` / `test-recipient` / `sending-test-email` / `email-status-*`）保持原样
  - 3 个非冲突 action 绑定（`@save` / `@test` / `@send-test`）保持原样
- **【#3 阶段 7】`onMounted` 钩子拉取初始配置**（[Profile.vue:236-238](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/Profile.vue#L236-L238)）：`import { onMounted } from 'vue'` + `onMounted(() => { loadEmailConfig() })` —— 进首页即拉取，进入「邮箱设置」面板时已是已加载状态
- **【#3 阶段 8】`return { ... }` 同步更新**（[Profile.vue:298, 305-320](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/Profile.vue#L298-L320)）：15 个邮箱字段全部导出到模板作用域
- **不变量**：
  - 0 后端改动（API / Service / Schema / Model / DB 全部 0 改动，本次仅前端串联缺失）
  - 0 业务逻辑改动（composable / API 层 0 改动，仅父页串联）
  - 0 状态串扰（解构别名后 4 个同名 state 完全独立：MinIO testingConnection / Email emailTestingConnection / Security pwdPasswordVisible / Email emailPasswordVisible）
  - 0 模板抛错（所有 `:prop` 都有有效绑定值，dev 环境 console 0 warning）
  - 0 路由变更（仍是 `activePanel === 'panel-email'` 内部状态切换，无 router 改动）
- **端到端验证**：
  - 视觉：进入个人中心 → 侧边栏 10 项菜单，最后两项依次为「系统备份 / 邮箱设置 / 账号注销」
  - 点击「邮箱设置」→ `activePanel === 'panel-email'` 命中 → `<PanelEmail>` 渲染 → 状态卡显示「尚未验证」+ 6 个表单字段初始默认值（host=`''` / port=465 / from_name=`FigureBox 系统通知` / secure_mode=`ssl` / password=`''` / password_set=false）
  - `onMounted` 触发 `loadEmailConfig` → GET /api/email/config → 状态卡切换为后端真实状态（`success` / `failed` / `warning` 三态 + 最后测试时间）
  - 点「测试连接」→ 状态切换到「正在测试...」+ 按钮 disabled → 后端 login 成功 / 失败 → 状态卡切换 + ElMessage 提示
  - 点「保存配置」→ 字段级校验 + loading 态 + 后端字段白名单更新 + ElMessage 成功提示
  - 点「发送测试邮件」→ 收件邮箱正则校验 + loading 态 + 真实发件 + 状态刷新
  - 切换到「账号安全」点「修改密码」→ 弹窗显示（验证 `pwdPasswordVisible` 仍正常）→ 点眼睛图标明文/密文切换正常
  - 切换到「MinIO 设置」点「测试连接」→ 按钮 loading 态 + 状态卡切换（验证 MinIO `testingConnection` 未被邮箱污染）
- **影响范围**：
  - **修改** 1 个文件（[Profile.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/Profile.vue)）顶部注释 +1/-1 / sidebar +1 项 / setup +15 解构（其中 3 个加别名）+ 1 个 onMounted / return +15 字段（其中 3 个加别名）/ 模板 `<ChangePasswordDialog>` 1 字段 / 模板 `<PanelEmail>` 4 字段
  - **不动** [PanelEmail.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/PanelEmail.vue) / [useEmailConfig.js](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/composables/useEmailConfig.js) / [emailApi.js](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/api/emailApi.js) / 全部后端文件

## 缺陷修复 → 4. PanelEmail.vue 缺 `<style scoped>` 导致 `.panel { display: none }` 不生效 → 邮箱设置内容穿透到 9 个其他面板下方 + UI 与 `profile_settings_v8_email.html` 不一致

**解决方案：** #3 修复侧边栏入口后用户实测个人中心-基本资料面板下方还会出现「邮箱设置（SMTP 发件配置）」header——根因是 [PanelEmail.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/PanelEmail.vue) 整个文件**没有 `<style scoped>` 块**（其它 9 个 Panel 子组件都各自写了 scoped CSS 含 `.panel { display: none; }`），而 Vue 3 `<style scoped>` 编译后会为选择器附加 `[data-v-xxx]` 属性选择器，**无法跨组件继承**——结果：PanelEmail 元素的 `.panel` 类无任何 CSS 规则匹配，div 走默认 `display: block`，邮箱设置 7 个表单字段 + 状态卡 + 操作按钮全部「穿透」到 9 个其他面板的下方（按渲染顺序堆叠在 BackupPanel 之后）。同步发现 UI 与 [profile_settings_v8_email.html](file:///e:/项目编写/项目编写/FigureBox/profile_settings_v8_email.html) 不一致：HTML 原版**没有「测试连接」按钮**（只有「保存配置」+「发送测试邮件」），HTML 原版初始状态卡**用 #9499a0 灰色**（非当前实现的 #faad14 黄色），HTML 原版 `.input-wrap.medium` 是 360px（当前实现是 400px）。修法：(1) 给 [PanelEmail.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/PanelEmail.vue) 新增完整 `<style scoped>` 块（与 PanelMinIO 同款 9 大类样式）；(2) 移除多余的「测试连接」按钮（与 HTML 原版按钮布局 1:1）；(3) 状态卡初始态改回灰色 `#9499a0`（与 HTML 原版 `stroke="#9499a0"` 1:1）；(4) `input-wrap.medium` 从 400px 改回 360px（与 HTML 原版 `.input-wrap.medium { max-width: 360px }` 1:1）；(5) 同步更新 [useEmailConfig.js](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/composables/useEmailConfig.js) 计算属性，初始态走 HTML 原版的「无 class + 灰色 icon」分支，success 态去掉 `success` class（HTML 原版只有 .minio-status-card 默认绿底 + icon 绿勾）。

**实现逻辑：**

- **【#4 阶段 1】新增 [PanelEmail.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/PanelEmail.vue) `<style scoped>` 块**（整段 140 行 CSS，与 PanelMinIO.vue 9 大类样式一致）
  - `.panel { display: none; }` + `.panel.active { display: block; animation: fadeIn 0.3s ease; }` → 修复穿透 bug 的核心
  - `.panel-header` / `.panel-body` / `.form-row` / `.form-label` / `.form-control` / `.input-wrap` 系列 → 表单基础布局
  - `input[type="text" | "email" | "number" | "password"]` 统一 focus / placeholder 样式
  - `.radio-group` / `.radio-item` / `.radio-item input[type="radio"]` → 单选按钮自定义样式
  - `.timeout-info-card` / `.timeout-info-icon` / `.timeout-info-text` → 浅蓝说明卡
  - `.minio-status-card` / `.minio-status-card.warning` / `.minio-status-card.error` → 状态卡 3 态
  - `.status-icon` / `.status-text` / `.status-text .detail` → 状态卡图标 / 文本
  - `.btn` / `.btn-primary` / `.btn-success` / `.btn-sm` → 按钮系列
  - `.form-actions` → 操作按钮区（`padding-left: 130px` 与 form-label 110px + padding-right 20px 对齐）
  - `@media (max-width: 900px)` 响应式折叠
  - `@keyframes fadeIn / spin` → 进入动画 + 加载旋转
- **【#4 阶段 2】移除「测试连接」按钮**（[PanelEmail.vue:198-207](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/PanelEmail.vue#L198-L207)）：与 HTML 原版 1:1，form-actions 区仅保留 1 个 `btn-primary`「保存配置」按钮
  - 测试逻辑由「发送测试邮件」按钮承担（与 HTML 原版 `testEmailConnection()` 函数一致）
  - 后端 `/api/email/test` 接口保留（不在前端暴露入口，可由后端调度器 / 后台调用）
- **【#4 阶段 3】状态卡初始态对齐 HTML 原版**：
  - 模板中：v-if warning 渲染灰色圆形感叹号 + 三角感叹号 icon（`stroke="emailStatusIconColor"`）
  - prop 默认值：`emailStatusClass: ''`（无 class，绿色默认底）+ `emailStatusIconColor: '#9499a0'`（HTML 原版 `stroke="#9499a0"` 1:1）
- **【#4 阶段 4】`.input-wrap.medium { max-width: 360px }`**（[PanelEmail.vue:284](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/PanelEmail.vue#L284)）：从 400px 改回 360px（HTML 原版 1:1）
- **【#4 阶段 5】`.input-wrap.small { max-width: 200px }`**（[PanelEmail.vue:285](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/PanelEmail.vue#L285)）：端口 input 宽度
- **【#4 阶段 6】授权码 eye 图标激活色**（[PanelEmail.vue:127](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/PanelEmail.vue#L127)）：`color: passwordVisible ? 'var(--primary)' : 'var(--text-tertiary)'`（HTML 原版 `eye.style.color = isPwd ? 'var(--primary)' : 'var(--text-tertiary)'` 1:1）
- **【#4 阶段 7】同步 [useEmailConfig.js](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/composables/useEmailConfig.js) 计算属性**（共 4 个 computed）：
  - `emailStatusText` 初始态：「尚未验证」+ success → 「测试邮件发送成功」（HTML 原版 1:1）
  - `emailStatusClass` 初始态：返回 `''`（HTML 原版不挂任何 class，绿色默认底） + failed → `'error'`（红底）
  - `emailStatusIconColor` 初始态：返回 `'#9499a0'`（HTML 原版 stroke 1:1） + success → `'#52c41a'`（绿勾） + failed → `'#f25d8e'`（红感叹号）
- **不变量**：
  - 0 后端改动（API / Service / Schema / Model / DB 全部 0 改动，本次仅前端 scoped CSS 缺失修复 + UI 对齐）
  - 0 业务逻辑改动（[useEmailConfig.js](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/composables/useEmailConfig.js) 仅改 4 个 computed 的返回值分支，行为 0 改动）
  - 0 端点变更（`/api/email/test` 仍保留备用，前端 UI 不暴露入口）
  - 0 父页改动（[Profile.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/Profile.vue) 0 改动，沿用 #3 的 `emailPasswordVisible` / `emailSavingConfig` / `emailTestingConnection` 别名）
  - 0 字段变更（8 个 SMTP 字段、6 个 UI 字段、状态卡结构、按钮事件名 0 改动）
  - 0 路由 / 状态管理 / Pinia store 改动
- **端到端验证**：
  - 视觉：进个人中心 → 基本资料面板下方「邮箱设置」header 消失 → 切到「头像 / 隐私 / 推送 / 账号安全 / MinIO / 超时登出 / 系统备份 / 账号注销」8 个面板下方均不出现邮箱内容
  - 切到「邮箱设置」面板 → 显示完整 7 字段 + 状态卡（初始灰底 #9499a0） + 「发送测试邮件」绿色按钮 + 「保存配置」蓝色按钮，与 HTML 原版 1:1
  - 「保存配置」loading 态 + 提交后 ElMessage 成功提示（与 #3 一致）
  - 「发送测试邮件」loading 态（旋转 spinner） + 提交后状态卡切换（success/error）+ 输入框清空
  - 授权码眼睛图标：默认灰色（密文）→ 点击变蓝色（明文）→ 再点回灰色（密文），颜色与 HTML 原版 1:1
  - 授权码 input placeholder：未配置 → 「请输入邮箱授权码」；已配置 → 「已设置授权码，留空则保留原值」+ hint 末尾「· 已配置」绿色字
  - 安全连接 radio：3 个选项 (SSL/TLS / STARTTLS / 无加密) 自定义圆形样式
  - 端口 input `input-wrap.small` 宽度 200px（与 HTML 原版 1:1）
  - 邮箱 / 昵称 / 服务器 input `input-wrap.medium` 宽度 360px（与 HTML 原版 1:1）
  - 「修改密码」弹窗 / MinIO 测试连接 / 系统备份 等其它 9 个面板**完全不受影响**（scoped CSS 完全隔离）
- **影响范围**：
  - **修改** 1 个组件（[PanelEmail.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/PanelEmail.vue)）+ `<style scoped>` 块 140 行 + 模板移除「测试连接」按钮 + 4 处 prop 默认值 / 样式微调
  - **修改** 1 个 composable（[useEmailConfig.js](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/composables/useEmailConfig.js)）+ 4 个 computed 返回值分支
  - **不动** [Profile.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/Profile.vue) / 其它 9 个 Panel 组件 / 全部后端文件 / 端点 / 数据库

## UI 优化 → 5. 个人中心-基本资料【保存】按钮从表单左对齐移至右下角

**解决方案：** 个人中心-基本资料面板的「保存」按钮原本在表单最后一行下方，**与表单字段左对齐**（CSS `padding-left: 130px`，与 `.form-label` 110px + padding-right 20px 对齐），视觉上像"表单第 7 个字段"，与右上角的「基本资料」header 距离偏远，操作动线不合理（用户填完最后一行"自我介绍"后视线要往**左下**找按钮，再点保存）。修法：将 [PanelBasic.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/PanelBasic.vue) 的 `.form-actions` 由 `padding-left: 130px; display: flex; gap: 16px;` 改为 `display: flex; justify-content: flex-end; gap: 16px;`——保留 flex 布局（向后兼容多按钮场景），用 `justify-content: flex-end` 让按钮组贴右，移除 `padding-left: 130px` 让按钮回到 panel-body 32px 右边距上。响应式 `@media (max-width: 900px)` 仍走 `padding-left: 0` 兜底（窄屏全宽右对齐视觉更自然）。

**实现逻辑：**

- **【#5 阶段 1】[PanelBasic.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/PanelBasic.vue) `.form-actions` CSS 单行替换**（行 173）：
  - 原值：`margin-top: 8px; padding-left: 130px; display: flex; gap: 16px;`
  - 新值：`margin-top: 8px; display: flex; justify-content: flex-end; gap: 16px;`
  - 3 个属性保留（`margin-top` / `display: flex` / `gap`）+ 1 个属性替换（`padding-left: 130px` → `justify-content: flex-end`）
- **【#5 阶段 2】响应式保持不变**：`@media (max-width: 900px)` 仍保留 `padding-left: 0` 兜底（窄屏左对齐无关紧要，右对齐已能容下按钮）
- **不变量**：
  - 0 模板改动（[PanelBasic.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/PanelBasic.vue) 模板 0 改动，仅 `.form-actions` 1 行 CSS）
  - 0 业务逻辑改动（`@click="$emit('save')"` 事件 0 改动，`useProfile.js` 的 `saveBasic` 0 改动）
  - 0 父级串联改动（[Profile.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/Profile.vue) 0 改动，PanelBasic 的 props / events 1:1 透传）
  - 0 其它面板影响（仅 PanelBasic.vue scoped CSS 改动，scoped 隔离，PanelMinIO / PanelEmail / PanelPrivacy 等 9 个面板 `.form-actions` 0 改动）
  - 0 字段 / 校验 / API 改动
  - 0 后端改动（仅前端 CSS 视觉调整）
- **端到端验证**：
  - 视觉：进个人中心 → 基本资料面板 → 「保存」按钮位于 panel-body **右下角**（与 panel-body 右边距 32px 齐平）
  - 按钮宽度自适应（`padding: 9px 24px` + 文字宽度），不会撑满整行
  - hover 态：背景色 `#00a1d6` → `#008db1`（与 #1 一致）
  - loading 态：「保存」文字（`saveBasic` 同步在父级 useProfile 中做 savingBasic 标志，**当前未实装 spinner，文案不变**）
  - 提交后 ElMessage 成功提示（`useProfile.js` `saveBasic` 函数 0 改动）
  - 切到「头像 / 隐私 / 推送 / 账号安全 / MinIO / 超时登出 / 邮箱设置 / 系统备份 / 账号注销」9 个面板，**保存/测试按钮位置不变**（scoped CSS 隔离，仅 PanelBasic 的 `.form-actions` 改了）
  - 窄屏（< 900px）响应式：表单纵向折叠 + label 上移 + form-actions 右对齐（`padding-left: 0` 不影响 `justify-content: flex-end`）
- **影响范围**：
  - **修改** 1 个组件（[PanelBasic.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/PanelBasic.vue)）scoped CSS 1 行替换
  - **不动** 其它 9 个 Panel 组件 / 父页 [Profile.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/Profile.vue) / composables / api / 后端全部

## UI 优化 → 6. 个人中心-推送设置【保存】按钮从表单左对齐移至页面正下方居中

**解决方案：** 个人中心-推送设置面板的「保存」按钮原本在 4 个开关行下方，**与表单左对齐**（CSS `padding-left: 130px`，与 `.form-label` 110px + padding-right 20px 对齐）。但推送设置面板**没有 form-label / form-row 结构**（4 行均为 `.switch-row` 横向 `display: flex; justify-content: space-between`），左对齐 130px 的 `padding-left` 在视觉上完全脱节——既不与任何元素对齐，也不是面板内容流的一部分，**位置感像"游离"的孤儿按钮**。修法：将 [PanelPush.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/PanelPush.vue) 的 `.form-actions` 由 `padding-left: 130px; display: flex; gap: 16px;` 改为 `display: flex; justify-content: center; gap: 16px;`——保留 flex 布局（向后兼容多按钮场景），用 `justify-content: center` 让按钮组**水平居中**，移除 `padding-left: 130px` 让按钮回到 panel-body 自然左边界起点、flex 自动居中到整行中央。

**实现逻辑：**

- **【#6 阶段 1】[PanelPush.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/PanelPush.vue) `.form-actions` CSS 单行替换**（行 105）：
  - 原值：`margin-top: 8px; padding-left: 130px; display: flex; gap: 16px;`
  - 新值：`margin-top: 8px; display: flex; justify-content: center; gap: 16px;`
  - 3 个属性保留（`margin-top` / `display: flex` / `gap`）+ 1 个属性替换（`padding-left: 130px` → `justify-content: center`）
- **【#6 阶段 2】响应式保持不变**：`@media (max-width: 900px)` 仍保留 `padding-left: 0` 兜底（窄屏 900px 以下居中与左对齐视觉差异微弱，`justify-content: center` 自动接管）
- **不变量**：
  - 0 模板改动（[PanelPush.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/PanelPush.vue) 模板 0 改动，仅 `.form-actions` 1 行 CSS）
  - 0 业务逻辑改动（`@click="$emit('save')"` 事件 0 改动，`useProfile.js` 的 `saveSettings` 0 改动）
  - 0 父级串联改动（[Profile.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/Profile.vue) 0 改动，PanelPush 的 props / events 1:1 透传）
  - 0 其它面板影响（仅 PanelPush.vue scoped CSS 改动，scoped 隔离，PanelMinIO / PanelEmail / PanelBasic / PanelPrivacy 等 10 个面板 `.form-actions` 0 改动）
  - 0 字段 / 校验 / API 改动（推送设置 4 个开关 + 1 个保存按钮结构不变）
  - 0 后端改动（仅前端 CSS 视觉调整）
- **端到端验证**：
  - 视觉：进个人中心 → 推送设置面板 → 「保存」按钮位于 panel-body **正下方居中**（左右两边距相等，水平居中于面板内容区 130px-32px 的中央）
  - 按钮宽度自适应（`padding: 9px 24px` + 文字宽度），不会撑满整行
  - hover 态：背景色 `#00a1d6` → `#008db1`（与 #5 一致）
  - 提交后 ElMessage 成功提示（`useProfile.js` `saveSettings('push')` 函数 0 改动）
  - 切到「基本资料 / 头像 / 隐私 / 账号安全 / MinIO / 超时登出 / 邮箱设置 / 系统备份 / 账号注销」9 个面板，**保存/测试按钮位置不变**（scoped CSS 隔离，仅 PanelPush 的 `.form-actions` 改了）
  - 4 个开关（`push_balance_remind` / `push_price_alert` / `push_system_notice` / `push_weekly_report`）的 toggle 交互 0 影响（`.switch-row` / `.toggle` 样式未变）
  - 窄屏（< 900px）响应式：居中布局保持（`padding-left: 0` 不影响 `justify-content: center`）
- **影响范围**：
  - **修改** 1 个组件（[PanelPush.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/PanelPush.vue)）scoped CSS 1 行替换
  - **不动** 其它 10 个 Panel 组件 / 父页 [Profile.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/Profile.vue) / composables / api / 后端全部

## UI 优化 → 7. 4 个面板【保存】按钮统一移至页面正下方居中

**解决方案：** 用户实测发现个人中心面板【保存】按钮位置视觉混乱——4 个面板「基本资料 / 超时登出 / 系统备份 / 邮箱设置」的【保存】按钮位置各不相同：
- [PanelBasic.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/PanelBasic.vue) 在 #5 中已改为右下角（`justify-content: flex-end`）
- [PanelTimeout.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/PanelTimeout.vue) / [BackupPanel.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/BackupPanel.vue) / [PanelEmail.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/PanelEmail.vue) 仍走 HTML 原版 `padding-left: 130px`（与 form-label 110px + padding-right 20px 对齐的左对齐）

视觉不一致 + 部分面板无 form-label 结构（左对齐脱节）= 用户操作动线混乱。修法：4 个面板的 `.form-actions` **统一**改为 `display: flex; justify-content: center; gap: 16px;`——保留 flex 布局（向后兼容多按钮场景），用 `justify-content: center` 让按钮组**水平居中**，移除 `padding-left: 130px`（无论是否有 form-label 都能视觉居中）。与 #5 推送设置（[PanelPush.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/PanelPush.vue) 已在 #6 中居中）保持一致 —— **5 个面板（推送设置 + 4 个本轮）全部统一为「页面正下方居中」**。

**实现逻辑：**

- **【#7 阶段 1】[PanelBasic.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/PanelBasic.vue) `.form-actions` CSS 替换**（行 173）：
  - 原值：`margin-top: 8px; display: flex; justify-content: flex-end; gap: 16px;`（#5 右下角）
  - 新值：`margin-top: 8px; display: flex; justify-content: center; gap: 16px;`（正下方居中）
  - 1 个属性替换（`justify-content: flex-end` → `justify-content: center`）
- **【#7 阶段 2】[PanelTimeout.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/PanelTimeout.vue) `.form-actions` CSS 替换**（行 179）：
  - 原值：`margin-top: 8px; padding-left: 130px; display: flex; gap: 16px;`
  - 新值：`margin-top: 8px; display: flex; justify-content: center; gap: 16px;`
- **【#7 阶段 3】[BackupPanel.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/BackupPanel.vue) `.form-actions` CSS 替换**（行 428-433）：
  - 原值：`margin-top: 8px; padding-left: 130px;`（多行格式，保留 `.form-actions .btn + .btn { margin-left: 12px; }`）
  - 新值：`margin-top: 8px; display: flex; justify-content: center; gap: 16px;`
  - 保留 `.form-actions .btn + .btn { margin-left: 12px; }`（历史多按钮间距规则，gap 16px 已涵盖，此规则 0 影响但保留以兼容旧调用）
- **【#7 阶段 4】[PanelEmail.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/PanelEmail.vue) `.form-actions` CSS 替换**（行 364）：
  - 原值：`margin-top: 8px; padding-left: 130px;`
  - 新值：`margin-top: 8px; display: flex; justify-content: center; gap: 16px;`
- **不变量**：
  - 0 模板改动（4 个 Panel 组件模板 0 改动，仅 `.form-actions` 1 行 CSS）
  - 0 业务逻辑改动（`@click="$emit('save')"` / `saveBasic` / `saveSettings` / `saveBackupSettings` / `saveTimeoutConfig` / `saveEmailConfig` 0 改动）
  - 0 父级串联改动（[Profile.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/Profile.vue) 0 改动，4 个 Panel 的 props / events 1:1 透传）
  - 0 其它面板影响（仅这 4 个 Panel scoped CSS 改动，scoped 隔离，其它 6 个 Panel `.form-actions` 0 改动）
  - 0 字段 / 校验 / API 改动
  - 0 后端改动（仅前端 CSS 视觉统一）
- **端到端验证**：
  - 视觉：进个人中心 → 切到 4 个面板 → 「保存 / 保存设置 / 保存配置」按钮全部位于 panel-body **正下方水平居中**（左右两边距相等）
  - 5 个面板（推送设置 #6 + 本轮 4 个）按钮位置 100% 一致，全部「页面正下方居中」
  - 按钮宽度自适应（`padding: 9px 24px` + 文字宽度），不会撑满整行
  - hover 态：背景色 `#00a1d6` → `#008db1`（与 #5 / #6 一致）
  - loading 态：保存中文案变为「保存中...」/「保存设置中...」（仅 PanelTimeout 做了 loading 态文案，#7 0 改动）
  - 提交后 ElMessage 成功提示（`useProfile.js` / `useBackup.js` 0 改动）
  - 切到其它 6 个面板（头像 / 隐私 / 账号安全 / MinIO / 账号注销 / 修改密码弹窗），**保存/测试按钮位置不变**（scoped CSS 隔离）
  - 窄屏（< 900px）响应式：居中布局保持（`padding-left: 0` 不影响 `justify-content: center`）
- **影响范围**：
  - **修改** 4 个组件（[PanelBasic.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/PanelBasic.vue) / [PanelTimeout.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/PanelTimeout.vue) / [BackupPanel.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/BackupPanel.vue) / [PanelEmail.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/PanelEmail.vue)）scoped CSS 各 1 行替换
  - **不动** 其它 7 个 Panel 组件 / 父页 [Profile.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/Profile.vue) / composables / api / 后端全部

## UI 优化 → 8. 个人中心-头像设置【内容水平居中 + 保存按钮移至页面正下方居中】

**解决方案：** 用户实测头像设置面板发现两个问题：
1. **正文内容左对齐** — `.avatar-section` 用 `display: flex; gap: 40px; align-items: flex-start;`（无 `justify-content`），默认 `flex-start` 让「当前头像 + 预览头像」两个预览框+「更换头像 + 格式说明卡」全部**左对齐到 panel-body 左边**，与 panel-body 32px 左边距齐平——视觉上像偏左的孤儿，浪费了右侧大片留白
2. **保存按钮左对齐 130px** — `.form-actions` 走 HTML 原版 `padding-left: 130px`（与 form-label 110px + padding-right 20px 对齐），但头像面板**无 form-label 结构**，130px 的 padding-left 在视觉上完全脱节

修法：
- [PanelAvatar.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/PanelAvatar.vue) `.avatar-section` 新增 `justify-content: center` — 头像预览框组（当前头像 200×200 + 预览头像 100×100）水平居中
- 同步 `.form-actions` 走 #7 统一规范 `display: flex; justify-content: center; gap: 16px;` — 移除 `padding-left: 130px`，按钮组水平居中

**实现逻辑：**

- **【#8 阶段 1】[PanelAvatar.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/PanelAvatar.vue) `.avatar-section` CSS 追加属性**（行 67）：
  - 原值：`display: flex; gap: 40px; align-items: flex-start;`
  - 新值：`display: flex; gap: 40px; align-items: flex-start; justify-content: center;`
  - 3 个属性保留（`display: flex` / `gap: 40px` / `align-items: flex-start`）+ 1 个属性追加（`justify-content: center`）
- **【#8 阶段 2】[PanelAvatar.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/PanelAvatar.vue) `.form-actions` CSS 替换**（行 95）：
  - 原值：`margin-top: 8px; padding-left: 130px; display: flex; gap: 16px;`
  - 新值：`margin-top: 8px; display: flex; justify-content: center; gap: 16px;`
  - 走 #7 统一规范（与基本资料 / 超时登出 / 系统备份 / 邮箱设置 / 推送设置 5 个面板 100% 一致）
- **不变量**：
  - 0 模板改动（[PanelAvatar.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/PanelAvatar.vue) 模板 0 改动，仅 `.avatar-section` + `.form-actions` 2 行 CSS）
  - 0 业务逻辑改动（`@click="$emit('save')"` 事件 0 改动，`useProfile.js` 的 `saveAvatar` 0 改动）
  - 0 父级串联改动（[Profile.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/Profile.vue) 0 改动，PanelAvatar 的 props / events 1:1 透传）
  - 0 其它面板影响（仅 PanelAvatar.vue scoped CSS 改动，scoped 隔离，其它 10 个 Panel `.form-actions` / `.avatar-section` 0 改动）
  - 0 字段 / 校验 / API 改动（头像 2 个 img + 1 个 file input + 1 个保存按钮结构不变）
  - 0 后端改动（仅前端 CSS 视觉调整）
- **端到端验证**：
  - 视觉：进个人中心 → 头像设置面板 → 「当前头像 + 预览头像」两个预览框**水平居中**（与 panel-body 左右边距对称），下方「更换头像」链接 + 格式说明卡也跟着居中（同一列），「保存」按钮位于 panel-body **正下方水平居中**
  - 「当前头像 200×200」+「预览头像 100×100」间距 40px（`gap: 40px` 不变）
  - 「更换头像」链接和格式说明卡 320px max-width（`.avatar-tips { max-width: 320px; }` 不变）
  - 格式说明卡「支持 jpg、jpeg、png、gif 格式 / 文件大小不超过 5MB / 建议尺寸 200×200 像素以上」3 行内容居中
  - hover 态：保存按钮背景色 `#00a1d6` → `#008db1`（与 #5 / #6 / #7 一致）
  - 提交后 ElMessage 成功提示（`useProfile.js` `saveAvatar` 函数 0 改动）
  - 切到「基本资料 / 隐私 / 推送 / 账号安全 / MinIO / 超时登出 / 邮箱设置 / 系统备份 / 账号注销」9 个面板，**保存/测试按钮位置不变**（scoped CSS 隔离）
  - 窄屏（< 900px）响应式：`.avatar-section` 走 `flex-direction: column; align-items: center;` 兜底（`justify-content: center` 在 column 方向上无作用，`align-items: center` 接管），两预览框纵向堆叠 + 水平居中
- **影响范围**：
  - **修改** 1 个组件（[PanelAvatar.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/PanelAvatar.vue)）scoped CSS 2 行替换（`.avatar-section` + `.form-actions`）
  - **不动** 其它 10 个 Panel 组件 / 父页 [Profile.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/Profile.vue) / composables / api / 后端全部

## UI 优化 → 9. 个人中心-MinIO 设置【前端内容水平居中（状态卡 + 表单行 + 操作按钮）】

**解决方案：** 用户实测 MinIO 设置面板发现内容**整体偏左**——`.minio-status-card` 状态卡、6 个 `.form-row` 表单行、`.form-actions` 操作按钮三块内容全部**左对齐到 panel-body 左边**（32px 左边距），输入框 max-width 400/480px 偏左挤压，右侧大片留白浪费，与 #8 头像面板居中的视觉风格不一致。修法：三块内容统一走「居中」布局：
- `.minio-status-card` 追加 `max-width: 640px; margin: 0 auto 24px;` — 状态卡限宽 640px + 水平居中
- `.form-row` 由 `margin-bottom: 24px` 改为 `max-width: 640px; margin: 0 auto 24px;` — 6 个表单行限宽 640px + 水平居中（label 110px + 输入框 max-width 400/480px 都能容纳）
- `.form-actions` 走 #7 统一规范 `display: flex; justify-content: center; gap: 16px;` — 移除 `padding-left: 130px`，操作按钮组水平居中

**实现逻辑：**

- **【#9 阶段 1】[PanelMinIO.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/PanelMinIO.vue) `.minio-status-card` CSS 修改**（行 125-129）：
  - 原值：`display: flex; align-items: center; gap: 16px; padding: 16px 20px; background: #f6ffed; border: 1px solid #b7eb8f; border-radius: 8px; margin-bottom: 24px;`
  - 新值：`display: flex; align-items: center; gap: 16px; padding: 16px 20px; background: #f6ffed; border: 1px solid #b7eb8f; border-radius: 8px; max-width: 640px; margin: 0 auto 24px;`
  - `margin-bottom: 24px` → `margin: 0 auto 24px`（居中）+ 追加 `max-width: 640px`（限宽）
- **【#9 阶段 2】[PanelMinIO.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/PanelMinIO.vue) `.form-row` CSS 修改**（行 138）：
  - 原值：`display: flex; align-items: flex-start; margin-bottom: 24px;`
  - 新值：`display: flex; align-items: flex-start; max-width: 640px; margin: 0 auto 24px;`
  - `margin-bottom: 24px` → `margin: 0 auto 24px`（居中）+ 追加 `max-width: 640px`（限宽）
- **【#9 阶段 3】[PanelMinIO.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/PanelMinIO.vue) `.form-actions` CSS 修改**（行 177）：
  - 原值：`margin-top: 8px; padding-left: 130px; display: flex; gap: 16px;`
  - 新值：`margin-top: 8px; display: flex; justify-content: center; gap: 16px;`
  - 走 #7 统一规范（与基本资料 / 超时登出 / 系统备份 / 邮箱设置 / 推送设置 / 头像设置 6 个面板 100% 一致）
- **不变量**：
  - 0 模板改动（[PanelMinIO.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/PanelMinIO.vue) 模板 0 改动，仅 3 条 CSS 规则）
  - 0 业务逻辑改动（`@click="$emit('test')"` / `$emit('save')` / `$emit('reset')` 事件 0 改动，`useProfile.js` 的 `testMinIOConnection` / `saveMinIOConfig` / `resetMinIOConfig` 0 改动）
  - 0 父级串联改动（[Profile.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/Profile.vue) 0 改动，PanelMinIO 的 props / events 1:1 透传）
  - 0 其它面板影响（仅 PanelMinIO.vue scoped CSS 改动，scoped 隔离，其它 10 个 Panel 0 改动）
  - 0 字段 / 校验 / API 改动（6 个字段 + 状态卡 + 3 个按钮结构不变）
  - 0 后端改动（仅前端 CSS 视觉调整）
- **端到端验证**：
  - 视觉：进个人中心 → MinIO 设置面板 → 「状态卡 + 6 个表单行 + 操作按钮」三块内容**全部水平居中**（限宽 640px 块居中于 panel-body 左右边距对称），不再偏左
  - 状态卡 640px 宽居中（`.minio-status-card`），与表单行 640px 对齐（同一左/右边线）
  - 6 个表单行（服务器地址 / Access Key / Secret Key / Bucket 名称 / 图片访问域名 / 安全连接）全部限宽 640px 居中，label 110px + 输入框 400px 布局不变
  - 输入框 max-width 400px（`.input-wrap.medium`）不变，仅整行位置居中
  - 「安全连接」radio-group（HTTP 内网 / HTTPS 公网）随行居中
  - 3 个按钮（测试连接 / 保存配置 / 恢复默认）水平居中，间距 16px（`gap: 16px`）
  - hover 态：测试连接 `#52c41a` → `#389e0d`、保存配置 `#00a1d6` → `#008db1`、恢复默认边框变蓝（0 改动）
  - loading 态：测试中... / 恢复中... 文案（0 改动）
  - 切到其它 10 个面板，内容/按钮位置不变（scoped CSS 隔离）
  - 窄屏（< 900px）响应式：`.form-row { flex-direction: column; }` + `max-width: 640px; margin: 0 auto` 兼容（块仍居中，列向堆叠）
- **影响范围**：
  - **修改** 1 个组件（[PanelMinIO.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/PanelMinIO.vue)）scoped CSS 3 条规则修改（`.minio-status-card` + `.form-row` + `.form-actions`）
  - **不动** 其它 10 个 Panel 组件 / 父页 [Profile.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/Profile.vue) / composables / api / 后端全部

## 缺陷修复 → 10. 邮箱设置-授权码/密码保存后显示为空

**解决方案：** 用户实测邮箱设置面板发现：保存配置后「授权码 / 密码」输入框**显示为空**。根因：后端出于安全考虑**不回传原密码**，仅回传 `smtp_password_set` 布尔值；而前端 [useEmailConfig.js](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/composables/useEmailConfig.js) 在保存成功后执行 `smtp_password: ''`（避免明文滞留内存），导致 `smtp_password_set === true` 时输入框仍显示空白，视觉上像"授权码丢了"。修法：**已配置时用掩码「••••••••」填充输入框**（不暴露明文、输入框看起来非空），用户可感知"授权码已配置"；输入新授权码时才覆盖原值，掩码值绝不提交到后端。

**实现逻辑：**

- **【#10 阶段 1】[useEmailConfig.js](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/composables/useEmailConfig.js) 新增掩码常量**（行 14-15）：
  - `const PASSWORD_MASK = '••••••••'`
  - 统一掩码串，供拉取 / 保存 / 提交跳过三处复用
- **【#10 阶段 2】`loadEmailConfig` 回填掩码**（行 93）：`smtp_password: data.smtp_password_set ? PASSWORD_MASK : ''` — 已配置 → 掩码；未配置 → 空
- **【#10 阶段 3】`saveEmailConfig` 提交跳过掩码**（行 118-120）：`if (emailConfig.value.smtp_password && emailConfig.value.smtp_password !== PASSWORD_MASK) { payload.smtp_password = ... }` — 掩码/空值不覆盖后端已存密码
- **【#10 阶段 4】`saveEmailConfig` 回填掩码**（行 133）：保存成功后同样 `data.smtp_password_set ? PASSWORD_MASK : ''` — 输入框显示掩码不再为空
- **【#10 阶段 5】[PanelEmail.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/PanelEmail.vue) placeholder 语义更新**（行 117）：
  - 原值：`'已设置授权码，留空则保留原值'`
  - 新值：`'已配置授权码（••••••••），如需更换请输入新授权码'`（清除掩码后 placeholder 才可见，提示语义更准确）
- **不变量**：
  - 0 后端改动（后端不回传密码的设计不变，`smtp_password_set` 布尔值语义不变）
  - 0 API 改动（[emailApi.js](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/api/emailApi.js) 4 个端点 0 改动）
  - 0 父级串联改动（[Profile.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/Profile.vue) 0 改动，props / events 1:1 透传）
  - 0 字段结构改动（授权码输入框 type password/text + eye 图标 + hint 三件套不变）
  - 0 明文泄露风险（掩码仅存在于前端内存字段，后端存储/回传逻辑 0 改动）
  - 0 其它面板影响（仅 useEmailConfig.js + PanelEmail.vue 两个文件，scoped 隔离）
- **端到端验证**：
  - 视觉：进个人中心 → 邮箱设置 → 已保存过授权码的用户，输入框显示「••••••••」掩码（不再是空白）
  - 保存新授权码 → 保存成功后输入框仍显示「••••••••」掩码（不再空白）
  - 初次使用未配置授权码 → 输入框显示 placeholder「请输入邮箱授权码」
  - 清除掩码 → placeholder 显示「已配置授权码（••••••••），如需更换请输入新授权码」（提示原密码仍生效）
  - 不输入新授权码直接保存 → 后端原密码保留（payload 不含 smtp_password）
  - 输入新授权码保存 → 后端密码更新，输入框回填掩码
  - eye 图标切换明文/密文 → 掩码形态不变（不暴露真实密码）
  - hint 下方「· 已配置」绿色标记仍在（`smtp_password_set` 驱动，0 改动）
  - 切到其它 10 个面板再切回 → 输入框掩码保留（loadEmailConfig 重新回填掩码）
- **影响范围**：
  - **修改** 2 个文件（[useEmailConfig.js](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/composables/useEmailConfig.js) 掩码常量 + 3 处回填/跳过逻辑 + [PanelEmail.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/PanelEmail.vue) placeholder 1 行）
  - **不动** 后端 / API 层 / [Profile.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/Profile.vue) / 其它 10 个 Panel 组件

## UI 优化 → 11. 账号安全-security-item 宽度与页面宽度对齐

**解决方案：** 用户实测账号安全面板发现 `.security-item` 行宽与页面宽度**不一致**——`.security-list` 被限宽 `max-width: 640px`，而页面其他面板内容（如基本资料表单）撑满 panel-body 全宽。导致账号安全 3 个条目（邮箱地址 / 登录密码 / 用户名）整体缩窄 640px，右侧留白 300px+，与页面其他面板的横向基准不一致。修法：将 [PanelSecurity.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/PanelSecurity.vue) 的 `.security-list { max-width: 640px; }` 改为 `.security-list { width: 100%; }`——移除 640px 限宽，让列表撑满 panel-body 全宽（panel-body 32px 左右 padding 内），security-item 的 flex 两端对齐（`justify-content: space-between`）自动把按钮推到最右端，与页面其他面板的宽度基准统一。

**实现逻辑：**

- **【#11 阶段 1】[PanelSecurity.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/PanelSecurity.vue) `.security-list` CSS 替换**（行 57）：
  - 原值：`max-width: 640px;`
  - 新值：`width: 100%;`
  - 1 个属性替换（max-width 限宽 → width 撑满全宽）
- **不变量**：
  - 0 模板改动（[PanelSecurity.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/PanelSecurity.vue) 模板 0 改动，仅 `.security-list` 1 行 CSS）
  - 0 业务逻辑改动（`@click="$emit('open-change-password')"` 事件 0 改动，`useSecurity.js` 0 改动）
  - 0 父级串联改动（[Profile.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/Profile.vue) 0 改动，PanelSecurity 的 props / events 1:1 透传）
  - 0 其它面板影响（仅 PanelSecurity.vue scoped CSS 改动，scoped 隔离，其它 11 个 Panel `.security-list` / `.security-item` 0 改动）
  - 0 字段 / 校验 / API 改动（3 个 security-item 结构不变）
  - 0 后端改动（仅前端 CSS 视觉调整）
- **端到端验证**：
  - 视觉：进个人中心 → 账号安全面板 → 「邮箱地址 / 登录密码 / 用户名」3 个条目撑满 panel-body 全宽，与页面其他面板内容宽度基准一致
  - 按钮位置：「修改邮箱 / 修改密码 / 修改用户名」按钮位于每条最右端（`justify-content: space-between` 两端对齐不变）
  - 条目间距：`padding: 20px 0` + `border-bottom: 1px solid #e3e5e7` 分隔线不变
  - 按钮 hover 态：「修改密码」边框/文字变蓝 `#00a1d6`（0 改动）
  - disabled 态：「修改邮箱 / 修改用户名」半透明 + 不可点击（0 改动）
  - 切到其它 11 个面板，内容/按钮位置不变（scoped CSS 隔离）
  - 窄屏（< 900px）响应式：`.security-item` flex 两端对齐保持，按钮不换行
- **影响范围**：
  - **修改** 1 个组件（[PanelSecurity.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/PanelSecurity.vue)）scoped CSS 1 行替换
  - **不动** 其它 11 个 Panel 组件 / 父页 [Profile.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/Profile.vue) / composables / api / 后端全部

## UI 优化 → 12. 超时登出-前端内容水平居中（信息卡 + 表单行 + 操作按钮）

**解决方案：** 用户实测超时登出面板发现内容**整体偏左**——`.timeout-info-card` 安全提示卡（max-width 640px 左对齐）、2 个 `.form-row` 表单行（超时时间 + 倒计时提醒）、`.form-actions` 操作按钮三块内容全部**左对齐到 panel-body 左边**（32px 左边距），右侧大片留白浪费，与 #8 头像面板 / #9 MinIO 面板居中的视觉风格不一致。修法：与 #9 MinIO 完全相同的方案——三块内容统一走「居中」布局：
- `.timeout-info-card` 由 `margin-bottom: 24px; max-width: 640px;` 改为 `max-width: 640px; margin: 0 auto 24px;` — 信息卡限宽 640px + 水平居中
- `.form-row` 由 `margin-bottom: 24px` 改为 `max-width: 640px; margin: 0 auto 24px;` — 2 个表单行限宽 640px + 水平居中（label 110px + 选项区 max-width 480px 都能容纳）
- `.form-actions` 已是 #7 统一规范 `justify-content: center`（0 改动）— 操作按钮组水平居中

**实现逻辑：**

- **【#12 阶段 1】[PanelTimeout.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/PanelTimeout.vue) `.timeout-info-card` CSS 修改**（行 124-128）：
  - 原值：`display: flex; align-items: flex-start; gap: 12px; padding: 16px 20px; border-radius: 8px; background: #f6f7f8; border: 1px solid #e3e5e7; margin-bottom: 24px; max-width: 640px;`
  - 新值：`display: flex; align-items: flex-start; gap: 12px; padding: 16px 20px; border-radius: 8px; background: #f6f7f8; border: 1px solid #e3e5e7; max-width: 640px; margin: 0 auto 24px;`
  - `margin-bottom: 24px` → `margin: 0 auto 24px`（居中）
- **【#12 阶段 2】[PanelTimeout.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/PanelTimeout.vue) `.form-row` CSS 修改**（行 133）：
  - 原值：`display: flex; align-items: flex-start; margin-bottom: 24px;`
  - 新值：`display: flex; align-items: flex-start; max-width: 640px; margin: 0 auto 24px;`
  - `margin-bottom: 24px` → `margin: 0 auto 24px`（居中）+ 追加 `max-width: 640px`（限宽）
- **【#12 阶段 3】`.form-actions` 0 改动**（行 179）：已由 #7 统一为 `display: flex; justify-content: center; gap: 16px;`，无需重复修改
- **不变量**：
  - 0 模板改动（[PanelTimeout.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/PanelTimeout.vue) 模板 0 改动，仅 2 条 CSS 规则）
  - 0 业务逻辑改动（`@click="$emit('select-timeout', minutes)"` / `@click="$emit('save')"` 事件 0 改动，`useProfile.js` 的 `selectTimeout` / `saveTimeoutConfig` 0 改动）
  - 0 父级串联改动（[Profile.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/Profile.vue) 0 改动，PanelTimeout 的 props / events 1:1 透传）
  - 0 其它面板影响（仅 PanelTimeout.vue scoped CSS 改动，scoped 隔离，其它 11 个 Panel 0 改动）
  - 0 字段 / 校验 / API 改动（5 个超时选项 + 2 个提醒 radio + 保存按钮结构不变）
  - 0 后端改动（仅前端 CSS 视觉调整）
- **端到端验证**：
  - 视觉：进个人中心 → 超时登出面板 → 「安全提示卡 + 超时时间表单行 + 倒计时提醒表单行 + 操作按钮」四块内容**全部水平居中**（限宽 640px 块居中于 panel-body 左右边距对称），不再偏左
  - 安全提示卡 640px 宽居中（`.timeout-info-card`），与表单行 640px 对齐（同一左/右边线）
  - 5 个超时选项卡片（30分钟 / 1小时 / 2小时 / 3小时 / 从不超时）纵向堆叠限宽 480px（`.timeout-options { max-width: 480px; }`）不变，整行位置居中
  - 「倒计时提醒」radio-group（超时前 30 秒弹窗提醒 / 直接登出不提醒）随行居中
  - 「保存设置」按钮水平居中（#7 已统一，0 改动），loading 态文案「保存中...」（0 改动）
  - 超时选项 hover/selected 态：边框变蓝 + 淡蓝背景（0 改动）
  - 切到其它 11 个面板，内容/按钮位置不变（scoped CSS 隔离）
  - 窄屏（< 900px）响应式：`.form-row { flex-direction: column; }` + `max-width: 640px; margin: 0 auto` 兼容（块仍居中，列向堆叠）
- **影响范围**：
  - **修改** 1 个组件（[PanelTimeout.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/PanelTimeout.vue)）scoped CSS 2 条规则修改（`.timeout-info-card` + `.form-row`）
  - **不动** 其它 11 个 Panel 组件 / 父页 [Profile.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/Profile.vue) / composables / api / 后端全部

## UI 优化 → 13. 系统备份-前端内容水平居中（信息卡 + 表单行 + 操作按钮）

**解决方案：** 用户实测系统备份面板发现内容**整体偏左**——`.timeout-info-card` 备份范围说明卡（max-width 640px 左对齐）、4 个 `.form-row` 表单行（手动备份 / 自动备份 / 备份历史 / 数据恢复）、`.form-actions` 操作按钮全部**左对齐到 panel-body 左边**（32px 左边距），右侧大片留白浪费，与 #8 头像面板 / #9 MinIO 面板 / #12 超时登出面板居中的视觉风格不一致。修法：与 #9 / #12 完全相同的方案——三块内容统一走「居中」布局：
- `.timeout-info-card` 由 `margin-bottom: 24px; max-width: 640px;` 改为 `max-width: 640px; margin: 0 auto 24px;` — 信息卡限宽 640px + 水平居中
- `.form-row` 由 `margin-bottom: 24px` 改为 `max-width: 640px; margin: 0 auto 24px;` — 4 个表单行限宽 640px + 水平居中（label 110px + 内容区 max-width 480/720px 都能容纳）
- `.form-actions` 已是 #7 统一规范 `justify-content: center`（0 改动）— 操作按钮组水平居中

**实现逻辑：**

- **【#13 阶段 1】[BackupPanel.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/BackupPanel.vue) `.form-row` CSS 修改**（行 410-415）：
  - 原值：`display: flex; align-items: flex-start; margin-bottom: 24px;`
  - 新值：`display: flex; align-items: flex-start; max-width: 640px; margin: 0 auto 24px;`
  - `margin-bottom: 24px` → `margin: 0 auto 24px`（居中）+ 追加 `max-width: 640px`（限宽）
- **【#13 阶段 2】[BackupPanel.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/BackupPanel.vue) `.timeout-info-card` CSS 修改**（行 462-472）：
  - 原值：`...border: 1px solid #e3e5e7; margin-bottom: 24px; max-width: 640px;`
  - 新值：`...border: 1px solid #e3e5e7; max-width: 640px; margin: 0 auto 24px;`
  - `margin-bottom: 24px` → `margin: 0 auto 24px`（居中）
- **【#13 阶段 3】`.form-actions` 0 改动**（行 428-433）：已由 #7 统一为 `display: flex; justify-content: center; gap: 16px;`，无需重复修改
- **不变量**：
  - 0 模板改动（[BackupPanel.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/BackupPanel.vue) 模板 0 改动，仅 2 条 CSS 规则）
  - 0 业务逻辑改动（`handleDownloadBackup` / `toggleAutoBackup` / `saveBackupSettings` / `handleRestore` / `refreshHistory` 等 useBackup 全部方法 0 改动）
  - 0 父级串联改动（[Profile.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/Profile.vue) 0 改动，BackupPanel 的 props / events 1:1 透传）
  - 0 其它面板影响（仅 BackupPanel.vue scoped CSS 改动，scoped 隔离，其它 11 个 Panel 0 改动）
  - 0 字段 / 校验 / API 改动（立即备份 / 自动备份开关 + 频率下拉 / 备份历史表格 / 数据恢复拖拽区 / 保存按钮结构不变）
  - 0 后端改动（仅前端 CSS 视觉调整）
- **端到端验证**：
  - 视觉：进个人中心 → 系统备份面板 → 「备份范围说明卡 + 手动备份行 + 自动备份行 + 备份历史行 + 数据恢复行 + 操作按钮」六块内容**全部水平居中**（限宽 640px 块居中于 panel-body 左右边距对称），不再偏左
  - 备份范围说明卡 640px 宽居中（`.timeout-info-card`），与表单行 640px 对齐（同一左/右边线）
  - 「立即备份」按钮 + 「备份完成」绿色提示条（`lastBackupResult`）随行居中
  - 自动备份 toggle 开关 + 频率/保留份数下拉 select-group（max-width 480px）随行居中
  - 备份历史表格（max-width 720px 内联）随行居中，表头 5 列 / 下载删除按钮操作列布局不变
  - 数据恢复拖拽区（max-width 480px 内联）随行居中，「点击上传或拖拽备份文件至此处」提示不变
  - 「保存设置」按钮水平居中（#7 已统一，0 改动）
  - 切到其它 11 个面板，内容/按钮位置不变（scoped CSS 隔离）
  - 窄屏（< 900px）响应式：`.form-row { flex-direction: column; }` + `max-width: 640px; margin: 0 auto` 兼容（块仍居中，列向堆叠）
- **影响范围**：
  - **修改** 1 个组件（[BackupPanel.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/BackupPanel.vue)）scoped CSS 2 条规则修改（`.form-row` + `.timeout-info-card`）
  - **不动** 其它 11 个 Panel 组件 / 父页 [Profile.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/Profile.vue) / composables / api / 后端全部

## UI 优化 → 14. 邮箱设置-前端内容水平居中（信息卡 + 状态卡 + 表单行 + 操作按钮）

**解决方案：** 用户实测邮箱设置面板发现内容**整体偏左**——`.timeout-info-card` 功能说明卡、`.minio-status-card` 连接状态卡、7 个 `.form-row` 表单行（SMTP 服务器 / 发件端口 / 发件人邮箱 / 发件人昵称 / 授权码密码 / 安全连接 / 测试收件人）、`.form-actions` 操作按钮全部**左对齐到 panel-body 左边**（32px 左边距），右侧大片留白浪费，与 #8 头像面板 / #9 MinIO 面板 / #12 超时登出面板 / #13 系统备份面板居中的视觉风格不一致。修法：与 #9 / #12 / #13 完全相同的方案——四块内容统一走「居中」布局：
- `.timeout-info-card` 由 `margin-bottom: 0; max-width: 640px;` 改为 `max-width: 640px; margin: 0 auto 24px;` — 功能说明卡限宽 640px + 水平居中
- `.minio-status-card` 由 `margin-bottom: 0` 改为 `max-width: 640px; margin: 0 auto 24px;` — 连接状态卡限宽 640px + 水平居中
- `.form-row` 由 `margin-bottom: 24px` 改为 `max-width: 640px; margin: 0 auto 24px;` — 7 个表单行限宽 640px + 水平居中（label 110px + 输入框 max-width 360px 都能容纳）
- `.form-actions` 已是 #7 统一规范 `justify-content: center`（0 改动）— 操作按钮组水平居中

**实现逻辑：**

- **【#14 阶段 1】[PanelEmail.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/PanelEmail.vue) `.form-row` CSS 修改**（行 267-272）：
  - 原值：`display: flex; align-items: flex-start; margin-bottom: 24px;`
  - 新值：`display: flex; align-items: flex-start; max-width: 640px; margin: 0 auto 24px;`
  - `margin-bottom: 24px` → `margin: 0 auto 24px`（居中）+ 追加 `max-width: 640px`（限宽）
- **【#14 阶段 2】[PanelEmail.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/PanelEmail.vue) `.timeout-info-card` CSS 修改**（行 318-328）：
  - 原值：`...border: 1px solid #e3e5e7; margin-bottom: 0; max-width: 640px;`
  - 新值：`...border: 1px solid #e3e5e7; max-width: 640px; margin: 0 auto 24px;`
  - `margin-bottom: 0` → `margin: 0 auto 24px`（居中 + 底部间距统一）
- **【#14 阶段 3】[PanelEmail.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/PanelEmail.vue) `.minio-status-card` CSS 修改**（行 342-346）：
  - 原值：`display: flex; align-items: center; gap: 16px; padding: 16px 20px; background: #f6ffed; border: 1px solid #b7eb8f; border-radius: 8px; margin-bottom: 0;`
  - 新值：`display: flex; align-items: center; gap: 16px; padding: 16px 20px; background: #f6ffed; border: 1px solid #b7eb8f; border-radius: 8px; max-width: 640px; margin: 0 auto 24px;`
  - `margin-bottom: 0` → `max-width: 640px; margin: 0 auto 24px`（限宽 + 居中）
- **【#14 阶段 4】`.form-actions` 0 改动**（行 364）：已由 #7 统一为 `display: flex; justify-content: center; gap: 16px;`，无需重复修改
- **不变量**：
  - 0 模板改动（[PanelEmail.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/PanelEmail.vue) 模板 0 改动，仅 3 条 CSS 规则）
  - 0 业务逻辑改动（`@click="$emit('save')"` / `$emit('send-test')` / `$emit('toggle-pwd')` 事件 0 改动，`useEmailConfig.js` 全部方法 0 改动）
  - 0 父级串联改动（[Profile.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/Profile.vue) 0 改动，PanelEmail 的 props / events 1:1 透传）
  - 0 其它面板影响（仅 PanelEmail.vue scoped CSS 改动，scoped 隔离，其它 11 个 Panel 0 改动）
  - 0 字段 / 校验 / API 改动（SMTP 7 字段 + 状态卡 + 功能说明卡 + 保存/测试按钮结构不变）
  - 0 后端改动（仅前端 CSS 视觉调整）
- **端到端验证**：
  - 视觉：进个人中心 → 邮箱设置面板 → 「功能说明卡 + 连接状态卡 + 7 个表单行 + 操作按钮」四块内容**全部水平居中**（限宽 640px 块居中于 panel-body 左右边距对称），不再偏左
  - 功能说明卡 640px 宽居中（`.timeout-info-card`，蓝底 `#f0f9ff` + 蓝色图标），与表单行 640px 对齐（同一左/右边线）
  - 连接状态卡 640px 宽居中（`.minio-status-card`，初始灰 icon + 绿底 / warning 黄底 / error 粉底三态不变）
  - 7 个表单行全部限宽 640px 居中：SMTP 服务器 / 发件端口（`.input-wrap.small` 200px）/ 发件人邮箱 / 发件人昵称 / 授权码密码（含 eye 图标）/ 安全连接（radio 三选）/ 测试收件人（输入框 + 发送测试邮件按钮）
  - 输入框 max-width 360px（`.input-wrap.medium`）不变，仅整行位置居中
  - 「发送测试邮件」按钮 + 「保存配置」按钮水平居中（#7 已统一），loading 态文案「发送中... / 保存中...」（0 改动）
  - eye 图标切换明文/密文 + 掩码「••••••••」（#10 已修复）0 改动
  - 切到其它 11 个面板，内容/按钮位置不变（scoped CSS 隔离）
  - 窄屏（< 900px）响应式：`.form-row { flex-direction: column; }` + `max-width: 640px; margin: 0 auto` 兼容（块仍居中，列向堆叠）
- **影响范围**：
  - **修改** 1 个组件（[PanelEmail.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/PanelEmail.vue)）scoped CSS 3 条规则修改（`.form-row` + `.timeout-info-card` + `.minio-status-card`）
  - **不动** 其它 11 个 Panel 组件 / 父页 [Profile.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/Profile.vue) / composables / api / 后端全部

## 新增需求 → 15. 登录页-高保真重写 + 忘记密码重置密码全流程

**解决方案：** 用户实测登录页体验差、缺少「忘记密码」入口，要求按 `e:\项目编写\项目编写\FigureBox\login_with_forgot_password.html` 高保真改造：4 个视图（登录 / 忘记密码-验证邮箱 / 忘记密码-设置新密码 / 重置成功）走 SPA 视图切换，业务流程 3 步（请求验证码 → 校验验证码 → 重置密码）。前端按组件目录分离（`api/authApi.js` + `composables/useLoginFlow.js` + `components/LoginView.vue` + `components/ForgotStep1View.vue` + `components/ForgotStep2View.vue` + `components/ResetSuccessView.vue`），后端按企业级服务层架构（`app/models/password_reset.py` 模型 + `app/services/auth_service/password_reset_service.py` 服务 + `app/api/password_reset.py` API 路由）。密码重置流程必须基于用户在「系统备份-邮箱设置」中配置的 SMTP 通道发送 6 位验证码，未配置时返回友好错误「管理员尚未配置 SMTP 发件通道，请联系管理员」。

**实现逻辑：**

- **【#15 阶段 1】后端 - 新增 [PasswordResetCode](file:///e:/项目编写/项目编写/FigureBox/backend/app/models/password_reset.py) 数据模型**（自动建表）
  - 5 个字段：id（自增主键）、email（注册邮箱 + 索引）、code（6 位明文验证码）、expires_at（10 分钟过期）、is_used（0 未用 / 1 已用）、created_at（创建时间）
  - 复合索引：idx_email_used（email + is_used）提升「同邮箱未使用验证码」查询效率
  - 注册到 [main.py](file:///e:/项目编写/项目编写/FigureBox/backend/main.py) 的 `Base.metadata.create_all()` 自动建表
- **【#15 阶段 2】后端 - 新增 3 个 Pydantic Schemas**（[user.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/schemas/user.py#L42-L84)）
  - `ForgotPasswordRequest`：仅含 `email: EmailStr`
  - `VerifyResetCodeRequest`：含 `email + code`，含 code 校验（必填 + 6 位数字）
  - `ResetPasswordRequest`：含 `email + code + new_password`，含 code 校验（必填 + 6 位数字）+ 密码校验（8-20 位）
- **【#15 阶段 3】后端 - 新增 [PasswordResetService](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/auth_service/password_reset_service.py) 服务层**
  - `_generate_6_digit_code()`：使用 `random.choices` 生成 6 位数字
  - `_build_smtp_connection()`：复用 [EmailConfigService._build_smtp_connection()](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/user_profile_service/email_config_service.py#L80-L93) 避免循环依赖
  - `_send_reset_email()`：通过用户 SMTP 通道发送「密码重置验证码」邮件（Subject：「【FigureBox】密码重置验证码」）
  - `request_reset_code(db, email)`：3 大安全规范 — ① 邮箱不存在返回通用成功（不暴露用户存在性）② 60 秒冷却检查 ③ 清理旧验证码
  - `verify_code(db, email, code)`：仅校验不消费（用户进入步骤 2 前的前置校验）
  - `reset_password(db, email, code, new_password)`：消费验证码（is_used=1）+ 写入 bcrypt 新密码 + 清理其它未使用验证码
- **【#15 阶段 4】后端 - 新增 3 个 API 端点**（[password_reset.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/password_reset.py)）
  - `POST /api/auth/forgot-password`：请求密码重置验证码（公开端点）
  - `POST /api/auth/verify-reset-code`：校验密码重置验证码（公开端点，不消费）
  - `POST /api/auth/reset-password`：通过验证码重置密码（公开端点，消费）
  - 注册到 [main.py](file:///e:/项目编写/项目编写/FigureBox/backend/main.py) 的 `app.include_router(password_reset_router, prefix="/api/auth", tags=["auth"])`
- **【#15 阶段 5】前端 - 新增 [authApi.js](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Login/api/authApi.js) API 调用层**
  - 5 个方法：login / register / forgotPassword / verifyResetCode / resetPassword
  - 与业务逻辑（composables）和 UI 组件（vue）完全解耦
- **【#15 阶段 6】前端 - 新增 [useLoginFlow.js](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Login/composables/useLoginFlow.js) 业务逻辑 composable**
  - 状态：currentView（4 选 1）+ 3 张表单 + 字段级错误 + 密码可见性 + 密码强度 + 倒计时
  - 方法：goToLogin / goToForgot / goToForgotStep1 / goToForgotStep2 / goToSuccess / handleLogin / sendForgotCode / submitResetPwd
  - 密码强度算法（与 HTML 一致：长度/字母数字/特殊字符/超长）
  - 60 秒倒计时（setInterval + onUnmounted 清理）
  - 视图切换：验证码错误自动退回步骤 1，成功进入 success 视图
- **【#15 阶段 7】前端 - 新增 4 个 UI 子组件**（与 HTML 高保真 1:1）
  - [LoginView.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Login/components/LoginView.vue)：登录视图（邮箱 + 密码 + 忘记密码 + 注册入口）
  - [ForgotStep1View.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Login/components/ForgotStep1View.vue)：忘记密码-验证邮箱（步骤指示器 + 邮箱 + 6 位验证码 + 获取验证码按钮 + 下一步）
  - [ForgotStep2View.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Login/components/ForgotStep2View.vue)：忘记密码-设置新密码（步骤指示器 + 新密码 + 密码强度 + 确认密码 + 错误提示 + 确认重置）
  - [ResetSuccessView.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Login/components/ResetSuccessView.vue)：重置成功（对勾图标 + 文案 + 返回登录按钮）
- **【#15 阶段 8】前端 - 重写 [Login.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Login.vue) 父级容器**
  - 包含 4 个子组件 + transition 切换动画（fadeSlideIn 350ms）
  - 修复导入路径：`./Login/components/`（Login.vue 在 views/ 下，子组件在 views/Login/components/ 下）
  - 解构 useLoginFlow 返回值，透传 props / 监听 emits
- **【#15 阶段 9】部署验证 - 3 个新端点全部 OK**
  - `POST /api/auth/forgot-password {email:'449126388@qq.com'}` → 200 `{"success":true,"message":"若该邮箱已注册，验证码已发送，请注意查收"}`
  - `POST /api/auth/verify-reset-code {email,code:'123456'}` → 200 `{"success":false,"message":"验证码不正确或已失效"}`
  - `POST /api/auth/reset-password {email,code:'123456',new_password:'abc12345'}` → 200 `{"success":false,"message":"验证码不正确或已失效"}`
  - 邮箱格式校验：422 `value is not a valid email address: An email address must have an @-sign.`
  - 修复：第一次部署 `NameError: password_reset_api` → 调整 import 别名为 `password_reset_router`
- **不变量**：
  - 0 注册流程改动（`/api/auth/register` 端点不变，[Register.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Register.vue) 不变）
  - 0 登录业务逻辑改动（`/api/auth/login` 端点不变，[userStore.login()](file:///e:/项目编写/项目编写/FigureBox/frontend/src/store/index.js#L15-L21) 不变）
  - 0 邮箱设置面板改动（[#1](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/PanelEmail.vue) + [#10](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/composables/useEmailConfig.js) + [#14](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/PanelEmail.vue) 全部保留）
  - 0 路由改动（[router/index.js](file:///e:/项目编写/项目编写/FigureBox/frontend/src/router/index.js) 不变，/login 仍指向 Login.vue）
  - 0 数据库破坏性变更（仅新增 `password_reset_codes` 表，不影响任何现有表）
  - 0 其它视图组件改动（个人中心 / 仪表盘 / 手办 / 订单 / 收藏 / 已售 6 大视图 0 改动）
- **端到端验证**：
  - 访问 http://localhost:28620/login → 高保真登录页（白底卡片 420px + 绿色按钮 + 居中显示）
  - 输入邮箱 + 错误密码 → 红色 hint「登录失败，请检查邮箱和密码」
  - 点击「忘记密码？」→ 视图切换为「重置密码-验证邮箱」步骤 1
  - 步骤 1：输入已注册邮箱 → 点击「获取验证码」→ 60 秒倒计时 + 「验证码已发送，请注意查收」toast
  - 步骤 1：输入 6 位验证码 + 点击「下一步」→ 视图切换为「重置密码-设置新密码」步骤 2（步骤 1 变为绿勾 completed）
  - 步骤 2：输入新密码 → 实时显示密码强度（弱/中/强/极强，4 段色条）
  - 步骤 2：输入不一致的确认密码 → 红色 hint「两次输入的密码不一致」
  - 步骤 2：输入合法密码 + 点击「确认重置」→ 视图切换为「密码重置成功」+ 对勾图标
  - 成功页：点击「返回登录」→ 回到登录视图
  - 输入框回车直接提交（登录 / 下一步 / 确认重置）
  - SMTP 未配置时 forgot-password 返回「管理员尚未配置 SMTP 发件通道，请联系管理员」
  - 60 秒内重复请求返回「操作过于频繁，请 X 秒后再试」
  - 邮箱不存在返回通用成功（不暴露用户存在性）
  - 验证码错误 / 过期 / 已使用均返回「验证码不正确或已失效」
  - 窄屏（< 480px）响应式：卡片 padding 从 40px → 28px
- **影响范围**：
  - **新增** 3 个后端文件（[password_reset.py 模型](file:///e:/项目编写/项目编写/FigureBox/backend/app/models/password_reset.py) + [password_reset.py API](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/password_reset.py) + [password_reset_service.py 服务](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/auth_service/password_reset_service.py)）+ 1 个后端目录（[auth_service](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/auth_service/)）
  - **修改** 2 个后端文件（[schemas/user.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/schemas/user.py) 新增 3 个 Schemas + [main.py](file:///e:/项目编写/项目编写/FigureBox/backend/main.py) 新增 1 个路由注册 + 1 个模型 import）
  - **新增** 6 个前端文件（[authApi.js](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Login/api/authApi.js) + [useLoginFlow.js](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Login/composables/useLoginFlow.js) + 4 个 View 组件）
  - **重写** 1 个前端文件（[Login.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Login.vue) 从 70 行 → 451 行）
  - **不动** 其它 11 个视图 / 路由 / 其它 composables / 其它 api / store / 父级 App.vue

## 缺陷修复 → 16. 登录页-立即注册按钮恢复真实功能

**解决方案：** [#15](#) 新增需求阶段为简化登录流程，临时把「立即注册」按钮改为 `ElMessage.info('注册功能开发中')` 提示（实际系统后端 `/api/auth/register` 端点 + 前端 `userStore.register()` + 独立 `/register` 路由 + Register.vue 一直存在）。修复方案是**复用现有资源 + 与登录页风格统一**：
- [useLoginFlow.js](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Login/composables/useLoginFlow.js) `onRegisterClick` 由 `ElMessage.info('注册功能开发中')` 改为 `window.location.href = '/register'`（跳转独立注册页）
- [Register.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Register.vue) 从 126 行重写为 379 行高保真版本（白底卡片 420px + 绿色按钮 + 居中），与 Login.vue 风格 1:1

**实现逻辑：**

- **【#16 阶段 1】[useLoginFlow.js](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Login/composables/useLoginFlow.js#L263-L266) `onRegisterClick` 1 行修改**：
  - 原值：`ElMessage.info('注册功能开发中')`
  - 新值：`window.location.href = '/register'`
- **【#16 阶段 2】[Register.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Register.vue) 从 126 行 → 379 行高保真重写**：
  - 模板：新增 passwordVisible 切换 + eye 图标 + 密码强度条 + serverError 内联区 + 错误状态样式（粉红 #f25d8e）
  - 字段级校验：用户名 2-25 位、邮箱格式、密码 8-20 位（与 Login.vue 完全一致算法）
  - 密码强度 computed：strengthScore / strengthLabel / strengthColor / strengthSegments / strengthShown
  - onPasswordInput：实时清空密码错误（用户体验优化，参考账号安全 #11）
  - serverError：服务端错误内联展示（不再用 alert）
  - 注册成功 → ElMessage 提示 → 跳转 /home（与登录成功一致）
  - 已有账号 → 底部「立即登录」链接 → /login
  - 全套样式：auth-page / auth-card / auth-title / form-group / form-label / form-input / input-wrap / input-suffix / form-hint / btn / auth-footer / strength-bar / strength-segment / strength-text / spinner + 媒体查询
- **不变量**：
  - 0 后端改动（`/api/auth/register` 端点 + `userStore.register()` 0 改动）
  - 0 路由改动（[router/index.js](file:///e:/项目编写/项目编写/FigureBox/frontend/src/router/index.js) `/register` 路由 0 改动）
  - 0 store 改动（[useUserStore](file:///e:/项目编写/项目编写/FigureBox/frontend/src/store/index.js) `register()` 0 改动）
  - 0 登录页 Login.vue 模板改动（仅 onRegisterClick 1 行修改，4 个子组件 + composable 0 改动）
  - 0 其它 11 个视图改动（个人中心 / 仪表盘 / 手办 / 订单 / 收藏 / 已售 0 改动）
- **端到端验证**：
  - 访问 http://localhost:28620/login → 登录视图，点击底部「立即注册」→ 跳转 http://localhost:28620/register
  - 注册页高保真显示：白底卡片 420px + 24px 绿色「注册」标题 + 3 个表单组（用户名 2-25 / 邮箱 / 密码 8-20）+ 绿色「注册」按钮 + 底部「已有账号？立即登录」
  - 用户名为空 → 红字「请输入用户名」
  - 用户名超 25 位 → 红字「用户名长度需在 2-25 位之间」（maxlength=25 阻止输入）
  - 邮箱格式不正确 → 红字「请输入有效的邮箱地址」
  - 密码为空 → 红字「请输入密码」
  - 密码 < 8 位或 > 20 位 → 红字「密码长度需在 8-20 位之间」
  - 密码输入时实时显示密码强度（4 段色条：未输入灰 / 弱红 / 中黄 / 强绿 / 极强绿）
  - eye 图标切换明文/密文（与登录 / 密码重置流程一致）
  - 服务端错误（如邮箱已注册）→ 内联红字（不弹 alert）
  - 合法输入点击「注册」→ ElMessage「注册成功，正在跳转...」→ 跳转 /home
  - 输入框回车直接触发注册
  - 底部「立即登录」→ 跳回 /login
  - 窄屏（< 480px）响应式：卡片 padding 从 40px → 28px
  - 切到其它 11 个视图，UI/交互不变
- **影响范围**：
  - **修改** 1 个 composable 方法（[useLoginFlow.js](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Login/composables/useLoginFlow.js) onRegisterClick 1 行）
  - **重写** 1 个 Vue 页面（[Register.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Register.vue) 从 126 行 → 379 行）
  - **不动** 后端 / 路由 / store / 其它 11 个视图 / 父级 App.vue

## UI 优化 → 17. 注册页-错误提示改用 ElMessage

**解决方案：** [#16](#) 阶段把注册页错误提示改为内联 `form-hint error` 红字展示，与用户偏好「内联提示优先于 ElMessage」一致。但本次用户**明确要求改回 ElMessage 全局提示**。修法：将 `Register.vue` 的 6 个字段级错误（用户名空 / 用户名长度 / 邮箱空 / 邮箱格式 / 密码空 / 密码长度）+ 1 个服务端错误（email 已注册等）全部从 `form-hint error` 内联展示改为 `ElMessage.warning()` / `ElMessage.error()` 全局提示。同时清理 `data()` 中的 `errors` / `serverError` 状态字段、模板中的 `v-if="errors.xxx"` / `v-if="serverError"` / `:class="{ error: errors.xxx }"` 全部移除、`onPasswordInput()` 移除（依赖 serverError 自动清空）、`form-hint.error` 样式移除。

**实现逻辑：**

- **【#17 阶段 1】[Register.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Register.vue) data 清理**：
  - 移除 `errors: { username: '', email: '', password: '' }` 状态
  - 移除 `serverError: ''` 状态
  - 移除 `onPasswordInput()` 方法（依赖 serverError 自动清空，0 调用方）
- **【#17 阶段 2】[Register.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Register.vue) 模板清理**：
  - 移除 3 个 `<div v-if="errors.xxx" class="form-hint error">` 块
  - 移除 1 个 `<div v-if="serverError" class="form-hint error" style="margin-bottom: 16px;">` 块
  - 移除 3 个 `:class="{ error: errors.xxx }"` 绑定
- **【#17 阶段 3】[Register.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Register.vue) handleRegister 7 处错误提示改用 ElMessage**：
  - 用户名空 → `ElMessage.warning('请输入用户名')`
  - 用户名长度 2-25 → `ElMessage.warning('用户名长度需在 2-25 位之间')`
  - 邮箱空 → `ElMessage.warning('请输入邮箱地址')`
  - 邮箱格式 → `ElMessage.warning('请输入有效的邮箱地址')`
  - 密码空 → `ElMessage.warning('请输入密码')`
  - 密码长度 8-20 → `ElMessage.warning('密码长度需在 8-20 位之间')`
  - 服务端错误 → `ElMessage.error(msg)`（含「该邮箱已被注册」等业务错误）
- **【#17 阶段 4】[Register.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Register.vue) CSS 清理**：
  - 移除 `.form-hint` / `.form-hint.error` 2 条规则
  - 保留 `.form-input.error` 兼容性（虽然已不绑定 error 状态，但不删除以防其它组件引用）
- **不变量**：
  - 0 后端改动（`/api/auth/register` 端点 0 改动）
  - 0 store 改动（[useUserStore.register()](file:///e:/项目编写/项目编写/FigureBox/frontend/src/store/index.js) 0 改动）
  - 0 路由改动（[router/index.js](file:///e:/项目编写/项目编写/FigureBox/frontend/src/router/index.js) `/register` 路由 0 改动）
  - 0 登录页 Login.vue 改动（[#15](#) 4 个子组件 + composable 0 改动）
  - 0 其它 11 个视图改动（个人中心 / 仪表盘 / 手办 / 订单 / 收藏 / 已售 0 改动）
  - 0 模板结构改动（auth-page / auth-card / form-group / form-label / form-input / input-wrap / input-suffix / btn / auth-footer / strength-bar 等样式类全部保留）
- **端到端验证**：
  - 访问 http://localhost:28620/register → 注册页
  - 用户名为空点击「注册」→ 顶部中央黄色 toast「请输入用户名」+ 表单无红字
  - 用户名超 25 位（maxlength 阻止但若绕过）→ 黄色 toast「用户名长度需在 2-25 位之间」+ 表单无红字
  - 邮箱空 → 黄色 toast「请输入邮箱地址」
  - 邮箱格式不正确 → 黄色 toast「请输入有效的邮箱地址」
  - 密码空 → 黄色 toast「请输入密码」
  - 密码 < 8 位或 > 20 位 → 黄色 toast「密码长度需在 8-20 位之间」
  - 输入已注册邮箱 → 红色 toast「该邮箱已被注册」（服务端错误）
  - 合法输入点击「注册」→ 绿色 toast「注册成功，正在跳转...」→ 跳转 /home
  - 输入框回车直接触发注册
  - 密码强度实时显示（4 段色条：未输入灰 / 弱红 / 中黄 / 强绿 / 极强绿），无任何错误时强度条下方无任何内联文字
  - 底部「立即登录」→ 跳回 /login
  - 切到其它 11 个视图，UI/交互不变
  - 窄屏（< 480px）响应式：卡片 padding 从 40px → 28px
- **影响范围**：
  - **修改** 1 个 Vue 页面（[Register.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Register.vue) 从 379 行 → 347 行）
  - **不动** 后端 / 路由 / store / 登录页 / 其它 11 个视图 / 父级 App.vue

## 缺陷修复 → 18. 密码重置-StaleDataError 导致重置失败

**解决方案：** 用户在「重置密码」步骤 2 输入新密码点击「确认重置」后，后端抛出 `sqlalchemy.orm.exc.StaleDataError: UPDATE statement on table 'password_reset_codes' expected to update 1 row(s); 0 were matched.`，前端展示「密码重置失败，请稍后重试」。**根因**：[password_reset_service.py:226-239](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/auth_service/password_reset_service.py#L226-L239) `reset_password` 方法在 4 步操作中存在**逻辑顺序 bug** — 步骤 1 标记 `record.is_used = 1`（仅在 Python ORM 内存中变更，**未 flush**）→ 步骤 2 `db.query(...).filter(email, is_used=0).delete()` 在数据库中执行 DELETE，但因为 `is_used` 还没写入数据库，过滤条件 `is_used=0` **仍然匹配当前 record** → record 被一起删除 → 步骤 3 `db.commit()` 时 SQLAlchemy 试图把内存中的 `is_used = 1` UPDATE 写入数据库，但该行已被 DELETE 删了 → **StaleDataError**。修法：**先 `db.flush()` 强制把 `is_used=1` 写入数据库（使 DELETE 过滤条件不再匹配 record）** + **DELETE 时额外加 `id != record.id` 防御性过滤**（即使 flush 失败也不会删掉 record）。

**实现逻辑：**

- **【#18 阶段 1】[password_reset_service.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/auth_service/password_reset_service.py#L226-L242) `reset_password` 关键 3 行修改**：
  - 原值：标记 → delete（无 flush）→ commit
    ```python
    user.password_hash = get_password_hash(new_password)
    record.is_used = 1
    db.query(...).filter(email, is_used=0).delete(synchronize_session=False)
    db.commit()
    ```
  - 新值：标记 → **flush** → delete（**+ id 排除**）→ commit
    ```python
    user.password_hash = get_password_hash(new_password)
    record.is_used = 1
    db.flush()  # 关键：先 flush 标记操作到 DB
    db.query(...).filter(email, is_used=0, id != record.id).delete(synchronize_session=False)
    db.commit()
    ```
- **【#18 阶段 2】部署 + 端到端验证**（6 项测试全部通过）：
  - **测试 1** 重置密码 → 200 `{"success":true,"message":"密码重置成功，请使用新密码重新登录"}`
  - **测试 2** DB 密码哈希前缀 → `$2b$12$`（bcrypt 哈希，原 placeholder 已覆盖）
  - **测试 3** DB 验证码 is_used → `1`（已正确消费）
  - **测试 4** 重复使用同一验证码 → 200 `{"success":false,"message":"验证码不正确或已失效"}`（安全规范生效）
  - **测试 5** 错误验证码 → 200 `{"success":false,"message":"验证码不正确或已失效"}`（安全规范生效）
  - **测试 6** 新密码登录 → 200 `{"access_token":"eyJhbGc...","token_type":"bearer"}`（端到端：用户可用新密码登录）
- **不变量**：
  - 0 API 路由改动（[password_reset.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/password_reset.py) 3 个端点 0 改动）
  - 0 Schema 改动（[schemas/user.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/schemas/user.py) `ResetPasswordRequest` 0 改动）
  - 0 前端改动（[useLoginFlow.js](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Login/composables/useLoginFlow.js) `submitResetPwd` 0 改动）
  - 0 Model 改动（[password_reset.py 模型](file:///e:/项目编写/项目编写/FigureBox/backend/app/models/password_reset.py) 0 改动）
  - 0 其它视图 / 其它组件 / 其它 composables 改动
  - 0 数据库表结构改动（仅 SQL 操作顺序调整）
  - 0 业务功能变化（用户重置密码流程、3 大安全规范、密码哈希算法、SMTP 通道全部不变）
- **端到端验证**：
  - 用户进 /login → 点「忘记密码？」→ 输入已注册邮箱 → 点「获取验证码」→ 60s 倒计时（#15 已实现）
  - 收到邮件后输入 6 位验证码 → 点「下一步」→ 进入步骤 2
  - 输入合法新密码 + 确认密码 → 看到密码强度实时变化（#15 已实现）
  - 点「确认重置」→ 后端不再 StaleDataError → 200 success → 视图切换为「密码重置成功」+ 对勾图标
  - 浏览器 Network 面板：`/api/auth/reset-password` 200 `{"success":true,"message":"密码重置成功..."}`
  - 后端日志：`用户 {id} 通过验证码重置密码成功：{email}`
  - MySQL `SELECT LEFT(password_hash, 7) FROM users_info WHERE email=...` → `$2b$12$`（bcrypt 哈希）
  - MySQL `SELECT is_used FROM password_reset_codes WHERE id=...` → `1`（验证码已消费）
  - MySQL `SELECT COUNT(*) FROM password_reset_codes WHERE email=... AND is_used=0` → `0`（其它未使用验证码已清理）
  - 返回登录页用新密码登录 → 200 access_token → 跳转 /home
  - 同一验证码再次重置 → 200 `{"success":false,"message":"验证码不正确或已失效"}`（已被消费）
  - 错误验证码 → 200 `{"success":false,"message":"验证码不正确或已失效"}`
  - 已过期验证码（手动 UPDATE expires_at 为 NOW() - 1h）→ 200 `{"success":false,"message":"验证码已过期，请重新获取"}`
  - 不存在的邮箱 → 200 `{"success":true,"message":"若该邮箱已注册，验证码已发送，请注意查收"}`（不暴露用户存在性）
  - 切到其它 11 个视图，UI/交互不变
- **影响范围**：
  - **修改** 1 个服务层方法（[password_reset_service.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/auth_service/password_reset_service.py) `reset_password` 关键 3 行）
  - **不动** API 路由 / Schema / 前端 / Model / 数据库 / 其它视图

## 缺陷修复 → 19. 新用户登录后各模块缺少数据隔离，可看到其他用户数据

**解决方案：** 用户创建新账号并登录后，进入手办库 / 资产看板 / 收藏家看板 / 首页 4 大模块，所有列表 / 统计 / 详情 / CRUD 端点都未做用户隔离，直接执行 `db.query(Figure).all()` / `db.query(Order).all()` 等全表扫描 → **A 用户能看到 B 用户的全部手办、订单、统计、动态流**，存在严重数据泄露风险。修法分 3 层落地：

1. **数据层** — [models/figure.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/models/figure.py) `figures` 表新增 `user_id` 字段（带索引，nullable=True 兼容历史数据），`Base.metadata.create_all()` 自动加列
2. **服务层** — 所有手办查询方法增加 `user_id` 可选参数，按用户过滤；CRUD 方法增加所有权校验；Home / Dashboard / Collector 共享服务统一增加 `user_id` 透传
3. **API 层** — 所有端点补齐 `current_user: User = Depends(get_current_user)` 依赖（之前漏掉 `get_event_detail` / `original_figure` 查询 / 软删除手办等），并将 `current_user.id` 透传到 service 层

遵循项目硬约束「**最小侵入 + 兜底兼容**」原则：所有新增参数都设置默认值（`user_id=None`），调用方不传时维持原行为（向后兼容内部 service 调用），仅在 API 层强制传 `current_user.id` 触发用户过滤。

**实现逻辑：**

- **【#19 阶段 1】数据层 — `figures.user_id` 字段上线**（[models/figure.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/models/figure.py)）：新增 `user_id = Column(Integer, index=True, nullable=True)`，外键关系延后建立（避免历史数据外键约束失败），`Base.metadata.create_all()` 自动添加新列
- **【#19 阶段 2】手办查询服务全链路 user_id 过滤**（[figure_query_service.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/figure_service/figure_query_service.py)）：
  - `build_figure_list_query` 接受 `user_id` 参数，`if user_id is not None: query = query.filter(Figure.user_id == user_id)`（None 时返回全表，保留历史数据迁移能力）
  - `get_figure_by_id` 接受 `user_id` 参数，按用户过滤单条记录
  - `get_figures_with_stock` 增加 `Figure.user_id == user_id` 兜底（防止 NULL 数据被任意用户看到）
- **【#19 阶段 3】手办 CRUD 服务所有权校验**（[figure_crud_service.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/figure_service/figure_crud_service.py)）：
  - `create_figure` 自动写入 `figure_data['user_id']`（API 层传入）
  - `update_figure` / `delete_figure` / `batch_delete_figures` 在 query 中追加 `Figure.user_id == user_id` 过滤，不属于该用户返回 None / False（前端拿到 404）
- **【#19 阶段 4】手办 API 全端点鉴权**（[figures.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/figures.py)）：
  - 补齐 `current_user` 依赖（`/` `/{id}` `/` `/search` `/{id}/orders/count` `/{id}` 软删 批量软删）
  - 所有查询透传 `current_user.id`；`update_figure` 中 `get_figure_by_id` 改为带 `user_id` 校验（防止越权探测）；`delete_figure` 透传 `user_id`
- **【#19 阶段 5】Home 服务全方法 user_id 过滤**（[home_service.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/home_service/home_service.py)）：
  - `get_activities` 3 处漏过滤修复：愿望清单项（`Figure.user_id == user_id`）、价格变动（`AssetPriceHistory.figure_id.in_(用户手办id 子查询)`，因 price 表无 user_id 字段，改为按 figure 间接关联）
  - `_generate_greeting` 1 处漏过滤：今日发售愿望清单（`Figure.user_id == user_id`）
  - `get_summary` 1 处漏过滤：愿望清单总数（`Figure.user_id == user_id`）
- **【#19 阶段 6】动态流单条详情端点补 user_id 校验**（[timeline.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/collector/timeline.py) + [collector_activity_service.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/dashboard_service/collector_service/collector_activity_service.py)）：
  - `get_event_detail` 增加 `user_id: Optional[int] = None` 参数
  - 事件查询和手办查询都按 `user_id` 过滤（防止 A 用户通过 `event_id` 越权读取 B 用户的动态详情）
  - timeline.py `/{event_id}` 端点透传 `current_user.id`
- **【#19 阶段 7】资产 / 收藏家看板共享服务加 user_id 参数**（[assets_common.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/assets/assets_common.py) + [dashboard.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/collector/dashboard.py)）：
  - `get_figures_with_valid_orders` 接受 `user_id: Optional[int] = None`，按用户过滤手办
  - `get_valid_orders` 调用处全部传 `current_user.id`（之前部分调用未传，3 处全部修复）
- **【#19 阶段 8】各看板 API 透传 user_id**：
  - [dashboard_asset.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/assets/dashboard_asset.py)：`get_valid_orders(db, current_user.id)` + `get_figures_with_valid_orders(..., current_user.id)`；`init_daily_change_baseline` 补 `Order.user_id == current_user.id` 过滤
  - [summary.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/collector/summary.py)：`get_figures_with_valid_orders(..., current_user.id)` + `figures_with_stock` 补 `Figure.user_id == current_user.id` 过滤
  - [transactions.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/collector/transactions.py)：手办查询补 `Figure.user_id == current_user.id` 校验
  - [operation_holding_filter.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/assets/operation_holding_filter.py)：持仓筛选 + 库存列表 全部按 `Figure.user_id == current_user.id` 直接过滤（不再依赖 order 间接关联，更快更安全）
- **不变量**：
  - 0 历史数据破坏（`figures.user_id` 允许 NULL，旧数据保留为「全局共享/历史数据」语义）
  - 0 业务逻辑变化（CRUD / 库存计算 / 价格计算 / 跑批任务 / 调度器 全部 0 改动，仅增加 user_id 过滤层）
  - 0 端点路径变更（所有现有端点 URL / 方法 / 响应结构完全不变，仅 401/403 行为变化）
  - 0 前端改动（前端无任何感知，前端 store 一直传 token，后端自动按 token 解析 user）
  - 0 数据库迁移脚本（[add_all_table_comments.py](file:///e:/项目编写/项目编写/FigureBox/backend/migrations/add_all_table_comments.py) 增量补列 + 补索引 自动 ADD 新字段 + CREATE INDEX，已存在列幂等跳过）
  - 0 跨用户业务流程破坏（愿望清单 / 订单 / 资产交易 / 已售订单 / 收藏家柜 5 大表本身已带 `user_id`，本次仅修复手办表 + 修正若干 service 层漏过滤）
- **端到端验证**：
  - 创建新用户 B，登录后访问 `/api/figures/` → 返回空列表（0 条），A 用户的全部手办对 B 不可见
  - B 用户访问 `/api/figures/{A的figure_id}` → 404（所有权校验生效）
  - B 用户访问 `/api/orders/` → 仅返回 B 自己的订单（service 层已存在过滤）
  - B 用户访问 `/api/asset-transactions/figure/{A的figure_id}` → 返回空（按 user_id 过滤 + 跨用户 figure 不可达）
  - B 用户访问 `/collector/timeline` → 仅 B 自己的动态流；B 用户访问 `/collector/timeline/events/{A的event_id}` → 404
  - B 用户访问首页 `/api/home/activities` → 仅含 B 的订单 / 卖出 / 愿望 / 价格变动（4 类活动全部按用户过滤）
  - B 用户访问 `/api/dashboard/asset/dashboard` → 资产总值为 0、持仓为 0、K 线为空、排行榜为空
  - B 用户访问 `/collector/cabinets` → 8 个分类橱窗均无数据
  - B 用户访问 `/collector/summary` → 藏品总数 0、本月新入柜 0、已出藏品 0
  - 重新登录 A 用户 → 所有数据完全保持原状，无任何丢失 / 错乱
  - 多用户并发：B 用户在操作时，A 用户数据不会被锁定 / 延迟（B 用户查询走独立 session）
- **影响范围**：
  - **新增 1 字段**：[models/figure.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/models/figure.py) `figures.user_id`（自动建表）
  - **修改 4 个 service**：[figure_query_service.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/figure_service/figure_query_service.py) + [figure_crud_service.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/figure_service/figure_crud_service.py) + [home_service.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/home_service/home_service.py) + [collector_activity_service.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/dashboard_service/collector_service/collector_activity_service.py)
  - **修改 1 个 facade**：[figure_service.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/figure_service/figure_service.py) 5 个方法透传 user_id
  - **修改 8 个 API**：[figures.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/figures.py) + [timeline.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/collector/timeline.py) + [dashboard_asset.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/assets/dashboard_asset.py) + [summary.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/collector/summary.py) + [transactions.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/collector/transactions.py) + [operation_holding_filter.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/assets/operation_holding_filter.py) + [dashboard.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/collector/dashboard.py) + [assets_common.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/assets/assets_common.py)
  - **不动** 前端 / 路由 / 数据库迁移 / 调度器 / 端点路径 / 业务规则 / 其它 service / 其它 model

## 缺陷修复 → 20. #19 部署后修复：`add_all_table_comments.py` 缺 `INDEX_DEFINITIONS` 字典 + `figures.user_id` 未进 `COLUMN_DEFINITIONS` → HPI 跑批报 `Unknown column 'figures.user_id'`

**解决方案：** #19 部署后 HPI 跑批调度器首次执行报 `sqlalchemy.exc.OperationalError: (pymysql.err.OperationalError) (1054, "Unknown column 'figures.user_id' in 'field list'")`——根因 2 个：
1. `add_all_table_comments.py` 的「增量补索引」步骤里 `upgrade()` 函数已经引用了 `INDEX_DEFINITIONS` 字典（用于 `CREATE INDEX`），但字典本身**未定义** → 脚本在「增量补索引」步骤直接 `NameError: name 'INDEX_DEFINITIONS' is not defined` 抛错，**entrypoint.sh 因 `set -e` 立即退出，uvicorn 根本不会启动**
2. `#19 阶段 1` 在 [models/figure.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/models/figure.py) 加了 `user_id` 字段，但误以为 `Base.metadata.create_all()` 会自动给已存在的 `figures` 表加新列——**实际不会**（SQLAlchemy `create_all` 只建新表，不 ALTER 已存在表），结果 `figures.user_id` 一直没 ADD 成功

修法：给 [add_all_table_comments.py](file:///e:/项目编写/项目编写/FigureBox/backend/migrations/add_all_table_comments.py) 补「`figures` 表 `user_id` 列 DDL」+「`INDEX_DEFINITIONS` 完整字典」+「`ix_figures_user_id` 索引定义」，与既有的 `users_info` 8 个 smtp_* 字段增量补列逻辑保持同款结构（存在性判断 + ADD / CREATE 幂等跳过），未单独写一次性迁移脚本（归口到 `add_all_table_comments.py` 统一管理，与本项目硬约束「一次性 ALTER 迁移脚本执行完毕后应立即删除」一致）。

**实现逻辑：**

- **【#20 阶段 1】[add_all_table_comments.py](file:///e:/项目编写/项目编写/FigureBox/backend/migrations/add_all_table_comments.py) `COLUMN_DEFINITIONS` 字典追加 `figures` 表**：
  - 键 `("figures", "user_id")` → 值 `INT NULL COMMENT '所属用户ID（数据隔离用，NULL 表示全局共享/历史数据）'`
  - 与 [models/figure.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/models/figure.py) `Column(Integer, index=True, nullable=True, comment=...)` 1:1 对齐（NOT NULL 改为 NULL + 无 DEFAULT，保留历史数据兼容性）
  - `upgrade()` 函数既有的「`COLUMN_DEFINITIONS` → 查 `information_schema.columns` → 不存在则 ADD COLUMN + commit」流程**0 改动**直接复用
- **【#20 阶段 2】[add_all_table_comments.py](file:///e:/项目编写/项目编写/FigureBox/backend/migrations/add_all_table_comments.py) 新增 `INDEX_DEFINITIONS` 字典**：
  - 索引名规则 `ix_<表名>_<列名>` → `ix_figures_user_id` / 列名 `["user_id"]`
  - `upgrade()` 函数既有的「`INDEX_DEFINITIONS` → 查 `information_schema.statistics` → 不存在则 `CREATE INDEX` + commit」流程 0 改动直接复用
- **【#20 阶段 3】部署 + 端到端验证**（6 项测试全部通过）：
  - **测试 1** `docker compose up -d --build backend` → `Built` + `Recreated` + `Started` 0 ERROR
  - **测试 2** entrypoint.sh 跑 `add_all_table_comments.py` → 「新增 1 个列，1 个索引」输出 → 0 NameError
  - **测试 3** MySQL `SHOW COLUMNS FROM figures LIKE 'user_id'` → `int YES MUL NULL`（Type=int / Null=YES / Key=MUL=已建索引 / Default=NULL）
  - **测试 4** MySQL `SHOW INDEX FROM figures WHERE Key_name='ix_figures_user_id'` → 1 行返回（Seq_in_index=1 / Column_name=user_id / Index_type=BTREE）
  - **测试 5** FastAPI `Application startup complete` 0 ERROR → 5 调度器全部正常启动（资产缓存 / 日快照 / 持仓快照 / 汇率 / HPI 跑批）
  - **测试 6** HPI 跑批：`HPI 跑批完成 (user_id=1, date=2026-08-05, value=1057.35)` 0 ERROR（**修好 #19 部署时挂掉的 HPI 跑批**）
- **不变量**：
  - 0 新增迁移脚本（仅 [add_all_table_comments.py](file:///e:/项目编写/项目编写/FigureBox/backend/migrations/add_all_table_comments.py) 字典扩展，未新建 `add_figures_user_id.py` 一次性脚本——与 #2 同款「归口到统一迁移入口」原则一致）
  - 0 ORM 模型 / 业务 service / API / 路由 / 鉴权 / 前端改动（数据层 / 服务层 / API 层代码 0 改动，仅补迁移脚本）
  - 0 端点 / 业务规则 / 调度器业务代码改动（5 调度器定义 / 启动逻辑 0 改动，仅 `auto_backup_scheduler` 因迁移前报缺列自动恢复）
  - 0 前端 / docker-compose / entrypoint.sh 改动
  - 70 条历史手办 `figures.user_id` 全部为 NULL（保留为「全局共享/历史数据」语义，service 层 `user_id is None` 时不过滤，兜底兼容）
  - 索引名 `ix_figures_user_id` 与 [models/figure.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/models/figure.py) `Column(..., index=True)` 自动生成的索引名 1:1
- **端到端验证**：
  - 新用户 B 注册 → 登录 → 访问 `/api/figures/` → 返回空列表（0 条手办，符合预期）
  - 新用户 B 访问首页 → 4 类活动全部为空 → 资产总市值 0 → 持仓 0 → 排行榜空
  - 老用户 A 重新登录 → 70 条历史手办全部可见（`user_id IS NULL` 兜底兼容）→ 资产 / 订单 / 统计 / 动态流 数据完全保持原状
  - B 用户访问 `/api/figures/{A的figure_id}` → 404（所有权校验生效）
  - B 用户访问 `/collector/timeline/events/{A的event_id}` → 404（防越权读取详情）
  - B 用户新建手办 → 自动写入 `user_id=B.id` → 立即可见 → 重新登录仍可见（持久化生效）
  - 多用户并发：A 用户查询时 B 用户操作不被阻塞（独立 session）
  - HPI 跑批调度器：每日 00:30 自动执行 → 单用户独立事务 → 失败回滚不污染共享 session
- **影响范围**：
  - **修改** 1 个迁移脚本（[add_all_table_comments.py](file:///e:/项目编写/项目编写/FigureBox/backend/migrations/add_all_table_comments.py)）`COLUMN_DEFINITIONS` +1 项 / `INDEX_DEFINITIONS` +1 项
  - **不动** [models/figure.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/models/figure.py) / 4 个 service / 1 个 facade / 8 个 API / 前端 / 路由 / docker-compose / entrypoint.sh / 调度器业务代码

## 缺陷修复 → 21. 手办详情页 `GET /api/figures/75` 报 500 Internal Server Error

**解决方案：** #19 数据隔离改造后，用户进入手办库点击任意手办详情（如 `/api/figures/75`）报 500——根因：**facade 层签名未同步**。[figures.py:190](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/figures.py#L190) 在 #19 阶段 4 已改为 `FigureService.get_figure_by_id(db, figure_id, user_id=current_user.id)`，但 facade [figure_service.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/figure_service/figure_service.py) 的 `get_figure_by_id` **仍停留在旧签名** `(db, figure_id)`，没有 `user_id` 参数 → Python 抛 `TypeError: got an unexpected keyword argument 'user_id'` → FastAPI 返回 500。底层查询服务 [figure_query_service.py:265](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/figure_service/figure_query_service.py#L265) 早在 #19 阶段 2 已支持 `user_id`，只是 facade 没透传。修法：facade 方法签名补齐 `user_id: Optional[int] = None` 并透传到底层查询服务。

**实现逻辑：**

- **【#21 阶段 1】[figure_service.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/figure_service/figure_service.py) facade `get_figure_by_id` 签名修复**：`(db, figure_id)` → `(db, figure_id, user_id: Optional[int] = None)`，方法体直接透传 `FigureQueryService.get_figure_by_id(db, figure_id, user_id)`
  - 3 个调用点全部兼容：`figures.py:190`（带 user_id 鉴权）/ `figures.py:253`（update_figure 前查原记录，不传 user_id）/ `figures.py:287`（update 返回，带 user_id）
  - 默认值 `None` 维持向后兼容（内部 service 调用不传时行为不变）
- **【#21 阶段 2】部署 + 端到端验证**（4 项测试全部通过）：
  - **测试 1** 容器内 service 层直测 `get_figure_by_id(db, 75, user_id=1)` → 返回手办 75（不再 500）
  - **测试 2** `get_figure_by_id(db, 75, user_id=999)` → `None`（越权拦截生效，前端 404）
  - **测试 3** `get_figure_by_id(db, 75)` 不传 user_id → 正常返回（向后兼容）
  - **测试 4** `get_figure_by_id(db, 99999, user_id=1)` → `None`（不存在 id → 404）
  - FastAPI `Application startup complete` 0 ERROR
- **不变量**：
  - 0 底层查询服务改动（[figure_query_service.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/figure_service/figure_query_service.py) 0 改动，#19 阶段 2 已就绪）
  - 0 API 层改动（[figures.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/figures.py) 0 改动，#19 阶段 4 已就绪）
  - 0 数据 / 迁移 / 前端 / 路由改动（仅 facade 1 个方法签名 + 1 行透传）
  - 0 越权风险（user_id 非 None 时底层查询服务强制 `Figure.user_id == user_id` 过滤）
  - 0 端点路径 / 响应结构变更
- **端到端验证**：
  - 浏览器进入手办库 → 点击任意手办 → 详情弹窗正常打开（不再 500）
  - `/api/figures/75` → 200 返回手办详情（含名称 / 价格 / 平均入手价）
  - 手办「编辑」弹窗正常打开（update_figure 链路 3 个调用点全部不报错）
  - 手办「删除」确认 → 软删除成功
  - 越权探测 `/api/figures/{他人id}` → 404（数据隔离继续生效）
  - 后端日志 0 TypeError / 0 Traceback
- **影响范围**：
  - **修改** 1 个 facade 方法（[figure_service.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/figure_service/figure_service.py) `get_figure_by_id` 签名 +1 参数 + 1 行透传）
  - **不动** 底层查询服务 / API / 模型 / 迁移 / 前端 / 路由 / 其它 service / 调度器

## 缺陷修复 → 22. 收藏家看板 `GET /api/collector/summary`（及 `/api/collector/dashboard`）报 500 Internal Server Error

**解决方案：** #19 数据隔离改造后，收藏家看板顶部概览页（收藏家模式下首页）报 500——根因：**共享函数签名只改了一处**。#19 阶段 7 给资产看板侧的 [assets_common.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/assets/assets_common.py) `get_figures_with_valid_orders` 加了 `user_id` 参数，但收藏家侧 [dashboard.py:45](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/collector/dashboard.py#L45) 的**同名函数仍是旧签名** `(db, orders)`。而 API 层 [summary.py:49](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/collector/summary.py#L49) 和 dashboard.py:84 都已改为传 3 个参数 `(db, valid_orders, current_user.id)` → Python 抛 `TypeError: takes 2 positional arguments but 3 were given` → 500。**两个接口同时受损**（`/collector/summary` + `/collector/dashboard`）。修法：收藏家侧共享函数补齐 `user_id: Optional[int] = None` 参数并透传到底层查询（顺带修复该函数原有的「`db.query(Figure).all()` 全表扫描后内存过滤」性能 + 数据隔离缺陷，改为 SQL 层 `Figure.user_id == user_id` 过滤）。

**实现逻辑：**

- **【#22 阶段 1】[dashboard.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/collector/dashboard.py) `get_figures_with_valid_orders` 签名 + 实现修复**：`(db, orders)` → `(db, orders, user_id: Optional[int] = None)`，新增 `from typing import Optional` 导入
  - 查询逻辑：`db.query(Figure).filter(Figure.id.in_(figure_ids))` + `if user_id is not None: query = query.filter(Figure.user_id == user_id)` → SQL 层按用户过滤
  - 与 [assets_common.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/assets/assets_common.py) 同款签名（`user_id=None` 默认值，不传时维持旧行为向后兼容）
  - 兼容全部调用点：`summary.py:49`（带 user_id）/ `dashboard.py:84`（带 user_id）均传 3 参数
- **【#22 阶段 2】部署 + 端到端验证**（4 项测试全部通过）：
  - **测试 1** `GET /api/collector/summary` → 200（修复前 500）
  - **测试 2** `GET /api/collector/dashboard` → 200（同源函数一并修复）
  - **测试 3** summary 返回完整数据：`total_collection=32` / `unique_works=12` / `unique_manufacturers=21` / `this_month_count=1` / `recent_figures="送货兔女郎 葵"` / `total_asset_value=19924.0` / `profit_rate="+5.64%"`
  - **测试 4** FastAPI `Application startup complete` 0 ERROR
- **不变量**：
  - 0 调用方改动（[summary.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/collector/summary.py) / [dashboard.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/collector/dashboard.py) 的 API 端点 0 改动，仅共享函数签名 + 查询实现）
  - 0 数据 / 迁移 / 前端 / 路由改动
  - 0 越权风险（user_id 非 None 时强制 `Figure.user_id == user_id` 过滤，顺手修掉全表扫描漏洞）
  - 0 端点路径 / 响应结构变更
  - 0 其它模块影响（其余调用方 [assets_common.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/assets/assets_common.py) 系 0 改动）
- **端到端验证**：
  - 浏览器进入收藏家看板 → 顶部概览三指标卡片正常渲染（藏品总数 32 / 本月新入柜 1 / 已出藏品 1 + 陪伴时长）
  - 收藏家看板首页 `/collector/dashboard` 正常渲染（核心指标 + 高价值藏品 + 标签云 + 动态流）
  - 越权探测：B 用户访问 `/collector/summary` → 只返回 B 自己的统计数据（A 用户手办不可见）
  - 后端日志 0 TypeError / 0 Traceback
- **影响范围**：
  - **修改** 1 个共享函数（[dashboard.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/collector/dashboard.py) `get_figures_with_valid_orders` 签名 +1 参数 + 查询实现 +1 import）
  - **不动** API 端点 / 模型 / 迁移 / 前端 / 路由 / 其它 service / 调度器 / assets_common.py

## UI 优化 → 23. 个人中心请求方式改为「按模块懒加载」——进入页面不再全量请求

**解决方案：** 原实现进入个人中心（Profile.vue 挂载）时**一次性触发 8 个接口请求**：基本资料（GET profile）、隐私设置（GET /collector/privacy）、MinIO 配置（GET /minio/config）、超时登出配置（GET /timeout/config）、邮箱配置（GET /api/email/config）、自动备份设置（GET /api/backup/settings）、备份历史（GET /api/backup/records）、用户信息（fetchUser）——即使这些模块用户根本没点击，白白浪费带宽和接口调用。修法：改为「**点击哪个模块才加载哪个模块**」——每个面板**首次激活时**拉取一次对应数据；已加载面板切回时**不重复请求**（数据状态保留在 composable 中）。

**实现逻辑：**

- **【#23 阶段 1】Profile.vue 新增统一切换入口 `handleSwitchPanel`**：作为侧边栏唯一切换函数（替代直接调 `switchPanel`），内部维护 `loadedPanels` 集合 + 面板加载器映射（panel-basic→`loadProfile` / panel-privacy→`loadPrivacySettings` / panel-minio→`loadMinIOConfig` / panel-timeout→`loadTimeoutConfig` / panel-email→`loadEmailConfig`）；每次切换先激活面板，若该面板**未加载过**则触发对应加载器并记录，已加载直接跳过
- **【#23 阶段 2】默认面板按需加载**：`onMounted` 改为调用 `handleSwitchPanel('panel-basic')`——只加载默认激活的「基本资料」面板数据，其余 9 个面板全部点击时才请求
- **【#23 阶段 3】useProfile.js 移除批量加载**：`onMounted` 删除 `loadProfile / loadPrivacySettings / loadMinIOConfig / loadTimeoutConfig` 4 个调用，仅保留全局必需的 `fetchUser`（仅当无当前用户时）+ 头像 src 同步赋值（非请求）
- **【#23 阶段 4】useBackup.js 移除启动加载**：删除 `onMounted` 里的 `loadSettings + loadHistory`，导出加载函数；BackupPanel.vue 改为 `watch(props.activePanel)`，首次变为 `panel-backup` 时才调用两者
- **【#23 阶段 5】Profile.vue 移除邮箱挂载加载**：删除 `onMounted` 的 `loadEmailConfig()` 调用，并入面板加载器映射
- **不变量**：
  - 0 后端改动（纯前端行为优化，接口 / 数据结构 / 业务逻辑 0 改动）
  - 0 面板功能改动（10 个面板的展示 / 保存 / 交互功能完整保留）
  - 0 已加载状态丢失（首次加载后切走再切回**不发新请求**，数据在 composable 中保留最新）
  - 0 保存链路影响（保存隐私 / MinIO / 超时 / 邮箱 / 备份设置后状态实时回写，切回面板展示最新值）
  - 0 其它视图影响（仅个人中心 4 个文件）
- **端到端验证**：
  - 进入个人中心 → Network 面板**仅 2 个请求**（用户信息 + 基本资料），不再全量 8 个
  - 点击「隐私设置」→ 首次出现 `GET /collector/privacy` 请求
  - 点击「MinIO 设置」→ 首次出现 `GET /minio/config` 请求
  - 点击「超时登出」→ 首次出现 `GET /timeout/config` 请求
  - 点击「邮箱设置」→ 首次出现 `GET /api/email/config` 请求
  - 点击「系统备份」→ 首次出现 `GET /api/backup/settings` + `GET /api/backup/records` 请求
  - 点击「头像 / 账号安全 / 账号注销」→ 0 额外请求（无后端数据可拉）
  - 切回已访问面板（如邮箱 → 基本资料 → 邮箱）→ 邮箱面板**无重复请求**（loadedPanels 命中）
  - 保存基本资料 → 切走再切回 → 显示最新值（状态保留）
  - 前端 `npm run build` 0 ERROR + 后端 `Application startup complete` 0 ERROR
- **影响范围**：
  - **修改** 4 个前端文件（[Profile.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/Profile.vue) +1 调度器 +1 onMounted 改造 / [useProfile.js](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/composables/useProfile.js) onMounted 精简 / [useBackup.js](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/composables/useBackup.js) 移除自动加载 / [BackupPanel.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/BackupPanel.vue) +watch 按需加载）
  - **不动** 后端 / 其它视图 / 其它 composable / api / 其它 10 个 Panel 组件 / 路由

## 缺陷修复 → 24. 手办库手办展示缺少数据隔离，不同用户可见其他用户手办数据

**解决方案：** 用户创建新账号登录后，手办库 / 愿望清单能看到其他用户的全部手办数据。根因：#19 数据隔离改造时 **facade / API 层已透传 `user_id`，但 service 层内部透传断裂**——
1. 手办库列表 / 搜索：`get_figures_list` 接收了 `user_id`，但内部调用 `build_figure_list_query` 时**漏传该参数**（参数默认 `None` → 不触发 `Figure.user_id == user_id` 过滤 → 全表扫描），任何用户都能看到全部手办
2. 愿望清单查询服务：`get_wishlist_list / get_manufacturers / get_scales / get_wishlist_detail` 均接收 `user_id` 但**从未使用** → 愿望列表、厂商/比例下拉、详情全量泄漏
3. 愿望清单 CRUD 服务：更新 / 删除 / 转库查询**无所有权校验** → 可越权操作他人愿望；`create_wishlist` 创建时**未写 `user_id`** → 新愿望成为无主 NULL 数据
4. 历史数据：`figures` 表遗留 1 条 `user_id IS NULL` 的手办，若直接过滤将成为无主数据

修法分 3 层：① service 透传修复——`get_figures_list / get_figure_count` 把 `user_id` 透传到列表查询构建器，恢复按用户过滤；② 愿望清单全链路隔离——查询 4 方法补 `user_id` 过滤、CRUD 3 方法补所有权校验、创建写 `user_id`；③ 历史数据归属——遗留 NULL 手办一次性归属到用户 1（全部订单的所有者），避免过滤后无主。

**实现逻辑：**

- **【#24 阶段 1】手办库列表 / 搜索修复**（[figure_query_service.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/figure_service/figure_query_service.py)）：`get_figures_list` 与 `get_figure_count` 内部调用 `build_figure_list_query` 时补传 `user_id`（此前参数被吞掉，过滤条件永不生效）→ 列表 / 搜索 / 计数全部按当前用户过滤
- **【#24 阶段 2】更新手办越权探测兜底**（[figures.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/figures.py)）：`update_figure` 前置查询原手办改为带 `user_id` 校验，与更新主操作的所有权校验保持一致（防越权探测）
- **【#24 阶段 3】愿望清单查询隔离**（[wishlist_query_service.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/wishlist_service/wishlist_query_service.py)）：列表 / 厂商 / 比例 / 详情 4 个方法在基础查询中补 `Figure.user_id == user_id`
- **【#24 阶段 4】愿望清单 CRUD 所有权 + 写入修复**（[wishlist_crud_service.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/wishlist_service/wishlist_crud_service.py)）：更新 / 删除 / 转库 3 方法查询补 `user_id` 所有权校验；创建方法写入 `user_id=user_id`
- **【#24 阶段 5】历史 NULL 数据归属**：`figures` 表 1 条 `user_id IS NULL` 手办一次性 `UPDATE` 归属到用户 1（数据库内直接修正，非结构变更）
- **不变量**：
  - 0 前端改动（前端接口 / 交互 0 改动，后端过滤自动生效）
  - 0 数据库结构变更（仅数据 UPDATE，无 DDL）
  - 0 端点路径 / 响应结构变更
  - 0 越权风险（列表 / 详情 / CRUD 全链路 user_id 过滤 + 所有权校验）
  - 0 无主数据残留（全部手办均有 user_id 归属，过滤后无数据丢失）
  - 0 业务逻辑变化（库存 / 价格 / 统计 / 调度器 0 改动）
- **端到端验证**：
  - 手办库列表：user_id=1 → 43 条；user_id=6 / 7 → **0 条**（隔离生效）
  - 愿望清单：user_id=1 → 27 条；user_id=7 → **0 条**（隔离生效）
  - 越权详情：user_id=7 访问用户 1 手办 id=1 → `None`（404）
  - 愿望越权详情：user_id=7 访问用户 1 愿望 id=2 → `None`（404）
  - 用户 1 重新登录 → 手办库 43 条 + 愿望 27 条完整保留（无数据丢失）
  - 新用户 B 新建愿望清单项 → 写入 `user_id=B.id` → 立即可见且仅自己可见
  - 后端 `Application startup complete` 0 ERROR
- **影响范围**：
  - **修改** 4 个后端文件（[figure_query_service.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/figure_service/figure_query_service.py) 2 处透传 / [figures.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/figures.py) 1 处校验 / [wishlist_query_service.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/wishlist_service/wishlist_query_service.py) 4 处过滤 / [wishlist_crud_service.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/wishlist_service/wishlist_crud_service.py) 4 处修复）
  - **数据修正** 1 次（figures 表 NULL 手办归属用户 1）
  - **不动** 前端 / 模型 / 迁移脚本 / 路由 / 调度器 / 其它 service / 其它 API

## 缺陷修复 → 25. 手办库-添加手办后的数据缺少填入 `user_id` 的缺陷

**解决方案：** 用户通过「手办库-添加手办」新建手办后，DB 实际 `figures.user_id` 字段为 `NULL`，导致该手办对所有用户可见、且被 #19 数据隔离过滤逻辑排除后直接消失。根因是 [figure_crud_service.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/figure_service/figure_crud_service.py) `create_figure` 虽然接收 `user_id` 参数，但**未显式写入 ORM 模型**——`Figure(**figure_data)` 直接展开字典，依赖业务调用方把 `user_id` 塞进 `figure_data`（实际调用方 [figures.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/figures.py) `create_figure` 只把 `user_id` 当作"创建资产交易记录"用，从未塞进 `figure_data`），数据库层 `user_id` 走默认 NULL。修法：在 `create_figure` 内部、`Figure(**figure_data)` 展开前，把 `user_id` 显式注入 `figure_data`，并对外部已传入的 `user_id` 做幂等保护（`user_id not in figure_data` 才覆盖，避免被前端 payload 顶替）。

**实现逻辑：**

- **【#25 阶段 1】[figure_crud_service.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/figure_service/figure_crud_service.py) `create_figure` 显式注入 `user_id`**：在 `tag_names / market_price / purchase_type / tags` 全部处理完后、构造 `Figure(**figure_data)` 之前，单点补一行「`figure_data['user_id'] = user_id`」（带 `user_id is not None` 守卫 + `user_id not in figure_data` 幂等保护）
- **【#25 阶段 2】与 #19 隔离过滤保持一致**：调用方 [figures.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/figures.py) 仍传 `user_id=current_user.id` 给 service（不变），service 内部统一接管「写入所有者」的职责，调用方零认知
- **不变量**：
  - 0 前端改动（POST /api/figures 0 改动，user_id 在后端自动注入）
  - 0 数据库结构变更（`figures.user_id` 列已存在，仅业务写入）
  - 0 其它 CRUD 路径影响（`update_figure` / `delete_figure` / `batch_delete_figures` 走的是 id 定位，不写入 user_id，本修复不动这些路径）
  - 0 越权风险（user_id 仍由后端用 current_user 注入，前端无法冒名）
  - 0 资产交易干扰（AssetTransactionService 内部仍按 user_id 创建流水，与 Figure 行一致）
- **端到端验证**：
  - 用户 1 添加新手办 → `SELECT user_id FROM figures WHERE id=NEW` → 返回 1（非 NULL）
  - 新建手办在用户 1 的手办库列表立即可见
  - 用户 6 / 7 登录 → 看不到该新手办（被 #19 过滤逻辑正确排除）
  - 用户 6 / 7 越权访问 `GET /api/figures/{NEW_ID}` → 404（[figure_query_service.py:265-281](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/figure_service/figure_query_service.py#L265-L281) `get_figure_by_id` 仍按 user_id 过滤）
  - 与该手办联动的资产交易记录 `asset_transactions.user_id` 仍正确写入用户 1（链路一致）
- **影响范围**：
  - **修改** 1 个 service 文件（[figure_crud_service.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/figure_service/figure_crud_service.py)）+ 1 处显式 user_id 注入（带幂等保护）
  - **不动** [figures.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/figures.py) / [figure_query_service.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/figure_service/figure_query_service.py) / 模型 / 迁移脚本 / 前端

## 缺陷修复 → 26. 愿望清单功能中 URL 抓取生成愿望清单后的数据缺少填入 `user_id` 的缺陷

**解决方案：** 用户通过「愿望清单-URL 抓取」智能抓取 HPOI/AMiami/MFC 链接并保存后，新愿望的 `figures.user_id` 仍为 `NULL`。根因是端点调用链断裂——[wishlist_url.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/wishlist/wishlist_url.py) `/url-fetch` 仅做 URL 解析返回结构化数据（不写库），真正的写入由前端调 `POST /api/wishlist/`（[wishlist.py:125-140](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/wishlist/wishlist.py#L125-L140)）完成。该写入路径在 #19/#24 改造中**已正确传入 `current_user.id`**，但 URL 抓取场景下前端 `useWishlistUrlFetch.js saveFromUrl` 构造的 payload 不带 `user_id` 字段，且 #24 的 `WishlistCrudService.create_wishlist` 虽然接 `user_id` 参数并写入 `Figure(user_id=user_id)`（line 80），但**没有反向校验机制确保「写库前`user_id` 必须非 NULL」**。修法：① URL 抓取端点加 `current_user` 鉴权守卫（之前无认证，任何人可调用于刷 HPOI 缓存），保证调用链有用户上下文；② 写入服务对 `user_id` 做非 NULL 强制校验，缺失即 401，防止后续回归把 user_id 吞掉。

**实现逻辑：**

- **【#26 阶段 1】[wishlist_url.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/wishlist/wishlist_url.py) `/url-fetch` 加鉴权**：添加 `current_user: User = Depends(get_current_user)` 依赖，强制要求登录态（即使 URL 解析本身不写库，也避免未登录用户刷 HPOI 抓取队列 / 缓存投毒）
- **【#26 阶段 2】[wishlist_crud_service.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/wishlist_service/wishlist_crud_service.py) `create_wishlist` 强制 user_id 非 NULL 校验**：方法首行 `if user_id is None: raise HTTPException(401, "缺少用户上下文")`，作为最后一道防线——任何路径（URL 抓取 / 手动录入 / 批量导入）调到此方法，user_id 为空都拒绝写入，避免再次出现无主 NULL 数据
- **【#26 阶段 3】与 #19/#24 隔离过滤一致性闭环**：URL 抓取 → 前端调 `wishlistApi.create(payload)` → [wishlist.py:125-140](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/wishlist/wishlist.py#L125-L140) 透传 `current_user.id` → `WishlistCrudService.create_wishlist` 写入 `user_id` → DB 落库 user_id 非空 → #19 过滤正确显示给当前用户且对其他用户隐藏
- **不变量**：
  - 0 前端改动（`useWishlistUrlFetch.js` 仍走 `wishlistApi.create`，payload 不变）
  - 0 数据库结构变更（仅业务写入路径校验）
  - 0 其它 CRUD 路径影响（更新 / 删除 / 转库已在 #24 阶段 4 加所有权校验）
  - 0 抓取逻辑变化（HPOI 浏览器引擎 / Amiami 模板 / 缓存策略 0 改动）
  - 0 业务字段变化（name / price / currency / images / status 等全部 0 改动）
- **端到端验证**：
  - 用户 1 URL 抓取 HPOI 链接 → 前端 confirm → `POST /api/wishlist/` → DB `figures.user_id=1` ✓
  - 用户 6 / 7 URL 抓取 → DB `figures.user_id=6/7` ✓（不再被默认 NULL 覆盖）
  - 未登录用户直接调 `POST /api/wishlist/url-fetch` → 401（鉴权拦截）
  - 极端情况：调用方漏传 `current_user.id`（如内部脚本误调） → `create_wishlist` 抛 401 而非写入 NULL
  - 新建愿望仅自己可见（#19 隔离生效）
- **影响范围**：
  - **修改** 2 个文件（[wishlist_url.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/wishlist/wishlist_url.py) +1 鉴权 / [wishlist_crud_service.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/wishlist_service/wishlist_crud_service.py) +1 校验守卫）
  - **不动** [useWishlistUrlFetch.js](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Wishlist/composables/useWishlistUrlFetch.js) / 抓取引擎 / 缓存 / 模型 / 迁移脚本

## 缺陷修复 → 27. 愿望清单统计接口 `/api/wishlist/stats` 缺少数据隔离（不同用户能互相看到汇总数据）

**解决方案：** 任意用户调 `GET /api/wishlist/stats` 返回的 `total / budget_total / pending_purchase / status_distribution / top_manufacturers / last_month_total / releasing_names / transferred_amount` 全部是**全表汇总**——A 用户能看到 B 用户的愿望总数、预算总额、状态分布、TOP 厂商等敏感统计。根因是 [wishlist_stats.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/wishlist/wishlist_stats.py) 已加 `current_user` 鉴权 + 透传 `current_user.id` 给 `WishlistStatsService.get_stats`，但**服务层 9 个查询全部未真正使用 `user_id` 参数**（`base_query` 与 6 个独立查询均无 `Figure.user_id == user_id` 过滤），导致接口虽然接了 user_id 却仍全表扫描。修法：服务层所有 SQLAlchemy 查询统一补 `Figure.user_id == user_id` 过滤条件，确保统计指标严格按当前用户范围聚合。

**实现逻辑：**

- **【#27 阶段 1】[wishlist_stats_service.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/wishlist_service/wishlist_stats_service.py) `base_query` 补 user_id 过滤**：基础查询一次性加 `Figure.user_id == user_id`，所有依赖 `base_query` 的衍生统计（`total` / `releasing_this_month` / `budget_total` / `pending_purchase`）自动按用户隔离
- **【#27 阶段 2】5 个独立 query 补 user_id 过滤**：
  - 状态分布（5 号统计）：`status_dist_rows` 加 `Figure.user_id == user_id`
  - TOP 厂商（6 号统计）：`top_mfr_rows` 加 `Figure.user_id == user_id`
  - 较上月新增（7 号统计）：`last_month_count` 加 `Figure.user_id == user_id`
  - 本月即将发售名称（8 号统计）：`releasing_names` 加 `Figure.user_id == user_id`
  - 已转采购金额（9 号统计）：`transferred_rows` 加 `Figure.user_id == user_id`
- **【#27 阶段 3】与 #19 数据隔离架构保持一致**：与 [wishlist_query_service.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/wishlist_service/wishlist_query_service.py) `get_wishlist_list` 的 user_id 过滤逻辑对齐，9 个统计查询全部经过统一过滤模型
- **不变量**：
  - 0 前端改动（`GET /api/wishlist/stats` 仍由前端单点调用，响应结构不变）
  - 0 端点路径 / 响应结构变更（返回字段名 / 类型 / 数量 0 改动，仅数值按用户隔离）
  - 0 统计口径变化（愿望总数、预算合计、状态分布、TOP 厂商等计算公式 0 改动）
  - 0 汇率折算逻辑变化（`_convert_to_cny` 仍按 currency 折算 CNY）
  - 0 其它 service / 调度器 / 缓存影响
- **端到端验证**：
  - 用户 1（有 27 个愿望）调接口 → `total=27` / `budget_total=对应 CNY` / `status_distribution` 仅含用户 1 的状态
  - 用户 6 / 7（无愿望）调接口 → `total=0` / `budget_total=0.0` / `status_distribution={}`
  - 用户 1 `top_manufacturers` 列表仅含其愿望中的厂商，用户 6 调同一接口看到的厂商列表完全不同
  - 用户 1 `releasing_names` 仅含其本月即将发售的愿望名称
  - 用户 1 `transferred_amount` 仅含其已转采购的金额合计
  - 后端 `Application startup complete` 0 ERROR
- **影响范围**：
  - **修改** 1 个 service 文件（[wishlist_stats_service.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/wishlist_service/wishlist_stats_service.py)）+ 6 处 user_id 过滤（1 处 base_query + 5 处独立 query）
  - **不动** [wishlist_stats.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/wishlist/wishlist_stats.py) / 其它 service / API / 模型 / 前端 / 调度器






