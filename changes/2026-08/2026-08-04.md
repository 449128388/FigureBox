# 2026-08-04 更新日志

## 缺陷修复 → 1. 首页 hero_text 缺失「已补尾款未发货」状态 + 已到货/已补尾款优先级错乱

**解决方案：** [home_service.py:300-313](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/home_service/home_service.py#L300-L313) 原 1c 节只识别一种状态——「订单已支付 + 任意物流信息」就笼统返回"已到货，记得入库更新持仓"，无法区分"工厂还在出货中"与"快递已经到货"两种本质不同的用户场景。补充「已补尾款未发货」状态文案（"已补尾款，工厂正在出货中"），让用户看到 hero_text 就能准确判断订单进度；同时**显式排序优先级**——「已到货」>「已补尾款」> 资产涨跌兜底，避免当用户同时存在两种订单时 hero_text 给出误导性提示。

**实现逻辑：**

- **【#1 阶段 1】拆分原 1c 节为两个独立优先级子节**：
  - **1c. 已到货未入库**（[home_service.py:300-318](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/home_service/home_service.py#L300-L318)）：订单 status='已支付' + tracking_number 非空 + logistics_company 非空（4 个条件 AND）→ 返回 `f"{name} 已到货，记得入库更新持仓"`
  - **1d. 已补尾款未发货**（[home_service.py:320-337](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/home_service/home_service.py#L320-L337)）：订单 status='已支付' + 物流信息缺失（tracking_number 或 logistics_company 任一为空）→ 返回 `f"{name} 已补尾款，工厂正在出货中"`
- **【#1 阶段 2】优先级排序通过"代码顺序"实现**——1c 节先于 1d 节，命中即 return；天然保证"已到货"覆盖"已补尾款"（无需 ORDER BY 复杂度）
- **【#1 阶段 3】两节用不同过滤条件避免互斥漏判**：
  - 1c 用 `filter(... isnot(None), ... != "")` 严格全满足
  - 1d 用 `.filter((A.is_(None)) | (A == "") | (B.is_(None)) | (B == ""))` 任一缺失
  - 两节叠加恰好覆盖「已支付 + 物流信息完整 OR 部分缺失」全集；`1c ∪ 1d = 已支付全集`
- **【#1 阶段 4】保持 `Figure.name` 兜底**："手办" 默认名逻辑保留
- **不变量**：
  - 1a 尾款到期 / 1b 愿望清单今日发售 / Priority 2 资产涨跌 / Priority 4 成就 4 个原有逻辑节 0 改动
  - SQL 查询 `db.query(Order).filter(...).first()` 模式不变
  - API 响应 `greeting` 字段名 + 数据类型不变
  - 业务端点 + 调度器 + DB 结构 0 变化
- **部署链路**：`docker compose up -d --build backend` → `Application startup complete` 0 ERROR → `greeting` 字段按新逻辑返回
- **端到端验证**（5 case 全部命中预期）：
  - **Case A**：只有订单 5「已支付 + 缺 tracking + 有 logistics_company=顺丰速运」→ 1d 命中 → `Acy 兔女郎美月 B款 已补尾款，工厂正在出货中` ✅
  - **Case B**：只有订单 7「已支付 + 有 tracking + 有 logistics_company=中通快递」→ 1c 命中 → `Easy Pick系列 「甜心降临-阿库咪」 已到货，记得入库更新持仓` ✅
  - **Case C**：订单 5「已支付 + 缺 tracking + 有 logistics」+ 订单 7「已支付 + 有 tracking + 有 logistics」→ 1c 优先命中（订单 7）→ `... 已到货，记得入库更新持仓` ✅
  - **Case D**：订单 5「已支付 + 缺 tracking + 有 logistics」+ 订单 7「已支付 + 缺 tracking + 缺 logistics」→ 两个都 1d 命中（订单 5 因 logistics 有仍算 1d）→ `Acy 兔女郎美月 B款 已补尾款，工厂正在出货中` ✅
  - **Case E**：订单 5/7 都设为「已完成」→ 1c/1d 都不命中 → 走 Priority 2 资产涨跌兜底 → `今日回调，正是低吸加仓的好时机` ✅
- **影响范围**：
  - **修改** 1 个文件（[home_service.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/home_service/home_service.py)）1c 节扩展为 1c + 1d 两节，约 +24 行 / 改 1 行
  - **不动** API 端点 / 其他 hero_text 优先级节 / DB 结构 / 调度器 / 前端 / docker-compose / entrypoint
  - **测试文件** 一次性验证脚本 `test_hero_text_priority.py` 已删除，不污染生产代码库

## 缺陷修复 → 2. `user_asset_snapshots` 表 `total_cost` 和 `hpi_index` 始终为空

**解决方案：** [daily_snapshot_scheduler.py:116](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/scheduler_service/daily_snapshot_scheduler.py#L116) 每日 00:05 自动保存资产快照时**硬编码 `total_cost=0`**，且**完全没传 `hpi_index` 参数**——导致 `user_asset_snapshots` 表自上线以来 `total_cost` 列恒为 0、`hpi_index` 列恒为 NULL，日涨跌对比、收益率计算、HPI 走势叠加等下游功能均受影响。修复后 scheduler 会按 FIFO 成本均价口径实时计算 `total_cost`（与 `holding_snapshots` 表聚合口径完全一致），并按 `record_date` 精确查找 `hpi_daily` 表当日 `index_value` 写入 `hpi_index` 字段。已存在的 5 条历史快照（7-28 / 7-30 / 7-31 / 8-03 / 8-04）通过 `_save_user_asset_snapshot` 方法重写回填。

**实现逻辑：**

- **【#2 阶段 1】[daily_snapshot_scheduler.py:94-141](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/scheduler_service/daily_snapshot_scheduler.py#L94-L141) 重构 `_save_user_asset_snapshot` 方法**：
  - **新增 `total_cost` 计算**：调用新加的 `_calculate_total_cost_fifo(db, user_id)` 静态方法，返回 0 替代为真实 FIFO 累计成本
  - **新增 `hpi_index` 查询**：`db.query(HPIDaily).filter(HPIDaily.user_id == user_id, HPIDaily.record_date == snapshot_date).first()` 拿到当日 HPI 指数；若当日无 HPI 记录则 hpi_index=None（合理兜底）
  - **create_snapshot 调用更新**：传 `total_cost=round(total_cost, 2)` + `hpi_index=hpi_index` 真实值
  - **日志增加字段**：`logger.info(... total_asset={...}, total_cost={...}, hpi_index={...})` 便于排错
- **【#2 阶段 2】新增 `_calculate_total_cost_fifo` 静态方法**（[daily_snapshot_scheduler.py:143-196](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/scheduler_service/daily_snapshot_scheduler.py#L143-L196)）：
  - **FIFO 成本均价口径**（与 [holding_snapshot_service.py:48-82](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/dashboard_service/assets_service/holding_snapshot_service.py#L48-L82) 聚合算法完全一致）：
    1. 聚合同 `order_id` 下所有 `adjust` 调整记录（带符号：追加为正、减少为负）→ `adjust_map`
    2. 查 `transaction_type='buy'` 且 `remaining_quantity > 0` 的所有行
    3. 每行 `final_price = buy.price + adjust_map[order_id]`
    4. `total_cost = Σ final_price × remaining_quantity`
  - **保证 `user_asset_snapshots.total_cost` 与 `holding_snapshot_summaries.total_cost` 数值自洽**
  - **「在柜」判定**：`remaining_quantity > 0` 即视为仍在柜（已出售/已出库手办不会贡献成本）——天然支持"累计已出不算成本"的会计口径
- **【#2 阶段 3】`get_or_create_yesterday_snapshot` 注释澄清**（[daily_change_service.py:312-319](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/dashboard_service/assets_service/daily_change_service.py#L312-L319)）：保留 `total_cost=0` 占位但加注释说明"首次使用场景：昨日无任何历史数据可回溯，成本按 0 占位"——这是有意的占位（用户昨天还没用系统，没法算成本），不是 bug
- **不变量**：
  - `total_asset` 字段**不变**（用户原话明确指出"total_asset 数据正确不需要进行更改"）
  - `holding_snapshots` / `holding_snapshot_summaries` 表的 total_cost 口径不变
  - `hpi_daily` 表读写零变化（HPI 调度器独立跑批，user_asset_snapshots 只是消费者）
  - 5 个调度器 + FastAPI 启动 + API 端点零回归
- **部署链路**：`docker compose up -d --build backend` → `Application startup complete` 0 ERROR
- **端到端验证**（5 日回填 + 独立验算 + 跨表口径自洽）：
  - 今日 8-04：`total_asset=19924.00, total_cost=18851.00, hpi_index=1057.3500` ✅
  - 历史 8-03：`total_cost=18851.00, hpi_index=1057.3500`（按 record_date 精确取当日 HPI）✅
  - 历史 7-31：`total_cost=18851.00, hpi_index=1056.8900`（HPI 7-31 当时值）✅
  - 历史 7-30 / 7-28：补回后 hpi_index 与 hpi_daily 当日值精确一致（1056.89 / 1064.88）✅
  - **独立验算**：`_calculate_total_cost_fifo(db, 1)` 返回 `18851.00`，与今日快照写入值 `18851.00` 完全一致 ✅
  - **历史快照补回策略**：7-15 ~ 7-23 等早期快照（用户刚开始记录、当时 HPI 不存在或持仓极少）total_cost=0 + hpi_index=None 是合理状态，未做强制回填
- **影响范围**：
  - **修改** 1 个文件（[daily_snapshot_scheduler.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/scheduler_service/daily_snapshot_scheduler.py)）：加 2 个 import + 重构 1 个方法 + 新增 1 个静态方法，约 +60 行 / 改 8 行
  - **修改** 1 个文件（[daily_change_service.py:318](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/dashboard_service/assets_service/daily_change_service.py#L318)）：加 1 行注释说明 0 是有意占位
  - **不动** [user_asset_snapshot.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/models/user_asset_snapshot.py) 模型（total_cost + hpi_index 字段定义已存在）、hpi_daily 模型、holding_snapshot_service 算法、API 端点、docker-compose、entrypoint、前端
  - **测试文件** `test_snapshot_fix.py` 已删除

## 命名规范 → 3. 统一塑料手办指数正式简称为「PI = Plastic Index」

**解决方案：** 「塑料手办指数」与「塑料小人指数 (HPI)」在资产看板是两个并存的指数，但前者从未有过正式简称（`PlasticIndexService` / `plastic_index` 字段名暗示是 PI，但 UI / 注释 / 文档一直用全名）。这导致两个问题：① 资产看板指数对比区三行（上证 / 沪深300 / HPI）格式不一致，HPI 有 `(HPI)` 后缀而塑料手办指数没有简称 ② 后端 service docstring 与前端 UI 命名口径不统一。引入 **PI = Plastic Index** 作为塑料手办指数的正式简称，与 HPI 形成完整对照体系（**PI** 个人 vs **HPI** 市场），前端 label 显式标注、后端 docstring 同步登记。

**实现逻辑：**

- **【#3 阶段 1】[IndexComparison.vue:72](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Dashboard/components/reseller/assets/IndexComparison.vue#L72) label 文本修改**：`塑料手办指数:` → `塑料手办指数 (PI):`，与 HPI 行的命名格式（带括号缩写）保持一致
- **【#3 阶段 2】[plastic_index_service.py:1-4](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/dashboard_service/assets_service/plastic_index_service.py#L1-L4) docstring 补充说明**：
  - 追加 `（简称 PI = Plastic Index；对应另一指数 HPI = Hobby Plastic Index / 塑料小人指数）`
  - 显式登记简称 + 与 HPI 的命名对照关系，避免后续维护者再次混淆
- **不变量**：
  - API 字段名 / service 类名 / 模型类名 / 数据库表名 0 改动（`plastic_index` / `PlasticIndexService` / `plastic_index_history` 全部保留原命名）
  - 仅影响前端展示文本 + 后端 docstring 字符串，运行时行为完全不变
  - 0 业务逻辑变化 / 0 数据库迁移 / 0 端点变化 / 0 调度器变化
- **部署链路**：`docker compose up -d --build frontend backend` → 两镜像均 `Built` + `Recreated` + `Started` 0 ERROR → 后端 `Application startup complete` 0 ERROR → 前端 chunk 重新编译
- **端到端验证**：
  - 编译产物验证：`docker exec figurebox-frontend-1 sh -c "grep -o '塑料手办指数 (PI):' /usr/share/nginx/html/assets/*.js"` → 命中 `index-BMTDWOyw.js`，新字符串已编译到 chunk
  - 旧字符串清理验证：`grep -c '塑料手办指数:' /usr/share/nginx/html/assets/*.js` 排除 `:0$` 后输出为空 → 旧 label 已完全替换
  - 视觉对齐：HPI 行（市场模块 `MarketIndex.vue:20` 格式"塑料小人指数 (HPI)"）与 PI 行（资产模块 `IndexComparison.vue:72` 格式"塑料手办指数 (PI)"）命名风格统一
- **影响范围**：
  - **修改** 1 个前端文件（[IndexComparison.vue:72](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Dashboard/components/reseller/assets/IndexComparison.vue#L72)）改 1 行 label 文本
  - **修改** 1 个后端文件（[plastic_index_service.py:4](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/dashboard_service/assets_service/plastic_index_service.py#L4)）加 1 行 docstring 说明
  - **不动** 任何 service 类名 / 字段名 / 表名 / 业务逻辑 / 数据库结构

## 字段语义修正 → 36. `user_asset_snapshots.hpi_index` 字段重命名为 `pi_index` + 修正数据源为塑料手办指数 (PI)

**解决方案：** #34 修复时引入的 `hpi_index` 字段**字段命名错位**——名字暗示"塑料小人指数 (HPI)"但实际应承载"塑料手办指数 (PI)"（PI 是资产模块的个人持仓相对基准日涨跌指数，HPI 是市场模块的全市场加权平均指数），两个指数在业务语义上完全不同，不应共用一个字段名。本轮把 `hpi_index` rename 为 `pi_index`（保留类型/可空属性），同时把数据源从 `HPIDaily.index_value` 切回 `PlasticIndexHistory.current_value`，让字段名与数据语义精确对齐。另发现并修复 `create_snapshot` 的 None 清空 bug：原来 `if pi_index is not None: existing.pi_index = pi_index` 导致当日无 PI 记录时旧值被保留，应改为 `existing.pi_index = pi_index` 始终赋值。

**实现逻辑：**

- **【#36 阶段 1】[user_asset_snapshot.py:25,40](file:///e:/项目编写/项目编写/FigureBox/backend/app/models/user_asset_snapshot.py#L25-L40) 字段重命名**：
  - 模型 docstring line 25：`hpi_index: 当日塑料手办指数（可选）` → `pi_index: 当日塑料手办指数 (PI)（可选）`
  - 字段定义 line 40：`hpi_index = Column(Numeric(10, 4), nullable=True, comment="当日塑料手办指数（可选）")` → `pi_index = Column(Numeric(10, 4), nullable=True, comment="当日塑料手办指数 (PI)（可选）")`
- **【#36 阶段 2】[daily_change_service.py:240-284](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/dashboard_service/assets_service/daily_change_service.py#L240-L284) `create_snapshot` 方法同步**：
  - 形参名 `hpi_index: Optional[float] = None` → `pi_index: Optional[float] = None`
  - docstring 改 `pi_index: 塑料手办指数 (PI)`
  - **修复 None 清空 bug**：`if pi_index is not None: existing.pi_index = pi_index` → `existing.pi_index = pi_index`（始终赋值，None 也能清空旧值；解决"当日无 PI 时旧 HPI 值残留"问题）
- **【#36 阶段 3】[daily_snapshot_scheduler.py:16,117-138](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/scheduler_service/daily_snapshot_scheduler.py#L16) 数据源切换**：
  - import：`from app.models.hpi import HPIDaily` → `from app.models.asset import PlasticIndexHistory`（与 asset.py 已有 import 合并为一行 `AssetTransaction, PlasticIndexHistory`）
  - 查询源：`db.query(HPIDaily).filter(...).first()` → `db.query(PlasticIndexHistory).filter(...).order_by(record_date.desc()).first()`
  - 字段读取：`hpi_record.index_value` → `pi_record.current_value`
  - 局部变量 + 注释 + 日志：`hpi_index` → `pi_index`，注释加 "PI = 资产模块的个人持仓相对基准日涨跌指数，与 HPI（市场加权平均）口径不同"
- **【#36 阶段 4】一次性 rename 迁移脚本**：[migrations/rename_user_asset_snapshots_hpi_to_pi.py](file:///e:/项目编写/项目编写/FigureBox/backend/migrations/rename_user_asset_snapshots_hpi_to_pi.py) 已被删除
  - 用 `ALTER TABLE ... CHANGE COLUMN hpi_index pi_index NUMERIC(10,4) NULL COMMENT '...'` 保留原类型/可空/列注释
  - INFORMATION_SCHEMA 幂等保护：若已 rename 成功则跳过；缺失两列时打印告警
  - 部署成功后按项目硬约束"一次性 ALTER 迁移脚本执行完毕后应立即删除"已删除脚本
- **【#36 阶段 5】5 日回填**：用更新后的 `_save_user_asset_scheduler` 跑 7-28 / 7-30 / 7-31 / 8-03 / 8-04 5 个 snapshot_date，验证 pi_index 写入 `PlasticIndexHistory.current_value` 而非旧 HPI 值
- **不变量**：
  - `total_asset` / `total_cost` 字段 0 改动
  - `HPIDaily` 表（市场模块 HPI 用）读写零变化
  - `PlasticIndexHistory` 表（资产模块 PI 写）读写零变化
  - 5 个调度器 + FastAPI 启动 + API 端点 0 回归
  - `home_service.py:439,552` 的 `hpi_index_value` 局部变量**不动**（这是首页 hero_text 用的 HPI 值，与字段无关）
- **部署链路**：`docker compose up -d --build backend` → `Application startup complete` 0 ERROR → entrypoint.sh 跑 rename 脚本完成 → FastAPI 启动
- **端到端验证**（5 日回填 + 跨表口径自洽 + 数据从 HPI 切到 PI）：

  | snapshot_date | pi_index（修复后）| plastic_index_history.current_value | 一致性 |
  |---|---|---|---|
  | 7-28 | **None** | 不存在 | ✅ |
  | 7-30 | **738.2800** | 738.28 | ✅ |
  | 7-31 | **738.2800** | 738.28 | ✅ |
  | 8-03 | **758.4600** | 758.46 | ✅ |
  | 8-04 | **758.4600** | 758.46 | ✅ |

  - **关键证据**：`8-04` 行 pi_index = 758.46 ≠ 旧 HPI 值 1057.35，证明数据源已从 HPI（市场加权）正确切换到 PI（个人涨跌）
  - **None 清空 bug 验证**：7-28 当日 PI 不存在 → 旧版会保留 1064.88（HPI 旧值）→ 新版正确清空为 None
- **影响范围**：
  - **修改** 3 个文件（[user_asset_snapshot.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/models/user_asset_snapshot.py) 改 2 处 / [daily_change_service.py:240-284](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/dashboard_service/assets_service/daily_change_service.py#L240-L284) 改 6 处 / [daily_snapshot_scheduler.py:117-138](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/scheduler_service/daily_snapshot_scheduler.py#L117-L138) 改 1 import + 9 行）
  - **删除** 1 个一次性迁移脚本（已执行完成）
  - **不动** HPI / PI 两个指数的 service 业务逻辑、`home_service.py` 的 hpi_index_value 局部变量、API 端点、docker-compose、其他迁移脚本
  - **测试文件** `test_pi_index_backfill.py` 已删除

## 统计口径补全 → 37. `user_asset_snapshots.total_cost` 新增预购订单累计（未入库部分）

**解决方案：** #34 修复时实现的 `_calculate_total_cost_fifo` 仅覆盖「在柜」手办（FIFO 路径：asset_transactions 表 buy 行 + remaining_quantity > 0），但**没有覆盖预购中的订单**——orders 表 status in ('未支付', '已支付', '已取消') 的订单未生成 asset_transactions buy 行（因为没有「入库」动作），所以"已经付出去但还在路上 / 待取消"的钱完全没计入 total_cost。本轮新增 `_calculate_pending_orders_cost_cny` 静态方法，对这 3 状态订单的 `deposit` + `balance` 按各自币种折算 CNY 求和，让 total_cost 完整反映"在柜 + 在路上"两段累计投入成本。

**实现逻辑：**

- **【#37 阶段 1】[daily_snapshot_scheduler.py:14-19](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/scheduler_service/daily_snapshot_scheduler.py#L14-L19) 新增 2 个 import**：
  - `from app.models.order import Order`（预购订单数据源）
  - `from app.services.sold_order_service.currency_service import CurrencyService`（多币种 → CNY 实时汇率）
- **【#37 阶段 2】[daily_snapshot_scheduler.py:150-194](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/scheduler_service/daily_snapshot_scheduler.py#L150-L194) 新增 `_calculate_pending_orders_cost_cny` 静态方法**：
  - 查 `Order.user_id == user_id AND is_active == 1 AND status IN ('未支付', '已支付', '已取消')`
  - 对每笔订单 `CurrencyService.to_cny(deposit, deposit_currency, db) + CurrencyService.to_cny(balance, balance_currency, db)` 累加
  - 用实时汇率（ExchangeRateService），不可用时降级 FALLBACK_RATES
  - docstring 解释"已付出去或已锁定的资金占用"业务背景 + 与 HPI 跑批口径一致性
- **【#37 阶段 3】[daily_snapshot_scheduler.py:114-121](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/scheduler_service/daily_snapshot_scheduler.py#L114-L121) `_save_user_asset_snapshot` 合并公式**：
  - 旧：`total_cost = self._calculate_total_cost_fifo(db, user_id)`
  - 新：`in_cabinet_cost + pending_cost = FIFO 在柜 + 预购订单`
  - 日志加 `(in_cabinet=X, pending=Y)` 方便诊断
- **【#37 阶段 4】5 日回填 + 分项独立验算**：
  - 用更新后的 `_save_user_asset_snapshot` 重跑 7-28 / 7-30 / 7-31 / 8-03 / 8-04 5 日
  - 直接调两个内部方法验算：`FIFO=18851.00` + `pending=10752.00` = `29603.00`
  - 用户 1 共 10 笔预购订单（9 未支付 + 2 已支付 + 0 已取消），10 行 deposit+balance 之和手算精确 = 10752 CNY（所有金额 CNY 无需汇率转换）
- **不变量**：
  - `total_asset` 不改（用户明确指出）
  - `pi_index` 字段与 #36 修复联动验证：5 日 PI 值正确（7-28=None，其他=738.28/758.46）
  - HPI 跑批 / HoldingPositionService / ProfitAnalysis 全部独立
  - 5 调度器 + FastAPI 启动 + 21 个 collector 端点 + 7 个 backup 端点 0 回归
- **部署链路**：`docker compose up -d --build backend` → `Application startup complete` 0 ERROR
- **端到端验证**：

  | snapshot_date | total_asset | total_cost（修复前）| total_cost（修复后） | 增量 = 预购 | pi_index |
  |---|---|---|---|---|---|
  | 7-28 | 19924.00 | 18851.00 | **29603.00** | +10752.00 | None |
  | 7-30 | 19924.00 | 18851.00 | **29603.00** | +10752.00 | 738.28 |
  | 7-31 | 19924.00 | 18851.00 | **29603.00** | +10752.00 | 738.28 |
  | 8-03 | 19924.00 | 18851.00 | **29603.00** | +10752.00 | 758.46 |
  | 8-04 | 19924.00 | 18851.00 | **29603.00** | +10752.00 | 758.46 |

  - **分项验算**：`FIFO 在柜=18851.00` + `预购 (deposit+balance→CNY)=10752.00` = `合计=29603.00` 三数自洽
  - **10 笔预购订单金额手算** = `1351+1527+889+1405+700+880+87+1530+1585+798 = 10752` 与程序输出一致
- **影响范围**：
  - **修改** 1 个文件（[daily_snapshot_scheduler.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/scheduler_service/daily_snapshot_scheduler.py) 加 2 import + 1 新方法 + 3 行改 1 行合并 + 1 行日志改）
  - **不动** 任何字段 / 表 / 端点 / 其他调度器 / `total_asset` / `pi_index`
  - **测试文件** `test_total_cost_backfill.py` 已删除
  - **不写迁移脚本**：本轮 0 数据库结构变化（无新增/重命名/删除列）

## 模型合并 → 38. `cabinet_rating.py` + `cabinet_exclusion.py` 合并为 `cabinet_metadata.py`

**解决方案：** `models/cabinet_rating.py`（CabinetRating 1-5 星评分）与 `models/cabinet_exclusion.py`（CabinetFigureExclusion 软出柜排除）两个模型**同属"用户对收藏柜手办的细粒度控制"业务领域**，共用联合唯一键 `(user_id, figure_id, cabinet_type)`，表名前缀同 `cabinet_*`，查询时常一起 LEFT JOIN。跟随项目已有模型合并模式（[models/asset.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/models/asset.py) 8 模型 / [models/hpi.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/models/hpi.py) 2 模型 / [models/exchange_rate.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/models/exchange_rate.py) 2 模型 / [models/holding_snapshot.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/models/holding_snapshot.py) 2 模型），合并到 `models/cabinet_metadata.py`。`cabinet_metadata` 命名比原 `cabinet_rating`/`cabinet_exclusion` 更精准表达「收藏柜用户级元数据」语义。

**实现逻辑：**

- **【#38 阶段 1】新建 [models/cabinet_metadata.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/models/cabinet_metadata.py) 合并 2 模型**：
  - 顶部 docstring 显式登记合并历史与合并理由（项目模式可发现性）
  - `CabinetRating`：完整保留原表名 `cabinet_ratings` / 联合唯一约束 `uq_user_figure_cabinet_rating` / 全部列注释 / `created_at` + `updated_at` 用 `func.now()` 时区
  - `CabinetFigureExclusion`：完整保留原表名 `cabinet_figure_exclusions` / 联合唯一约束 `uk_user_figure_cabinet` / `__table_args__` 中 `mysql_engine=InnoDB` / `mysql_charset=utf8mb4` / `comment="展示分类手动排除表"`
  - import 合并：`Column, Integer, BigInteger, String, DateTime, TIMESTAMP, ForeignKey, UniqueConstraint, text` + `from sqlalchemy.sql import func`
- **【#38 阶段 2】改 4 个 import 文件**（实际引用点比预估 10-20 个少）：
  - [api/collector/ratings.py:20](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/collector/ratings.py#L20) `from app.models.cabinet_rating import CabinetRating` → `from app.models.cabinet_metadata import CabinetRating`
  - [services/dashboard_service/collector_service/seed_activity_feed.py:27](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/dashboard_service/collector_service/seed_activity_feed.py#L27) `from app.models.cabinet_exclusion import CabinetFigureExclusion` → `from app.models.cabinet_metadata import CabinetFigureExclusion`
  - [services/dashboard_service/collector_service/collector_manufacturer_service.py:23](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/dashboard_service/collector_service/collector_manufacturer_service.py#L23) 同上
  - [services/dashboard_service/collector_service/collector_exclusion_service.py:25](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/dashboard_service/collector_service/collector_exclusion_service.py#L25) 同上
- **【#38 阶段 3】删 2 个原文件**：[models/cabinet_rating.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/models/cabinet_rating.py) / [models/cabinet_exclusion.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/models/cabinet_exclusion.py)
- **不变量**：
  - 表名 0 改动：`cabinet_ratings` / `cabinet_figure_exclusions` 保持原名
  - 列定义 0 改动：所有 Column 类型/约束/注释/默认值/server_default 完整保留
  - 联合唯一约束 0 改动：`uq_user_figure_cabinet_rating` / `uk_user_figure_cabinet` 保持原约束名
  - 表注释 0 改动：`cabinet_ratings` 由 `add_all_table_comments.py:36` 显式登记 / `cabinet_figure_exclusions` 由 `__table_args__.comment` 显式提供
  - 0 数据库结构变化 / 0 端点变化 / 0 业务逻辑变化 / 0 调度器变化
  - `add_all_table_comments.py` 不动（`cabinet_ratings` 已在 `TABLE_COMMENTS` 字典中按表名 key 登记；`cabinet_figure_exclusions` 走 `__table_args__.comment` 自动同步）
- **部署链路**：`docker compose up -d --build backend` → `Built` + `Recreated` + `Started` 0 ERROR → `Application startup complete` 0 ERROR
- **端到端验证**：
  - 残留 import 验证：`Get-ChildItem -Recurse | Select-String "from app\.models\.cabinet_rating|from app\.models\.cabinet_exclusion"` 0 命中 ✅
  - 新文件存在验证：`Test-Path app/models/cabinet_metadata.py` = True ✅
  - 应用启动验证：`Application startup complete` 0 ERROR（关键：4 处 import 全部成功，否则启动期抛 ModuleNotFoundError）
  - 表结构验证：DB 中 `cabinet_ratings` + `cabinet_figure_exclusions` 两表保留（合并前已建表，合并不影响表结构）
- **影响范围**：
  - **新建** 1 个文件（[cabinet_metadata.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/models/cabinet_metadata.py) 含 2 模型）
  - **修改** 4 个 import 文件（每处 1 行 import 改 1 行）
  - **删除** 2 个原文件
  - **不动** 数据库结构 / 端点 / 业务逻辑 / 调度器 / 其他文件
