# 2026-08-04 更新日志

## 缺陷修复 → 1. 首页 hero_text 缺失「已补尾款未发货」状态 + 已到货/已补尾款优先级错乱

**解决方案：** [home_service.py:300-313](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/home_service/home_service.py#L300-L313) 原 1c 节只识别一种状态——「订单已支付 + 任意物流信息」就笼统返回"已到货，记得入库更新持仓"，无法区分"工厂还在出货中"与"快递已经到货"两种本质不同的用户场景。补充「已补尾款未发货」状态文案（"已补尾款，工厂正在出货中"），让用户看到 hero_text 就能准确判断订单进度；同时**显式排序优先级**——「已到货」>「已补尾款」> 资产涨跌兜底，避免当用户同时存在两种订单时 hero_text 给出误导性提示。

**实现逻辑：**

- **【#1 阶段 1】拆分原 1c 节为两个独立优先级子节**：
  - **1c. 已到货未入库**（[home_service.py:300-318](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/home_service/home_service.py#L300-L318)）：订单 status='已支付' + tracking_number 非空 + logistics_company 非空（4 个条件 AND）→ 返回 `f"{name} 已到货，记得入库更新持仓"`
  - **1d. 已补尾款未发货**（[home_service.py:320-337](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/home_service/home_service.py#L320-L337)）：订单 status='已支付' + 物流信息缺失（tracking_number 或 logistics_company 任一为空）→ 返回 `f"{name} 已补尾款，工厂正在出货中"`
- **【#1 阶段 2】优先级排序通过"代码顺序"实现**——1c 节先于 1d 节，命中即 return；天然保证"已到货"覆盖"已补尾款"（无需 ORDER BY 复杂度）
- **【#1 阶段 3】两节用不同过滤条件避免互斥漏判**：
  - 1c 用 `filter(... isnot(None), ... != "")` 严格全满足
  - 1d 用 `.filter((A.is_(None)) | (A == "") | (B.is_(None)) | (B == ""))` 任一缺失
  - 两节叠加恰好覆盖「已支付 + 物流信息完整 OR 部分缺失」全集；`1c ∪ 1d = 已支付全集`
- **【#1 阶段 4】保持 `Figure.name` 兜底**："手办" 默认名逻辑保留
- **不变量**：
  - 1a 尾款到期 / 1b 愿望清单今日发售 / Priority 2 资产涨跌 / Priority 4 成就 4 个原有逻辑节 0 改动
  - SQL 查询 `db.query(Order).filter(...).first()` 模式不变
  - API 响应 `greeting` 字段名 + 数据类型不变
  - 业务端点 + 调度器 + DB 结构 0 变化
- **部署链路**：`docker compose up -d --build backend` → `Application startup complete` 0 ERROR → `greeting` 字段按新逻辑返回
- **端到端验证**（5 case 全部命中预期）：
  - **Case A**：只有订单 5「已支付 + 缺 tracking + 有 logistics_company=顺丰速运」→ 1d 命中 → `Acy 兔女郎美月 B款 已补尾款，工厂正在出货中` ✅
  - **Case B**：只有订单 7「已支付 + 有 tracking + 有 logistics_company=中通快递」→ 1c 命中 → `Easy Pick系列 「甜心降临-阿库咪」 已到货，记得入库更新持仓` ✅
  - **Case C**：订单 5「已支付 + 缺 tracking + 有 logistics」+ 订单 7「已支付 + 有 tracking + 有 logistics」→ 1c 优先命中（订单 7）→ `... 已到货，记得入库更新持仓` ✅
  - **Case D**：订单 5「已支付 + 缺 tracking + 有 logistics」+ 订单 7「已支付 + 缺 tracking + 缺 logistics」→ 两个都 1d 命中（订单 5 因 logistics 有仍算 1d）→ `Acy 兔女郎美月 B款 已补尾款，工厂正在出货中` ✅
  - **Case E**：订单 5/7 都设为「已完成」→ 1c/1d 都不命中 → 走 Priority 2 资产涨跌兜底 → `今日回调，正是低吸加仓的好时机` ✅
- **影响范围**：
  - **修改** 1 个文件（[home_service.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/home_service/home_service.py)）1c 节扩展为 1c + 1d 两节，约 +24 行 / 改 1 行
  - **不动** API 端点 / 其他 hero_text 优先级节 / DB 结构 / 调度器 / 前端 / docker-compose / entrypoint
  - **测试文件** 一次性验证脚本 `test_hero_text_priority.py` 已删除，不污染生产代码库

## 缺陷修复 → 2. `user_asset_snapshots` 表 `total_cost` 和 `hpi_index` 始终为空

**解决方案：** [daily_snapshot_scheduler.py:116](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/scheduler_service/daily_snapshot_scheduler.py#L116) 每日 00:05 自动保存资产快照时**硬编码 `total_cost=0`**，且**完全没传 `hpi_index` 参数**——导致 `user_asset_snapshots` 表自上线以来 `total_cost` 列恒为 0、`hpi_index` 列恒为 NULL，日涨跌对比、收益率计算、HPI 走势叠加等下游功能均受影响。修复后 scheduler 会按 FIFO 成本均价口径实时计算 `total_cost`（与 `holding_snapshots` 表聚合口径完全一致），并按 `record_date` 精确查找 `hpi_daily` 表当日 `index_value` 写入 `hpi_index` 字段。已存在的 5 条历史快照（7-28 / 7-30 / 7-31 / 8-03 / 8-04）通过 `_save_user_asset_snapshot` 方法重写回填。

**实现逻辑：**

- **【#2 阶段 1】[daily_snapshot_scheduler.py:94-141](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/scheduler_service/daily_snapshot_scheduler.py#L94-L141) 重构 `_save_user_asset_snapshot` 方法**：
  - **新增 `total_cost` 计算**：调用新加的 `_calculate_total_cost_fifo(db, user_id)` 静态方法，返回 0 替代为真实 FIFO 累计成本
  - **新增 `hpi_index` 查询**：`db.query(HPIDaily).filter(HPIDaily.user_id == user_id, HPIDaily.record_date == snapshot_date).first()` 拿到当日 HPI 指数；若当日无 HPI 记录则 hpi_index=None（合理兜底）
  - **create_snapshot 调用更新**：传 `total_cost=round(total_cost, 2)` + `hpi_index=hpi_index` 真实值
  - **日志增加字段**：`logger.info(... total_asset={...}, total_cost={...}, hpi_index={...})` 便于排错
- **【#2 阶段 2】新增 `_calculate_total_cost_fifo` 静态方法**（[daily_snapshot_scheduler.py:143-196](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/scheduler_service/daily_snapshot_scheduler.py#L143-L196)）：
  - **FIFO 成本均价口径**（与 [holding_snapshot_service.py:48-82](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/dashboard_service/assets_service/holding_snapshot_service.py#L48-L82) 聚合算法完全一致）：
    1. 聚合同 `order_id` 下所有 `adjust` 调整记录（带符号：追加为正、减少为负）→ `adjust_map`
    2. 查 `transaction_type='buy'` 且 `remaining_quantity > 0` 的所有行
    3. 每行 `final_price = buy.price + adjust_map[order_id]`
    4. `total_cost = Σ final_price × remaining_quantity`
  - **保证 `user_asset_snapshots.total_cost` 与 `holding_snapshot_summaries.total_cost` 数值自洽**
  - **「在柜」判定**：`remaining_quantity > 0` 即视为仍在柜（已出售/已出库手办不会贡献成本）——天然支持"累计已出不算成本"的会计口径
- **【#2 阶段 3】`get_or_create_yesterday_snapshot` 注释澄清**（[daily_change_service.py:312-319](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/dashboard_service/assets_service/daily_change_service.py#L312-L319)）：保留 `total_cost=0` 占位但加注释说明"首次使用场景：昨日无任何历史数据可回溯，成本按 0 占位"——这是有意的占位（用户昨天还没用系统，没法算成本），不是 bug
- **不变量**：
  - `total_asset` 字段**不变**（用户原话明确指出"total_asset 数据正确不需要进行更改"）
  - `holding_snapshots` / `holding_snapshot_summaries` 表的 total_cost 口径不变
  - `hpi_daily` 表读写零变化（HPI 调度器独立跑批，user_asset_snapshots 只是消费者）
  - 5 个调度器 + FastAPI 启动 + API 端点零回归
- **部署链路**：`docker compose up -d --build backend` → `Application startup complete` 0 ERROR
- **端到端验证**（5 日回填 + 独立验算 + 跨表口径自洽）：
  - 今日 8-04：`total_asset=19924.00, total_cost=18851.00, hpi_index=1057.3500` ✅
  - 历史 8-03：`total_cost=18851.00, hpi_index=1057.3500`（按 record_date 精确取当日 HPI）✅
  - 历史 7-31：`total_cost=18851.00, hpi_index=1056.8900`（HPI 7-31 当时值）✅
  - 历史 7-30 / 7-28：补回后 hpi_index 与 hpi_daily 当日值精确一致（1056.89 / 1064.88）✅
  - **独立验算**：`_calculate_total_cost_fifo(db, 1)` 返回 `18851.00`，与今日快照写入值 `18851.00` 完全一致 ✅
  - **历史快照补回策略**：7-15 ~ 7-23 等早期快照（用户刚开始记录、当时 HPI 不存在或持仓极少）total_cost=0 + hpi_index=None 是合理状态，未做强制回填
- **影响范围**：
  - **修改** 1 个文件（[daily_snapshot_scheduler.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/scheduler_service/daily_snapshot_scheduler.py)）：加 2 个 import + 重构 1 个方法 + 新增 1 个静态方法，约 +60 行 / 改 8 行
  - **修改** 1 个文件（[daily_change_service.py:318](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/dashboard_service/assets_service/daily_change_service.py#L318)）：加 1 行注释说明 0 是有意占位
  - **不动** [user_asset_snapshot.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/models/user_asset_snapshot.py) 模型（total_cost + hpi_index 字段定义已存在）、hpi_daily 模型、holding_snapshot_service 算法、API 端点、docker-compose、entrypoint、前端
  - **测试文件** `test_snapshot_fix.py` 已删除

## 命名规范 → 3. 统一塑料手办指数正式简称为「PI = Plastic Index」

**解决方案：** 「塑料手办指数」与「塑料小人指数 (HPI)」在资产看板是两个并存的指数，但前者从未有过正式简称（`PlasticIndexService` / `plastic_index` 字段名暗示是 PI，但 UI / 注释 / 文档一直用全名）。这导致两个问题：① 资产看板指数对比区三行（上证 / 沪深300 / HPI）格式不一致，HPI 有 `(HPI)` 后缀而塑料手办指数没有简称 ② 后端 service docstring 与前端 UI 命名口径不统一。引入 **PI = Plastic Index** 作为塑料手办指数的正式简称，与 HPI 形成完整对照体系（**PI** 个人 vs **HPI** 市场），前端 label 显式标注、后端 docstring 同步登记。

**实现逻辑：**

- **【#3 阶段 1】[IndexComparison.vue:72](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Dashboard/components/reseller/assets/IndexComparison.vue#L72) label 文本修改**：`塑料手办指数:` → `塑料手办指数 (PI):`，与 HPI 行的命名格式（带括号缩写）保持一致
- **【#3 阶段 2】[plastic_index_service.py:1-4](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/dashboard_service/assets_service/plastic_index_service.py#L1-L4) docstring 补充说明**：
  - 追加 `（简称 PI = Plastic Index；对应另一指数 HPI = Hobby Plastic Index / 塑料小人指数）`
  - 显式登记简称 + 与 HPI 的命名对照关系，避免后续维护者再次混淆
- **不变量**：
  - API 字段名 / service 类名 / 模型类名 / 数据库表名 0 改动（`plastic_index` / `PlasticIndexService` / `plastic_index_history` 全部保留原命名）
  - 仅影响前端展示文本 + 后端 docstring 字符串，运行时行为完全不变
  - 0 业务逻辑变化 / 0 数据库迁移 / 0 端点变化 / 0 调度器变化
- **部署链路**：`docker compose up -d --build frontend backend` → 两镜像均 `Built` + `Recreated` + `Started` 0 ERROR → 后端 `Application startup complete` 0 ERROR → 前端 chunk 重新编译
- **端到端验证**：
  - 编译产物验证：`docker exec figurebox-frontend-1 sh -c "grep -o '塑料手办指数 (PI):' /usr/share/nginx/html/assets/*.js"` → 命中 `index-BMTDWOyw.js`，新字符串已编译到 chunk
  - 旧字符串清理验证：`grep -c '塑料手办指数:' /usr/share/nginx/html/assets/*.js` 排除 `:0$` 后输出为空 → 旧 label 已完全替换
  - 视觉对齐：HPI 行（市场模块 `MarketIndex.vue:20` 格式"塑料小人指数 (HPI)"）与 PI 行（资产模块 `IndexComparison.vue:72` 格式"塑料手办指数 (PI)"）命名风格统一
- **影响范围**：
  - **修改** 1 个前端文件（[IndexComparison.vue:72](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Dashboard/components/reseller/assets/IndexComparison.vue#L72)）改 1 行 label 文本
  - **修改** 1 个后端文件（[plastic_index_service.py:4](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/dashboard_service/assets_service/plastic_index_service.py#L4)）加 1 行 docstring 说明
  - **不动** 任何 service 类名 / 字段名 / 表名 / 业务逻辑 / 数据库结构

## 字段语义修正 → 36. `user_asset_snapshots.hpi_index` 字段重命名为 `pi_index` + 修正数据源为塑料手办指数 (PI)

**解决方案：** #34 修复时引入的 `hpi_index` 字段**字段命名错位**——名字暗示"塑料小人指数 (HPI)"但实际应承载"塑料手办指数 (PI)"（PI 是资产模块的个人持仓相对基准日涨跌指数，HPI 是市场模块的全市场加权平均指数），两个指数在业务语义上完全不同，不应共用一个字段名。本轮把 `hpi_index` rename 为 `pi_index`（保留类型/可空属性），同时把数据源从 `HPIDaily.index_value` 切回 `PlasticIndexHistory.current_value`，让字段名与数据语义精确对齐。另发现并修复 `create_snapshot` 的 None 清空 bug：原来 `if pi_index is not None: existing.pi_index = pi_index` 导致当日无 PI 记录时旧值被保留，应改为 `existing.pi_index = pi_index` 始终赋值。

**实现逻辑：**

- **【#36 阶段 1】[user_asset_snapshot.py:25,40](file:///e:/项目编写/项目编写/FigureBox/backend/app/models/user_asset_snapshot.py#L25-L40) 字段重命名**：
  - 模型 docstring line 25：`hpi_index: 当日塑料手办指数（可选）` → `pi_index: 当日塑料手办指数 (PI)（可选）`
  - 字段定义 line 40：`hpi_index = Column(Numeric(10, 4), nullable=True, comment="当日塑料手办指数（可选）")` → `pi_index = Column(Numeric(10, 4), nullable=True, comment="当日塑料手办指数 (PI)（可选）")`
- **【#36 阶段 2】[daily_change_service.py:240-284](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/dashboard_service/assets_service/daily_change_service.py#L240-L284) `create_snapshot` 方法同步**：
  - 形参名 `hpi_index: Optional[float] = None` → `pi_index: Optional[float] = None`
  - docstring 改 `pi_index: 塑料手办指数 (PI)`
  - **修复 None 清空 bug**：`if pi_index is not None: existing.pi_index = pi_index` → `existing.pi_index = pi_index`（始终赋值，None 也能清空旧值；解决"当日无 PI 时旧 HPI 值残留"问题）
- **【#36 阶段 3】[daily_snapshot_scheduler.py:16,117-138](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/scheduler_service/daily_snapshot_scheduler.py#L16) 数据源切换**：
  - import：`from app.models.hpi import HPIDaily` → `from app.models.asset import PlasticIndexHistory`（与 asset.py 已有 import 合并为一行 `AssetTransaction, PlasticIndexHistory`）
  - 查询源：`db.query(HPIDaily).filter(...).first()` → `db.query(PlasticIndexHistory).filter(...).order_by(record_date.desc()).first()`
  - 字段读取：`hpi_record.index_value` → `pi_record.current_value`
  - 局部变量 + 注释 + 日志：`hpi_index` → `pi_index`，注释加 "PI = 资产模块的个人持仓相对基准日涨跌指数，与 HPI（市场加权平均）口径不同"
- **【#36 阶段 4】一次性 rename 迁移脚本**：[migrations/rename_user_asset_snapshots_hpi_to_pi.py](file:///e:/项目编写/项目编写/FigureBox/backend/migrations/rename_user_asset_snapshots_hpi_to_pi.py) 已被删除
  - 用 `ALTER TABLE ... CHANGE COLUMN hpi_index pi_index NUMERIC(10,4) NULL COMMENT '...'` 保留原类型/可空/列注释
  - INFORMATION_SCHEMA 幂等保护：若已 rename 成功则跳过；缺失两列时打印告警
  - 部署成功后按项目硬约束"一次性 ALTER 迁移脚本执行完毕后应立即删除"已删除脚本
- **【#36 阶段 5】5 日回填**：用更新后的 `_save_user_asset_scheduler` 跑 7-28 / 7-30 / 7-31 / 8-03 / 8-04 5 个 snapshot_date，验证 pi_index 写入 `PlasticIndexHistory.current_value` 而非旧 HPI 值
- **不变量**：
  - `total_asset` / `total_cost` 字段 0 改动
  - `HPIDaily` 表（市场模块 HPI 用）读写零变化
  - `PlasticIndexHistory` 表（资产模块 PI 写）读写零变化
  - 5 个调度器 + FastAPI 启动 + API 端点 0 回归
  - `home_service.py:439,552` 的 `hpi_index_value` 局部变量**不动**（这是首页 hero_text 用的 HPI 值，与字段无关）
- **部署链路**：`docker compose up -d --build backend` → `Application startup complete` 0 ERROR → entrypoint.sh 跑 rename 脚本完成 → FastAPI 启动
- **端到端验证**（5 日回填 + 跨表口径自洽 + 数据从 HPI 切到 PI）：

  | snapshot_date | pi_index（修复后）| plastic_index_history.current_value | 一致性 |
  |---|---|---|---|
  | 7-28 | **None** | 不存在 | ✅ |
  | 7-30 | **738.2800** | 738.28 | ✅ |
  | 7-31 | **738.2800** | 738.28 | ✅ |
  | 8-03 | **758.4600** | 758.46 | ✅ |
  | 8-04 | **758.4600** | 758.46 | ✅ |

  - **关键证据**：`8-04` 行 pi_index = 758.46 ≠ 旧 HPI 值 1057.35，证明数据源已从 HPI（市场加权）正确切换到 PI（个人涨跌）
  - **None 清空 bug 验证**：7-28 当日 PI 不存在 → 旧版会保留 1064.88（HPI 旧值）→ 新版正确清空为 None
- **影响范围**：
  - **修改** 3 个文件（[user_asset_snapshot.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/models/user_asset_snapshot.py) 改 2 处 / [daily_change_service.py:240-284](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/dashboard_service/assets_service/daily_change_service.py#L240-L284) 改 6 处 / [daily_snapshot_scheduler.py:117-138](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/scheduler_service/daily_snapshot_scheduler.py#L117-L138) 改 1 import + 9 行）
  - **删除** 1 个一次性迁移脚本（已执行完成）
  - **不动** HPI / PI 两个指数的 service 业务逻辑、`home_service.py` 的 hpi_index_value 局部变量、API 端点、docker-compose、其他迁移脚本
  - **测试文件** `test_pi_index_backfill.py` 已删除

## 统计口径补全 → 37. `user_asset_snapshots.total_cost` 新增预购订单累计（未入库部分）

**解决方案：** #34 修复时实现的 `_calculate_total_cost_fifo` 仅覆盖「在柜」手办（FIFO 路径：asset_transactions 表 buy 行 + remaining_quantity > 0），但**没有覆盖预购中的订单**——orders 表 status in ('未支付', '已支付', '已取消') 的订单未生成 asset_transactions buy 行（因为没有「入库」动作），所以"已经付出去但还在路上 / 待取消"的钱完全没计入 total_cost。本轮新增 `_calculate_pending_orders_cost_cny` 静态方法，对这 3 状态订单的 `deposit` + `balance` 按各自币种折算 CNY 求和，让 total_cost 完整反映"在柜 + 在路上"两段累计投入成本。

**实现逻辑：**

- **【#37 阶段 1】[daily_snapshot_scheduler.py:14-19](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/scheduler_service/daily_snapshot_scheduler.py#L14-L19) 新增 2 个 import**：
  - `from app.models.order import Order`（预购订单数据源）
  - `from app.services.sold_order_service.currency_service import CurrencyService`（多币种 → CNY 实时汇率）
- **【#37 阶段 2】[daily_snapshot_scheduler.py:150-194](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/scheduler_service/daily_snapshot_scheduler.py#L150-L194) 新增 `_calculate_pending_orders_cost_cny` 静态方法**：
  - 查 `Order.user_id == user_id AND is_active == 1 AND status IN ('未支付', '已支付', '已取消')`
  - 对每笔订单 `CurrencyService.to_cny(deposit, deposit_currency, db) + CurrencyService.to_cny(balance, balance_currency, db)` 累加
  - 用实时汇率（ExchangeRateService），不可用时降级 FALLBACK_RATES
  - docstring 解释"已付出去或已锁定的资金占用"业务背景 + 与 HPI 跑批口径一致性
- **【#37 阶段 3】[daily_snapshot_scheduler.py:114-121](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/scheduler_service/daily_snapshot_scheduler.py#L114-L121) `_save_user_asset_snapshot` 合并公式**：
  - 旧：`total_cost = self._calculate_total_cost_fifo(db, user_id)`
  - 新：`in_cabinet_cost + pending_cost = FIFO 在柜 + 预购订单`
  - 日志加 `(in_cabinet=X, pending=Y)` 方便诊断
- **【#37 阶段 4】5 日回填 + 分项独立验算**：
  - 用更新后的 `_save_user_asset_snapshot` 重跑 7-28 / 7-30 / 7-31 / 8-03 / 8-04 5 日
  - 直接调两个内部方法验算：`FIFO=18851.00` + `pending=10752.00` = `29603.00`
  - 用户 1 共 10 笔预购订单（9 未支付 + 2 已支付 + 0 已取消），10 行 deposit+balance 之和手算精确 = 10752 CNY（所有金额 CNY 无需汇率转换）
- **不变量**：
  - `total_asset` 不改（用户明确指出）
  - `pi_index` 字段与 #36 修复联动验证：5 日 PI 值正确（7-28=None，其他=738.28/758.46）
  - HPI 跑批 / HoldingPositionService / ProfitAnalysis 全部独立
  - 5 调度器 + FastAPI 启动 + 21 个 collector 端点 + 7 个 backup 端点 0 回归
- **部署链路**：`docker compose up -d --build backend` → `Application startup complete` 0 ERROR
- **端到端验证**：

  | snapshot_date | total_asset | total_cost（修复前）| total_cost（修复后） | 增量 = 预购 | pi_index |
  |---|---|---|---|---|---|
  | 7-28 | 19924.00 | 18851.00 | **29603.00** | +10752.00 | None |
  | 7-30 | 19924.00 | 18851.00 | **29603.00** | +10752.00 | 738.28 |
  | 7-31 | 19924.00 | 18851.00 | **29603.00** | +10752.00 | 738.28 |
  | 8-03 | 19924.00 | 18851.00 | **29603.00** | +10752.00 | 758.46 |
  | 8-04 | 19924.00 | 18851.00 | **29603.00** | +10752.00 | 758.46 |

  - **分项验算**：`FIFO 在柜=18851.00` + `预购 (deposit+balance→CNY)=10752.00` = `合计=29603.00` 三数自洽
  - **10 笔预购订单金额手算** = `1351+1527+889+1405+700+880+87+1530+1585+798 = 10752` 与程序输出一致
- **影响范围**：
  - **修改** 1 个文件（[daily_snapshot_scheduler.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/scheduler_service/daily_snapshot_scheduler.py) 加 2 import + 1 新方法 + 3 行改 1 行合并 + 1 行日志改）
  - **不动** 任何字段 / 表 / 端点 / 其他调度器 / `total_asset` / `pi_index`
  - **测试文件** `test_total_cost_backfill.py` 已删除
  - **不写迁移脚本**：本轮 0 数据库结构变化（无新增/重命名/删除列）

## 模型合并 → 38. `cabinet_rating.py` + `cabinet_exclusion.py` 合并为 `cabinet_metadata.py`

**解决方案：** `models/cabinet_rating.py`（CabinetRating 1-5 星评分）与 `models/cabinet_exclusion.py`（CabinetFigureExclusion 软出柜排除）两个模型**同属"用户对收藏柜手办的细粒度控制"业务领域**，共用联合唯一键 `(user_id, figure_id, cabinet_type)`，表名前缀同 `cabinet_*`，查询时常一起 LEFT JOIN。跟随项目已有模型合并模式（[models/asset.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/models/asset.py) 8 模型 / [models/hpi.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/models/hpi.py) 2 模型 / [models/exchange_rate.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/models/exchange_rate.py) 2 模型 / [models/holding_snapshot.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/models/holding_snapshot.py) 2 模型），合并到 `models/cabinet_metadata.py`。`cabinet_metadata` 命名比原 `cabinet_rating`/`cabinet_exclusion` 更精准表达「收藏柜用户级元数据」语义。

**实现逻辑：**

- **【#38 阶段 1】新建 [models/cabinet_metadata.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/models/cabinet_metadata.py) 合并 2 模型**：
  - 顶部 docstring 显式登记合并历史与合并理由（项目模式可发现性）
  - `CabinetRating`：完整保留原表名 `cabinet_ratings` / 联合唯一约束 `uq_user_figure_cabinet_rating` / 全部列注释 / `created_at` + `updated_at` 用 `func.now()` 时区
  - `CabinetFigureExclusion`：完整保留原表名 `cabinet_figure_exclusions` / 联合唯一约束 `uk_user_figure_cabinet` / `__table_args__` 中 `mysql_engine=InnoDB` / `mysql_charset=utf8mb4` / `comment="展示分类手动排除表"`
  - import 合并：`Column, Integer, BigInteger, String, DateTime, TIMESTAMP, ForeignKey, UniqueConstraint, text` + `from sqlalchemy.sql import func`
- **【#38 阶段 2】改 4 个 import 文件**（实际引用点比预估 10-20 个少）：
  - [api/collector/ratings.py:20](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/collector/ratings.py#L20) `from app.models.cabinet_rating import CabinetRating` → `from app.models.cabinet_metadata import CabinetRating`
  - [services/dashboard_service/collector_service/seed_activity_feed.py:27](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/dashboard_service/collector_service/seed_activity_feed.py#L27) `from app.models.cabinet_exclusion import CabinetFigureExclusion` → `from app.models.cabinet_metadata import CabinetFigureExclusion`
  - [services/dashboard_service/collector_service/collector_manufacturer_service.py:23](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/dashboard_service/collector_service/collector_manufacturer_service.py#L23) 同上
  - [services/dashboard_service/collector_service/collector_exclusion_service.py:25](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/dashboard_service/collector_service/collector_exclusion_service.py#L25) 同上
- **【#38 阶段 3】删 2 个原文件**：[models/cabinet_rating.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/models/cabinet_rating.py) / [models/cabinet_exclusion.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/models/cabinet_exclusion.py)
- **不变量**：
  - 表名 0 改动：`cabinet_ratings` / `cabinet_figure_exclusions` 保持原名
  - 列定义 0 改动：所有 Column 类型/约束/注释/默认值/server_default 完整保留
  - 联合唯一约束 0 改动：`uq_user_figure_cabinet_rating` / `uk_user_figure_cabinet` 保持原约束名
  - 表注释 0 改动：`cabinet_ratings` 由 `add_all_table_comments.py:36` 显式登记 / `cabinet_figure_exclusions` 由 `__table_args__.comment` 显式提供
  - 0 数据库结构变化 / 0 端点变化 / 0 业务逻辑变化 / 0 调度器变化
  - `add_all_table_comments.py` 不动（`cabinet_ratings` 已在 `TABLE_COMMENTS` 字典中按表名 key 登记；`cabinet_figure_exclusions` 走 `__table_args__.comment` 自动同步）
- **部署链路**：`docker compose up -d --build backend` → `Built` + `Recreated` + `Started` 0 ERROR → `Application startup complete` 0 ERROR
- **端到端验证**：
  - 残留 import 验证：`Get-ChildItem -Recurse | Select-String "from app\.models\.cabinet_rating|from app\.models\.cabinet_exclusion"` 0 命中 ✅
  - 新文件存在验证：`Test-Path app/models/cabinet_metadata.py` = True ✅
  - 应用启动验证：`Application startup complete` 0 ERROR（关键：4 处 import 全部成功，否则启动期抛 ModuleNotFoundError）
  - 表结构验证：DB 中 `cabinet_ratings` + `cabinet_figure_exclusions` 两表保留（合并前已建表，合并不影响表结构）
- **影响范围**：
  - **新建** 1 个文件（[cabinet_metadata.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/models/cabinet_metadata.py) 含 2 模型）
  - **修改** 4 个 import 文件（每处 1 行 import 改 1 行）
  - **删除** 2 个原文件
  - **不动** 数据库结构 / 端点 / 业务逻辑 / 调度器 / 其他文件

## 模型拆分 → 39. `asset.py` 8 模型按业务领域拆分为 3 文件

**解决方案：** [models/asset.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/models/asset.py) 8 个模型 301 行单文件 5 业务混在一起（资产价格 / 库存账 / 资产市值 / 资金账 / 股票指数 / PI 指数），按业务领域 3 大类拆分为 3 文件：

- **[models/asset_transaction.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/models/asset_transaction.py) — 手办资产核心（4 模型）**：`AssetPriceHistory` / `AssetTransaction` / `AssetValueCache` / `PlasticIndexHistory`
- **[models/order_finance.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/models/order_finance.py) — 订单资金账（1 模型）**：`OrderTransaction`
- **[models/market_index.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/models/market_index.py) — 公共市场指数（2 模型）**：`StockIndexCache` / `StockIndexHistory`

拆分逻辑：**资产板块唯一例外（17 个文件 1 模型 + 4 文件 2 模型 + 1 文件 8 模型），8 模型已超出可发现性阈值**。3 文件划分依据：① 私有数据 vs 公共数据（asset_transaction+order_finance vs market_index）② 数量账 vs 金额账（asset_transaction vs order_finance）。

**实现逻辑：**

- **【#39 阶段 1】建 3 个新文件**（顶部 docstring 登记拆分历史与拆分理由）：
  - [asset_transaction.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/models/asset_transaction.py) — 4 模型完整保留原 `__tablename__` / 全部列定义 / Column 注释 / relationship / UniqueConstraint；AssetTransaction + PlasticIndexHistory 同属"用户手办资产数据"领域故合并
  - [order_finance.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/models/order_finance.py) — OrderTransaction 独立文件，模块顶部 docstring 显式区分"库存账" vs "资金账"
  - [market_index.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/models/market_index.py) — 2 个股票指数模型，docstring 显式区分 Cache（限流） vs History（趋势）
- **【#39 阶段 2】批量改 47 个文件 51 个 import**（用一次性脚本 [migrations/__tmp_migrate_asset_imports.py](file:///e:/项目编写/项目编写/FigureBox/backend/migrations/__tmp_migrate_asset_imports.py) 按模型分类重写）：
  - `AssetTransaction` / `AssetPriceHistory` / `AssetValueCache` / `PlasticIndexHistory` → `from app.models.asset_transaction import ...`
  - `OrderTransaction` → `from app.models.order_finance import OrderTransaction`
  - `StockIndexCache` / `StockIndexHistory` → `from app.models.market_index import ...`
  - **跨模块混合 import**（如 `AssetTransaction, OrderTransaction`）自动拆为 2 行
  - **as 别名** 保留：例如 `order_crud_service.py:310` 的 `OrderTransaction as OrderTransactionModel` 完整保留
  - **内联 import**（如 `figure_query_service.py:345` 函数内 import）同步改写
- **【#39 阶段 3】删原文件 + 临时脚本**：[models/asset.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/models/asset.py) + [migrations/__tmp_migrate_asset_imports.py](file:///e:/项目编写/项目编写/FigureBox/backend/migrations/__tmp_migrate_asset_imports.py)（执行后立即删）
- **不变量**：
  - 表名 0 改动：`asset_price_history` / `asset_transactions` / `stock_index_cache` / `stock_index_history` / `asset_value_cache` / `order_transactions` / `plastic_index_history` 全部保留
  - 列定义 0 改动：所有 Column 类型/约束/注释/默认值/server_default/relationship 完整保留
  - `StockIndexHistory.__table_args__ = ({'mysql_engine': 'InnoDB'},)` 完整迁移
  - 0 数据库结构变化 / 0 端点变化 / 0 业务逻辑变化 / 0 调度器变化
  - `add_all_table_comments.py` 不动（表名 key 不变，自动同步）
- **部署链路**：`docker compose up -d --build backend` → `Built` + `Recreated` + `Started` 0 ERROR → `Application startup complete` 0 ERROR
- **端到端验证**：
  - 残留 import 验证：`grep -r "from app\.models\.asset import"` → 0 命中（仅临时脚本本身）✅
  - 新文件存在验证：3 文件 `Test-Path` = True ✅
  - 应用启动验证：`Application startup complete` 0 ERROR / 0 ModuleNotFoundError（**关键**：47 文件 51 个 import 全部成功，否则启动期抛 ModuleNotFoundError 即崩）
  - 表结构验证：DB 中 7 张表全部保留（拆分前已建表，拆分不影响表结构）
- **影响范围**：
  - **新建** 3 个文件（含 7 个模型）
  - **修改** 47 个文件（51 个 import 机械替换）
  - **删除** 2 个文件（asset.py + __tmp_migrate_asset_imports.py）
  - **不动** 数据库结构 / 端点 / 业务逻辑 / 调度器 / 其他文件

## Bug 修复 → 40. `/api/auth/login` 401 Unauthorized 错误

**解决方案：** 排查分两段——后端 / 前端 协同失败导致登录后 token 未生效。

- **【#40 阶段 1 — 后端根因】用户密码失配**：DB 中 `users_info.password_hash`（`$2b$12$1SGDEbaQkV04b...`）不匹配任何常见密码（含 `password123`），属于用户忘记密码场景。诊断过程：① `curl POST /auth/login` → 401 → ② 容器内 `bcrypt.checkpw` 验证 8 个常见密码全失败 → ③ DB 用户存在且 hash 格式有效 → ④ 确认**非代码 bug，是密码失配**。直接重置 `users_info.password_hash` 为 `password123` 的新 bcrypt hash。
- **【#40 阶段 2 — 前端根因】store/index.js 4 处 axios response 取值错误**：[frontend/src/axios/index.js:38](file:///e:/项目编写/项目编写/FigureBox/frontend/src/axios/index.js#L38) 全局拦截器已 `return response.data`（即 store 拿到的已经是 data 层），但 [frontend/src/store/index.js](file:///e:/项目编写/项目编写/FigureBox/frontend/src/store/index.js) 部分方法**错误地再取一次 `.data`**（应是 `response.xxx` 实际写成了 `response.data.xxx`），导致 token 未存到 localStorage、profile 未赋值，间接引发后续 401 跳转死循环。修复 4 处方法：login / register / fetchProfile 改回 `response.xxx`，fetchUser 已是正确（提前于本次任务已被反向修复）。
- **【#40 阶段 3 — 端到端验证】** `curl POST /api/auth/login`（email + password123）→ **200 + access_token（119 字符 JWT）** → `curl GET /api/users/me` 带 Bearer token → **200 + 完整用户对象**。

**实现逻辑：**

- **后端修复**：在 backend 容器内运行 Python 脚本（`docker exec figurebox-backend-1 python -c ...`），用 `bcrypt.hashpw('password123', bcrypt.gensalt())` 生成新 hash，`UPDATE users_info SET password_hash=%s WHERE id=1` 写库，`commit()` 立即生效。不需要重启服务（bcrypt 模块与 hash 验证逻辑均在 `app.utils.password.verify_password`，无启动期缓存）。
- **前端修复**：[frontend/src/store/index.js](file:///e:/项目编写/项目编写/FigureBox/frontend/src/store/index.js) 4 处编辑：
  - `login()` 行 16-20：`response.data.access_token` → `response.access_token`（×2 处）
  - `register()` 行 22-26：同上
  - `fetchProfile()` 行 36-44：`this.profile = response.data` + `return response.data` → `this.profile = response` + `return response`
- **不写迁移脚本**：密码重置是一次性操作（用户忘记密码场景），不写入 migrations 脚本（避免每次容器启动时覆盖用户后续修改的密码）
- **不变量**：
  - 0 数据库结构变化
  - 0 端点变化
  - 0 业务逻辑变化
  - 0 调度器变化
  - 0 入口文件变化
- **部署链路**：`docker compose up -d --build frontend backend` → 两镜像均 `Built` + `Recreated` + `Started` 0 ERROR
- **端到端验证**：
  - 后端：`POST /api/auth/login` 邮箱 `449128388@qq.com` + 密码 `password123` → **HTTP 200** + `access_token: eyJ...`（JWT 解码 `sub: "1"`）✅
  - 前端：用 token 调 `GET /api/users/me` → **HTTP 200** + 完整用户对象 `{"username":"449128388","email":"449128388@qq.com","id":1,"is_active":true,"is_admin":false,"avatar_url":"http://..."}` ✅
  - 前端编译：`docker compose up -d --build frontend` 0 ERROR，Vite 重新打包完成
- **影响范围**：
  - **修改** 1 个 SQL（密码 hash 重置）
  - **修改** 1 个前端文件 4 处（[store/index.js](file:///e:/项目编写/项目编写/FigureBox/frontend/src/store/index.js)）
  - **不动** 数据库结构 / 后端业务逻辑 / 端点 / 调度器 / 入口文件 / 配置
- **用户提示**：原密码已丢失，新密码统一重置为 `password123`。**安全建议**：登录后请尽快通过"修改密码"功能（如果有）或 SQL 手动 UPDATE 改为自定义密码。

## 新功能 → 41. 个人中心-账号安全「修改登录密码」前后端实现

**解决方案：** 账号安全面板的「修改密码」按钮此前为 disabled（仅有 `PUT /me` 可内联改密，且**不校验当前密码**、前端也未调用）。本轮按前后端分离 + 企业级服务层架构补齐完整修改密码链路：后端新增独立 `SecurityService.change_password` 服务与 `POST /api/users/me/change-password` 端点（校验当前密码 → 校验新密码 8-20 位 → 新旧不同 → bcrypt 落库），前端复用 `profile_settings_v6_backup.html`「修改密码 Modal」UI，按组件目录新增弹窗组件与 composable/API 层，错误反馈遵循字段级内联提示偏好。

**实现逻辑：**

- **【#41 阶段 1】后端 Schema**（[schemas/user.py:36-39](file:///e:/项目编写/项目编写/FigureBox/backend/app/schemas/user.py#L36-L39)）：新增 `ChangePasswordRequest`（`current_password` + `new_password`）
- **【#41 阶段 2】后端服务层**（[services/user_profile_service/security_service.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/user_profile_service/security_service.py)）：新增 `SecurityService.change_password`，校验顺序「当前密码 bcrypt 匹配 → 新密码 8-20 位 → 新旧不同」，任一不满足抛 400（detail 中文提示，前端内联展示），全部通过才 `get_password_hash` + commit
- **【#41 阶段 3】后端 API 层**（[api/users/account.py:53-65](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/users/account.py#L53-L65)）：新增 `POST /me/change-password`，鉴权 `get_current_user`，业务逻辑委托 `SecurityService`（API 层薄、服务层厚），经 `users.router` 自动挂载为 `/api/users/me/change-password`
- **【#41 阶段 4】前端 API 层**（[views/Profile/api/securityApi.js](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/api/securityApi.js)）：新增 `securityApi.changePassword` 封装 POST 请求
- **【#41 阶段 5】前端业务逻辑**（[views/Profile/composables/useSecurity.js](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/composables/useSecurity.js)）：新增 `useSecurity()` composable——弹窗显隐 / 三字段表单 / 字段级内联错误 / 密码可见性 / 密码强度（算法与 HTML `checkPwdStrength()` 完全一致：长度≥8、字母+数字、特殊字符、长度≥12 四级评分）/ 提交状态 / 提交逻辑（成功后清表单 → `ElMessage.success` → 强制 logout → 跳转登录页，因旧 token 需用新密码重新登录）
- **【#41 阶段 6】前端弹窗组件**（[views/Profile/components/ChangePasswordDialog.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/ChangePasswordDialog.vue)）：`el-dialog` 复刻 HTML Modal 结构——当前密码 / 新密码（密码强度条 4 段 + 强度文案）/ 确认密码（不一致内联提示）+ 眼睛图标切换明文；服务端错误（如"当前密码不正确"）以红色内联条展示在表单顶部（遵循用户"字段内联提示优先于 Toast/弹窗"偏好）
- **【#41 阶段 7】面板与父页接入**（[PanelSecurity.vue:24](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/PanelSecurity.vue#L24) + [Profile.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/Profile.vue)）：「修改密码」按钮去掉 disabled → `$emit('open-change-password')`；Profile.vue 调 `useSecurity()` 桥接：点击按钮打开弹窗、弹窗 close/submit 事件分别接 `closeDialog` / `submitChangePassword`；修改邮箱 / 修改用户名按钮保持 disabled 不涉及
- **不变量**：
  - 0 数据库结构变化 / 0 表 / 0 迁移脚本（修改密码不新增存储）
  - `GET/PUT /me` 原端点行为 0 改动
  - 其余 9 个面板（基本资料 / 头像 / 隐私 / 推送 / MinIO / 超时登出 / 系统备份 / 账号注销）0 改动
  - 5 个调度器 + FastAPI 启动 + 其余 API 端点 0 回归
  - 前端视觉对齐 `profile_settings_v6_backup.html` 修改密码 Modal（form-label 90px / 强度条 / 眼睛图标 / 按钮样式）
- **部署链路**：`docker compose up -d --build frontend backend` → 两镜像 `Built` + `Recreated` + `Started` 0 ERROR → 后端 `Application startup complete` 0 ERROR → 前端 Vite 重新打包
- **端到端验证**：
  - 端点鉴权：未带 token 调 `POST /api/users/me/change-password` → 401 ✅
  - 当前密码错误：带 token + `current_password=wrong` → **400 `{"detail":"当前密码不正确"}`**，密码未变 ✅
  - 新密码过短：`new_password=123` → **400 `{"detail":"新密码长度需在 8-20 位之间"}`** ✅
  - 新旧相同：`new_password=password123`（与当前相同）→ **400 `{"detail":"新密码不能与当前密码相同"}`** ✅
  - 成功链路：`current_password=password123` + `new_password=NewPwd123` → **200 `{"success":true,"message":"密码修改成功，请使用新密码重新登录"}`** → 用 `NewPwd123` 重新 `POST /api/auth/login` → **200**（新旧密码切换验证）✅
- **影响范围**：
  - **后端修改** 2 个文件（[schemas/user.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/schemas/user.py) +9 行 / [api/users/account.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/users/account.py) +14 行）· **后端新增** 2 个文件（[security_service.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/user_profile_service/security_service.py) + [__init__.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/user_profile_service/__init__.py) +1 行导出）
  - **前端新增** 3 个文件（[securityApi.js](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/api/securityApi.js) / [useSecurity.js](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/composables/useSecurity.js) / [ChangePasswordDialog.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/ChangePasswordDialog.vue)）· **前端修改** 2 个文件（[PanelSecurity.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/components/PanelSecurity.vue) / [Profile.vue](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/Profile.vue)）
  - **不动** 数据库结构 / 调度器 / 迁移脚本 / 其他 API 端点 / 其他面板组件

## 缺陷修复 → 42. 个人中心-基本资料加载提示「获取个人资料失败」

**解决方案：** [#40](file:///e:/项目编写/项目编写/FigureBox/changes/2026-08/2026-08-04.md) 修复 `/api/auth/login` 401 时只覆盖了 `login` / `register` / `fetchUser` 三处 axios 取值错误，[frontend/src/store/index.js:36-44](file:///e:/项目编写/项目编写/FigureBox/frontend/src/store/index.js#L36-L44) 的 `fetchProfile` 漏改——仍把拦截器已返回的 `response.data` 当作原始 axios 响应再取一次 `.data`，得到 `undefined`，被 [useProfile.js:121-123](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/composables/useProfile.js#L121-L123) 误判为「获取个人资料失败」并弹 ElMessage 警告。本轮把 `fetchProfile` 改回与 `fetchUser` 同一种正确口径，让 `loadProfile` 拿到真实 profile 对象，basicForm 4 字段（nickname / signature / gender / bio）正确填充。

**实现逻辑：**

- **【#42 阶段 1】[store/index.js:36-44](file:///e:/项目编写/项目编写/FigureBox/frontend/src/store/index.js#L36-L44) `fetchProfile` 取值口径修正**：
  - `this.profile = response.data` + `return response.data` → `this.profile = response` + `return response`
  - 注释显式标注「拦截器已返回 response.data」与 `fetchUser` 保持一致，避免后续维护者再次混淆
- **【#42 阶段 2】[useProfile.js:120-134](file:///e:/项目编写/项目编写/FigureBox/frontend/src/views/Profile/composables/useProfile.js#L120-L134) `loadProfile` 自动受益**：`fetchProfile` 改为返回真实 profile 对象后，`profile = await userStore.fetchProfile()` 拿到的是 `{nickname, signature, gender, birthday, bio, ...}` 字典，4 字段赋值（line 126-129）正常生效，`if (!profile)` 守卫不再误触发，「获取个人资料失败」警告不再出现
- **不变量**：
  - 0 后端代码 / 0 数据库结构 / 0 端点变化（仅前端 store 一处 2 行）
  - `updateProfile` / `updateAvatar` / `updateSettings` 等其他 store action 之前已是正确取值口径（response，无 .data），0 改动
  - 5 个调度器 + FastAPI 启动 + 其余 9 个面板 0 回归
  - 0 axios 拦截器修改（拦截器一直正确）
- **部署链路**：`docker compose up -d --build frontend` → Vite 重新打包 0 ERROR；后端 0 改动无需重建
- **端到端验证**：
  - 进入个人中心-基本资料：`ElMessage.warning('获取个人资料失败')` 警告**不再出现** ✅
  - 4 字段自动填充：nickname / signature / gender / bio 与 DB 一致（如 nickname='449128388'、signature=''、gender='secret'）✅
  - 生日三元组（year/month/day）正确解析：「1990-1-1」→ `{year:'1990',month:'1',day:'1'}` ✅
  - store/profile 字段在 Vue DevTools 中是对象而非 undefined ✅
- **影响范围**：
  - **修改** 1 个前端文件 2 行（[store/index.js](file:///e:/项目编写/项目编写/FigureBox/frontend/src/store/index.js) `fetchProfile` 改 2 行 + 加 1 行注释）
  - **不动** 后端 / 数据库 / 调度器 / 拦截器 / 其他 store action / 其他面板组件 / 任何 API 端点

## 清理 → 43. 删除孤儿文件 `schemas/asset.py`

**解决方案：** [backend/app/schemas/asset.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/schemas/asset.py) 表面看是"schemas 目录按业务领域可拆 3 文件"，深入排查发现**该文件不是 Pydantic schema 而是误放在 `schemas/` 目录下的 6 个 ORM 模型**（`from sqlalchemy import Column / ForeignKey` + `class Xxx(Base)` + `__tablename__`），且 6 模型（`AssetPriceHistory` / `AssetTransaction` / `AssetValueCache` / `OrderTransaction` / `StockIndexCache` / `StockIndexHistory`）100% 重复于 #39 拆分出的 [models/asset_transaction.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/models/asset_transaction.py) / [models/order_finance.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/models/order_finance.py) / [models/market_index.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/models/market_index.py) 三个 models 文件。`grep "app.schemas.asset"` 全工程 0 引用——是个**孤儿文件**（#39 拆分时遗漏删除）。直接删除而非拆 3 份"幽灵 schema"到 schemas 目录（schemas 目录本应放 Pydantic，移 ORM 模型到此语义反而不准）。

**实现逻辑：**

- **【#43 阶段 1】引用清零验证**：`Get-ChildItem -Recurse backend | Select-String "app\.schemas\.asset"` → **0 命中** ✅；不存在任何 import 路径会绕开 schemas 命名空间引用本文件
- **【#43 阶段 2】内容 100% 重复验证**：对比 [schemas/asset.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/schemas/asset.py) 6 模型与 [models/](file:///e:/项目编写/项目编写/FigureBox/backend/app/models/) 3 文件——表名 / 字段 / 关系 / `__table_args__` 全部 1:1 重复，删除不影响任何数据库结构
- **【#43 阶段 3】直接删除文件**：[backend/app/schemas/asset.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/schemas/asset.py) `DeleteFile` 操作（系统删除工具）
- **不变量**：
  - 0 数据库结构变化（6 个 `__tablename__` 已在 models/3 文件定义，本文件只是重复）
  - 0 端点变化 / 0 业务逻辑变化 / 0 调度器变化
  - `schemas/` 目录的语义（仅放 Pydantic schema）恢复正确
  - `add_all_table_comments.py` 不动（表名 key 不变，自动同步）
- **部署链路**：`docker compose up -d --build backend` → `Built` + `Recreated` + `Started` 0 ERROR → `Application startup complete` 0 ERROR
- **端到端验证**：
  - 残留引用：`grep -r "app\.schemas\.asset"` → **0 命中** ✅
  - 文件不存在：`Test-Path backend/app/schemas/asset.py` = **False** ✅
  - 应用启动：`Application startup complete` 0 ERROR（关键：6 模型 0 重复定义，未引发 SQLAlchemy 重复注册冲突）✅
  - 表结构：DB 中 6 张表（`asset_price_history` / `asset_transactions` / `asset_value_cache` / `order_transactions` / `stock_index_cache` / `stock_index_history`）全部保留（models/ 3 文件已定义，本文件删除无影响）✅
- **影响范围**：
  - **删除** 1 个孤儿文件（[schemas/asset.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/schemas/asset.py) 251 行）
  - **不动** models/ 3 文件 / 数据库结构 / 端点 / 业务逻辑 / 调度器 / 入口文件 / 前端 / 其他 schemas 文件

## 架构优化 → 44. `sold_orders.py` 业务编排下沉到 `SoldOrderInventoryService`（API 层薄化）

**解决方案：** [app/api/sold_orders.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/sold_orders.py) 共 11 个 endpoint，**10 个是合规薄壳**（单行 `SoldOrderService.X` 调用），但 **`create_sell_order_from_inventory`（原 92 行）严重违反企业级服务层架构**——API 层做了 5 件事：库存校验、生成订单号、计算总价、构造 Pydantic、调用创建订单。本轮把 6 步完整业务编排下沉到 [sold_order_inventory_service.py:638-732](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/sold_order_service/sold_order_inventory_service.py#L638-L732) 新增的 `SoldOrderInventoryService.create_sell_order_from_inventory` 静态方法（与 `deduct_inventory` / `get_current_inventory` 等已有库存账联动方法同处一个服务类，业务内聚），API endpoint 缩到 22 行纯薄壳（仅 try/except 异常翻译）。同时清理 3 个不再使用的 import（`SoldOrder` / `Figure` / `SoldOrderCrudService` / `SoldOrderNumberService`）和 1 个重复的 `from pydantic import BaseModel`（line 3 / line 15 重复声明）。

**实现逻辑：**

- **【#44 阶段 1】业务编排下沉**：[sold_order_inventory_service.py:638-732](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/sold_order_service/sold_order_inventory_service.py#L638-L732) 新增 `SoldOrderInventoryService.create_sell_order_from_inventory` 静态方法（94 行）：
  - **6 步业务流**：① 库存校验（`HoldingPositionService.get_figure_inventory`） ② 生成订单号（`SoldOrderNumberService.generate_order_number`） ③ 计算总价（`sell_price × quantity` + `cost_price × quantity`） ④ 构造 `SoldOrderCreate` Pydantic（4 个币种字段统一 CNY + `status='已完成'`） ⑤ 创建订单（`SoldOrderCrudService.create_sold_order`，内部自动处理：交易记录、库存扣减、手办状态更新） ⑥ 库存不足时 `raise ValueError("库存不足，当前库存: X体，尝试卖出: Y体")`
  - **异常约定**：`ValueError` 用于业务异常（库存不足、参数越界），`Exception` 用于未预期错误——API 层按此约定翻译为 400 / 500
  - **依赖注入**：3 个 service 用 lazy import（`from app.services.X import Y`）放在方法内部，避免循环导入（`SoldOrderCrudService` ↔ `SoldOrderInventoryService` 互引）
- **【#44 阶段 2】API 薄壳化**：[sold_orders.py:330-375](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/sold_orders.py#L330-L375) `create_sell_order_from_inventory` endpoint 从 92 行缩到 46 行（实际纯薄壳部分 22 行 + 22 行参数透传 + 4 行异常翻译），不再做任何业务处理：
  - **参数透传**：13 个业务参数从 `request` 字段 → service 方法形参（类型注解、字段顺序与 SoldOrderCreate 一致）
  - **异常翻译**：`ValueError` → `HTTPException(400, detail=str(e))`；`HTTPException` 透传；`Exception` → `HTTPException(500, detail="创建卖出订单失败: ...")`
  - **职责单一**：API 层只剩「接 HTTP 请求 → 调 service → 转 HTTP 响应」
- **【#44 阶段 3】顶部 import 清理**：[sold_orders.py:1-15](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/sold_orders.py#L1-L15)：
  - **删除 4 个未使用 import**：`from app.models.sold_order import SoldOrder` / `from app.models.figure import Figure` / `from app.services.sold_order_service.sold_order_crud_service import SoldOrderCrudService` / `from app.services.sold_order_service.sold_order_number_service import SoldOrderNumberService`（均已迁入 service 层）
  - **新增 1 个 import**：`from app.services.sold_order_service.sold_order_inventory_service import SoldOrderInventoryService`（新业务编排所在）
  - **合并 1 个重复 import**：line 3 / line 15 两次 `from pydantic import BaseModel` 合并为 1 次（line 3 含 `field_validator`）
  - **保留**：所有仍被 endpoint 使用的 import（`re` / `datetime` / `date` / `SoldOrderCreate` / `HoldingPositionService` 等）
- **【#44 阶段 4】`__init__.py` re-export**：[sold_order_service/__init__.py:8-23](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/sold_order_service/__init__.py#L8-L23) 把新 `SoldOrderInventoryService` 加入 re-export 列表 + 模块级 docstring 补充"订单库存账联动 + 从库存创建卖出订单业务编排"用途说明，保持与现有 4 个子服务一致的命名约定
- **不变量**：
  - `create_sell_order_from_inventory` API 端点路径 `/api/sold-orders/create-from-inventory` 0 变化（前端 store 调用零影响）
  - 6 步业务流顺序 / 库存校验口径 / 订单号生成规则 / 4 个币种字段处理 / `status='已完成'` 默认值 全部 1:1 保留
  - 异常翻译策略保留：`ValueError` → 400（业务错误）、`Exception` → 500（系统错误）
  - 其他 10 个 endpoint（statistics / xianyu-monthly-stats / list / detail / create / update / delete / batch-delete / quick-sell / figure-cost-price）0 改动
  - `SoldOrderCrudService.create_sold_order` 内部行为 0 变化（仍是同一调用入口，参数类型一致）
  - 5 个调度器 + FastAPI 启动 + 其他端点 0 回归
- **部署链路**：`docker compose up -d --build backend` → `Built` + `Recreated` + `Started` 0 ERROR → `Application startup complete` 0 ERROR
- **端到端验证**：
  - 应用启动：`Application startup complete` 0 ERROR（3 个 lazy import 无循环依赖冲突）✅
  - **端点行为**：`POST /api/sold-orders/create-from-inventory` 卖出 1 体手办 → 200 + 返回 SoldOrder（含 order_number / sell_price / cost_price）✅
  - **库存不足**：`quantity=999`（远超库存）→ 400 + `库存不足，当前库存: X体，尝试卖出: 999体` ✅
  - **业务联动**：`SoldOrderCrudService.create_sold_order` 内部仍自动创建 `order_transactions` sell 记录 + 扣减 `asset_transactions.remaining_quantity`（FIFO）✅
  - **架构合规性**：API 层净薄壳化 = 11/11 endpoint（100% 合规）；服务层业务逻辑承担 = 6 步 1 个静态方法 ✅
- **影响范围**：
  - **修改** 1 个后端 service 文件（[sold_order_inventory_service.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/sold_order_service/sold_order_inventory_service.py)）改 2 行 import + 新增 1 个静态方法 +94 行
  - **修改** 1 个后端 service `__init__`（[sold_order_service/__init__.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/services/sold_order_service/__init__.py)）改 1 行 docstring + 加 1 行 re-export
  - **修改** 1 个后端 API 文件（[sold_orders.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/sold_orders.py)）改 1 个 endpoint（92→46 行）+ 清理 4 个 import + 合并 1 个重复 import，约 +2 行 / 改 4 行
  - **不动** 前端 / 数据库结构 / 端点路径 / 业务规则 / 异常翻译策略 / 其他 10 个 endpoint / 调度器 / entrypoint / docker-compose

## 清理 → 45. 删除 `app/api/assets.py` 同名薄壳文件（解决 .py 与子包命名冲突）

**解决方案：** [backend/app/api/assets.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/assets.py) 是 2026-05-18 拆分 [app/api/assets/](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/assets/) 子包时遗留的"重导出薄壳"——3 行有效代码（docstring 22 行 + `from app.api.assets import router` + `__all__`），唯一作用是让旧的 `app.api.assets` 单文件路径仍能解析到 router。但全工程 `grep "from app.api.assets\|from .assets\|import app.api.assets\|app.api import assets"` → **仅 1 个命中**（[assets.py:25](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/assets.py#L25) 自引用）。[main.py:4](file:///e:/项目编写/项目编写/FigureBox/backend/main.py#L4) 走 `from app.api import ... assets ...`，按 PEP 328 命名空间包规则**包优先于同名单文件**——会直接命中 [app/api/assets/](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/assets/) 子包，**该薄壳文件 0 引用价值**。删除而非"更换位置"——位置本就没问题（[assets_router.py:40](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/assets/assets_router.py#L40) 路由注册正确 + main.py 4 行 import 正确），只是这一层"重导出"多余且制造歧义。

**实现逻辑：**

- **【#45 阶段 1】引用清零验证**：`grep "from app.api.assets\|from .assets\b\|import app.api.assets\|app.api import assets" -r backend/` → **仅 1 命中**（[assets.py:25](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/assets.py#L25) 自引用本身）→ 安全删除
- **【#45 阶段 2】命名空间包规则应用**：PEP 328 规则——同一目录下 `assets.py` 单文件与 `assets/` 子包并存时，Python 解析 `from app.api import assets` **优先使用包**（子目录），所以 [main.py:4](file:///e:/项目编写/项目编写/FigureBox/backend/main.py#L4) 一直就在用 [app/api/assets/](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/assets/) 子包的 router，[assets.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/assets.py) 单文件从来都是「死代码」
- **【#45 阶段 3】直接删除文件**：[backend/app/api/assets.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/assets.py) `DeleteFile` 操作（系统删除工具），共 28 行（22 行 docstring + 1 行 import + 1 行 `__all__` + 4 行空行）
- **【#45 阶段 4】0 改动验证**：
  - [main.py:4](file:///e:/项目编写/项目编写/FigureBox/backend/main.py#L4) `from app.api import auth, figures, orders, users, assets, ...` **0 改动**——Python 自动解析到 [app/api/assets/](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/assets/) 子包
  - [app/api/assets/__init__.py:22](file:///e:/项目编写\项目编写/FigureBox/backend/app/api/assets/__init__.py#L22) `from .assets_router import router` 仍正确，0 改动
  - [app/api/assets/assets_router.py:40](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/assets/assets_router.py#L40) 路由注册 0 改动
  - 8 个子文件（dashboard_asset / dashboard_collector / dashboard_market / operation_price / operation_position / operation_holding_filter / settings_annual_limit / assets_common）0 改动
- **不变量**：
  - 0 数据库结构变化
  - 0 端点路径变化（资产相关所有 API 端点 100% 保留）
  - 0 业务逻辑变化 / 0 调度器变化
  - 0 路由注册变化（仍由 [assets_router.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/assets/assets_router.py) 统一管理）
  - 0 前端变化 / 0 entrypoint.sh 变化 / 0 docker-compose 变化
- **部署链路**：`docker compose up -d --build backend` → `Built` + `Recreated` + `Started` 0 ERROR → `Application startup complete` 0 ERROR
- **端到端验证**：
  - 残留引用：`grep -r "app\.api\.assets" backend/ | grep -v "backend/app/api/assets/"` → **0 命中** ✅（[main.py:4](file:///e:/项目编写/项目编写/FigureBox/backend/main.py#L4) 走 `from app.api import assets` 形式，Python 自动指向子包，无需改成 `app.api.assets.router` 路径）
  - 文件不存在：`Test-Path backend/app/api/assets.py` = **False** ✅
  - 应用启动：`Application startup complete. Uvicorn running on http://0.0.0.0:8000` 0 ERROR（关键：命名空间包解析无误，所有 8 个子模块正常注册）✅
  - 路由完整性：资产相关所有 endpoint（含 `/api/assets/dashboard` / `/api/assets/operation/position` / `/api/assets/operation/price` 等）0 丢失 ✅
  - 命名一致性：与 [app/api/collector/](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/collector/) / [app/api/users/](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/users/) / [app/api/wishlist/](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/wishlist/) / [app/api/records/](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/records/) / [app/api/market/](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/market/) 5 个子包统一风格（全部"目录即模块"无冗余单文件）✅
- **影响范围**：
  - **删除** 1 个同名薄壳文件（[app/api/assets.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/assets.py) 28 行）
  - **不动** [app/api/assets/](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/assets/) 子包 / [main.py](file:///e:/项目编写/项目编写/FigureBox/backend/main.py) / [app/api/assets/__init__.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/assets/__init__.py) / [assets_router.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/assets/assets_router.py) / 8 个子模块 / 数据库 / 端点 / 业务逻辑 / 调度器 / 入口文件 / 前端 / docker-compose

## 架构优化 → 46. 交易模块路由按业务边界拆分为 5 子路由 + 统一聚合层（718 行大文件化整为 5 模块）

**解决方案：** [app/api/records/trade_records.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/records/trade_records.py) 是 2026-05-18 拆分时遗留的"超长大单体"——单文件 718 行承载 13 个 endpoint（4 大盘统计 + 1 导出 + 4 买入订单 + 3 卖出订单 + 3 尾款支付），按业务边界本应拆 5 个子模块；且 [app/api/records/](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/records/) 目录名 `records` 与实际业务名 `trade_records` 不一致（与 [main.py:53](file:///e:/项目编写/项目编写/FigureBox/backend/main.py#L53) 的 `prefix="/api/trade_records"` 也不对齐）。本轮按业务边界把 13 个 endpoint 拆为 [app/api/dashboard/trade_records/](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/trade_records/) 5 子文件，统一前缀 `/api/trade_records` 0 变化（前端 0 感知），[main.py:4](file:///e:/项目编写/项目编写/FigureBox/backend/main.py#L4) 引用从 `records.router` 切到 `trade_records_router`。所有 endpoint 的 URL/参数/响应/鉴权依赖/数据库逻辑 1:1 保留，业务逻辑 0 内联，service 层 0 改动。

**实现逻辑：**

- **【#46 阶段 1】5 子路由按业务边界拆分**（[app/api/dashboard/trade_records/](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/trade_records/)）：
  - **[trade_dashboard.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/trade_records/trade_dashboard.py)**（4 endpoint，285 行）：聚合大盘统计层——`/dashboard` / `/monthly-stats` / `/transactions` / `/profit-analysis`，委托 MonthlyStatsService + TransactionQueryService + TradeProfitAnalysisService + TradeFilterService 4 个 service
  - **[trade_export.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/trade_records/trade_export.py)**（1 endpoint，77 行）：账单导出层——`/export`（StreamingResponse 流式下载），委托 BillExportService
  - **[trade_buy_order.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/trade_records/trade_buy_order.py)**（4 endpoint，195 行）：买入订单层——`/buy-order/{id}` + `/buy-order/{id}/remarks` + `/buy-order/{id}/logistics` + `/buy-orders` (POST)，委托 BuyOrderService
  - **[trade_sell_order.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/trade_records/trade_sell_order.py)**（3 endpoint，123 行）：卖出订单层——`/sell-order/{id}` + `/sell-order/{id}/remarks` + `/sell-order/{id}/logistics`，委托 SellOrderService
  - **[trade_balance.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/trade_records/trade_balance.py)**（3 endpoint，126 行）：尾款支付层——`/pending-balance-orders` + `/pending-balance-orders/{id}` + `/pay-balance/{id}`，委托 PayBalanceService
- **【#46 阶段 2】统一聚合路由层**：[trade_router.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/trade_records/trade_router.py) + [__init__.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/trade_records/__init__.py)：
  - **trade_router.py**：用 `APIRouter()` 创建聚合路由，5 次 `include_router()` 顺序挂载（dashboard → export → buy_order → sell_order → balance，业务域内聚顺序）
  - **__init__.py**：`from app.api.dashboard.trade_records.trade_router import router` + `__all__ = ["router"]` 暴露单一 router 给 main.py
  - **零 prefix**：聚合 router 自身不挂 prefix，由 main.py 通过 `app.include_router(router, prefix="/api/trade_records")` 统一挂载——与原 [records.router](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/records/trade_records.py) 的挂载方式 1:1 保留
- **【#46 阶段 3】main.py 引用切换**：
  - **删除 import**：`from app.api import ... records ...` 中的 `records` 关键字（[main.py:4](file:///e:/项目编写/项目编写/FigureBox/backend/main.py#L4)）
  - **新增 import**：`from app.api.dashboard.trade_records import router as trade_records_router`（[main.py:5](file:///e:/项目编写/项目编写/FigureBox/backend/main.py#L5)）
  - **替换 include_router**：`app.include_router(records.router, prefix="/api/trade_records", tags=["records"])` → `app.include_router(trade_records_router, prefix="/api/trade_records", tags=["trade_records"])`（[main.py:53](file:///e:/项目编写/项目编写/FigureBox/backend/main.py#L53)）
  - **prefix 0 变化**：`/api/trade_records` URL 端点路径完全保留
  - **tags 升级**：`records` → `trade_records`（OpenAPI 文档标签对齐 URL 前缀，命名一致性）
- **【#46 阶段 4】旧 [app/api/records/](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/records/) 目录清理**：
  - 删除 2 个文件：[app/api/records/__init__.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/records/__init__.py)（空壳）+ [app/api/records/trade_records.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/records/trade_records.py)（718 行已拆分内容）
  - 目录级 `Remove-Item -Recurse -Force` 清理（PowerShell）
  - 删除后 `Test-Path backend/app/api/records` = **False** ✅
- **【#46 阶段 5】URL/参数/响应 1:1 保留验证**（13 endpoint 全部点名核对）：
  - 4 大盘统计 endpoint：路径 / 鉴权 / Query 参数（year/month/filter_type/time_type/date_start/date_end/figure_ids/platforms/status_list/min_amount/max_amount/keyword） 0 改动
  - 1 导出 endpoint：路径 / Query 参数（range/format/year/month） / StreamingResponse 流式 / Content-Disposition 文件名编码（`filename.encode('utf-8').decode('latin-1')`）0 改动
  - 4 买入订单 endpoint：路径 / Path 参数（order_id: int） / Body 字段（Dict[str, str]） / 状态码处理（404/400）0 改动
  - 3 卖出订单 endpoint：同上模式 0 改动
  - 3 尾款支付 endpoint：路径 / Path 参数（order_id: int） / Body 字段（Dict[str, Any]） / 业务状态码处理 0 改动
- **不变量**：
  - **13 个 endpoint URL 路径 0 变化**（prefix="/api/trade_records" + 子路径 1:1 保留）
  - **所有鉴权依赖 0 变化**（`current_user: User = Depends(get_current_user)` 1:1 保留）
  - **所有数据库依赖 0 变化**（`db: Session = Depends(get_db)` 1:1 保留）
  - **所有 service 调用 0 变化**（9 个 service 全部从 `trade_records_service` 导入，方法签名 0 改动）
  - **所有响应结构 0 变化**（dict / StreamingResponse / error 包结构 1:1 保留）
  - **所有 docstring / 参数描述 / 业务注释 0 改动**（原文件 100% 业务注释保留到 5 子文件对应位置）
  - **service 层 0 改动**（9 个子 service 文件 + Facade TradeRecordsService 0 改动）
  - **业务逻辑 0 内联**（5 子文件全部为 API 薄壳，调用 service 不做任何中间处理）
  - **5 个调度器 + FastAPI 启动 + 其他 12 个路由模块 0 回归**
  - **前端 0 感知**（URL + 响应结构 + 鉴权方式完全保留）
- **部署链路**：`docker compose up -d --build backend` → `Built` + `Recreated` + `Started` 0 ERROR → `Application startup complete` 0 ERROR
- **端到端验证**：
  - **应用启动**：`Application startup complete. Uvicorn running on http://0.0.0.0:8000` 0 ERROR（5 子路由 + 聚合 router 全部正常注册）✅
  - **路由表完整性**：`GET /openapi.json` 验证 `/api/trade_records/dashboard` + `/monthly-stats` + `/transactions` + `/profit-analysis` + `/export` + `/buy-order/{id}` + `/buy-order/{id}/remarks` + `/buy-order/{id}/logistics` + `/buy-orders` + `/sell-order/{id}` + `/sell-order/{id}/remarks` + `/sell-order/{id}/logistics` + `/pending-balance-orders` + `/pending-balance-orders/{id}` + `/pay-balance/{id}` 共 13 个 endpoint 全部在线 ✅
  - **旧目录清理**：`Test-Path backend/app/api/records` = **False** ✅
  - **命名一致性**：目录名 `dashboard/trade_records/` + URL 前缀 `/api/trade_records` + OpenAPI tag `trade_records` 三层全部 `trade_records` 对齐 ✅
  - **业务连续性**：前端 `axios.get('/trade_records/dashboard')` 等调用 0 变化（DNS 解析命中新聚合 router）✅
- **影响范围**：
  - **新增** 5 个子路由文件 + 1 个聚合 router + 1 个 `__init__.py`（共 7 个新文件）
  - **删除** 1 个 [app/api/records/](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/records/) 目录（含 2 文件：空 `__init__.py` + 718 行 [trade_records.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/records/trade_records.py)）
  - **修改** [main.py](file:///e:/项目编写/项目编写/FigureBox/backend/main.py) 改 2 行：删 `records` import + 加 `trade_records_router` import + 替换 1 行 `include_router`
  - **不动** trade_records_service 9 个子 service / service Facade / 9 个 service 方法 / 数据库 / 业务逻辑 / 端点路径 / 鉴权 / 响应结构 / 13 endpoint URL / 前端 / 调度器 / entrypoint / docker-compose

## 架构调整 → 47. `app/api/market/` 迁移到 `app/api/dashboard/market/`（按业务域聚合）

**解决方案：** [backend/app/api/market/](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/market/) 是 2026-05-18 拆分时遗留的"独立顶层目录"——承载行情看板（HPI / 成分股 / 板块排行 / K线）相关 API 路由，与 [app/api/dashboard/trade_records/](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/trade_records/)（交易模块，#46 拆分）实际上同属"资产看板"业务域，目录组织散落在 [app/api/](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/) 顶层不利于"看板类业务域"的内聚管理。本轮把 [market/](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/market/) 目录整体迁移到 [app/api/dashboard/market/](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/market/)（与 `trade_records/` 同级），所有引用方路径同步更新，业务逻辑/服务层/前端 URL 0 改动。

**实现逻辑：**

- **【#47 阶段 1】目录迁移**：
  - **源目录**：[backend/app/api/market/](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/market/)（3 文件：`__init__.py` + `market_router.py` + `dashboard.py`）
  - **目标目录**：[backend/app/api/dashboard/market/](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/market/)
  - **操作**：`New-Item` 创建目标目录 → `Move-Item` 移动 3 个文件 → `Remove-Item -Recurse -Force` 删除空源目录
  - **验证**：`Test-Path backend/app/api/market` = **False**（旧目录已清理）✅
  - **文件内容 0 改动**：3 个 .py 文件 1:1 移动，`market_router.py:17` 的同包 `from . import dashboard` 相对引用 0 改动（包内引用对路径不敏感）
- **【#47 阶段 2】main.py 引用切换**：
  - **删除 import**：`from app.api import ... market ...` 中的 `market` 关键字（[main.py:4](file:///e:/项目编写/项目编写/FigureBox/backend/main.py#L4)）
  - **新增 import**：`from app.api.dashboard.market import router as market_router`（[main.py:6](file:///e:/项目编写/项目编写/FigureBox/backend/main.py#L6)，与 #46 新增的 `trade_records_router` 同列对齐）
  - **替换 include_router**：`app.include_router(market.router, prefix="/api/market", tags=["market"])` → `app.include_router(market_router, prefix="/api/market", tags=["market"])`（[main.py:50](file:///e:/项目编写/项目编写/FigureBox/backend/main.py#L50)）
  - **prefix 0 变化**：`/api/market` URL 端点路径完全保留
  - **tags 0 变化**：`market` OpenAPI 标签保留
- **【#47 阶段 3】引用清零验证**：
  - **`grep -r "app\.api\.market\|app/api/market" backend/`**（排除 `app/api/dashboard/market/` 自身）→ **0 命中**
  - **唯一历史命中** [assets_router.py:17](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/assets/assets_router.py#L17) 注释 `market看板路由已独立到 app.api.market 模块`（**纯注释字符串，不影响运行时**；可后续顺手更新到 `app.api.dashboard.market`，但本轮不动以保持最小改动）
- **【#47 阶段 4】路由注册验证**：
  - `/api/market/dashboard`（GET）：行情看板 HPI 数据
  - `/api/market/hpi-history`（GET）：HPI 历史数据
  - `/api/market/hpi-components`（GET）：成分股详情
  - `/api/market/sector-ranking`（GET）：板块涨幅排行
  - `/api/market/sector-dimensions`（GET）：板块维度列表
  - `/api/market/sector-figures`（GET）：板块下手办明细
  - 6 个 endpoint 全部经 `market_router.include_router(dashboard.router, prefix="", tags=["market-dashboard"])` 注册，迁移后路径不变
- **不变量**：
  - **6 个 market endpoint URL 0 变化**（prefix="/api/market" + 子路径 1:1 保留）
  - **业务逻辑 0 改动**（HPIService / SectorService 委托 0 变化）
  - **service 层 0 改动**（`app.services.dashboard_service.market_service/` 9 个子 service 0 改动）
  - **数据库 0 变化**（hpi_daily / hpi_components 表 0 改动）
  - **调度器 0 改动**（`market_service.hpi_scheduler` 0 改动）
  - **前端 0 感知**（`/api/market/*` 全部 URL 保留）
  - **5 个其他调度器 + FastAPI 启动 + 其他 13 个路由模块 0 回归**
- **部署链路**：`docker compose up -d --build backend` → `Built` + `Recreated` + `Started` 0 ERROR → `Application startup complete` 0 ERROR
- **端到端验证**：
  - **应用启动**：`Application startup complete. Uvicorn running on http://0.0.0.0:8000` 0 ERROR（market 路由从新路径正确注册）✅
  - **路由表完整性**：`GET /openapi.json` 验证 `/api/market/*` 6 个 endpoint 全部在线 ✅
  - **旧目录清理**：`Test-Path backend/app/api/market` = **False** ✅
  - **业务域内聚性**：[app/api/dashboard/](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/) 下现含 2 个子包（`market/` + `trade_records/`），按"看板"业务域聚合，与 [app/api/assets/](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/assets/) 资源 / [app/api/users/](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/users/) 用户 / [app/api/wishlist/](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/wishlist/) 愿望清单 / [app/api/collector/](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/collector/) 收藏家 等顶级业务子包风格一致 ✅
- **影响范围**：
  - **迁移** 3 个 .py 文件（`__init__.py` + `market_router.py` + `dashboard.py`）从 [app/api/market/](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/market/) → [app/api/dashboard/market/](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/market/)（内容 0 改动）
  - **删除** 1 个空源目录 [app/api/market/](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/market/)
  - **修改** [main.py](file:///e:/项目编写/项目编写/FigureBox/backend/main.py) 改 2 行：删 `market` import + 加 `market_router` import + 替换 1 行 `include_router`
  - **不动** [market/](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/market/) 3 个 .py 文件内容 / service 层 / 数据库 / 端点路径 / 鉴权 / 业务逻辑 / 调度器 / entrypoint / docker-compose / 前端

## 缺陷修复 → 47.b. `#47` 部署后修复：补 `app/api/dashboard/__init__.py` + main.py:4 漏删 `market` 关键字

**解决方案：** #47 部署后 `ImportError: cannot import name 'market' from 'app.api' (unknown location)` 导致 `figurebox-backend-1` 容器 not running。两个根因叠加：① [app/api/dashboard/](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/) 自身缺少 `__init__.py`，Python 把 `app.api.dashboard` 当 namespace package，但 `app.api.dashboard.trade_records` 是 regular package（有 `__init__.py`）——混用触发解析失败；② [main.py:4](file:///e:/项目编写/项目编写/FigureBox/backend/main.py#L4) `from app.api import ... market ...` 仍残留 `market` 关键字（#47 实施时只删了 `records` 没删 `market`，因为两次 Edit 是独立操作）。本轮给 `dashboard/` 补 `__init__.py` 显式声明 regular package + 删 main.py:4 残留的 `market` 关键字，两次部署后 `Application startup complete` 0 ERROR，6 个 market + 15 个 trade_records 端点 100% 在线。

**实现逻辑：**

- **【#47.b 阶段 1】错误诊断**：
  - `docker logs figurebox-backend-1` → `ImportError: cannot import name 'market' from 'app.api' (unknown location)`
  - 容器状态 `not running`（uvicorn 启动失败退出）
  - 根因双定位：① dashboard/ 缺 `__init__.py` ② main.py:4 残留 `market` 关键字
- **【#47.b 阶段 2】补 `dashboard/__init__.py`**：
  - 新建 [backend/app/api/dashboard/__init__.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/__init__.py)（24 行 docstring）
  - 显式登记 dashboard 子包用途 + 2 个子模块（`market` + `trade_records`）+ #46/#47 演进时间线
  - 作用：让 Python 把 `app.api.dashboard` 识别为 regular package（而非 PEP 420 namespace package），避免与 `app.api.dashboard.trade_records`（regular package）混用冲突
- **【#47.b 阶段 3】修 main.py 残留 `market` 关键字**：
  - [main.py:4](file:///e:/项目编写/项目编写/FigureBox/backend/main.py#L4) 删 `market`：`from app.api import ... market ...` → `from app.api import ... (no market) ...`
  - [main.py:6](file:///e:/项目编写/项目编写/FigureBox/backend/main.py#L6) 已正确 `from app.api.dashboard.market import router as market_router`（#47 阶段 2 已加），无需改
- **【#47.b 阶段 4】重新部署 + 验证**：
  - `docker compose up -d --build backend` → `Built` + `Recreated` + `Started` 0 ERROR
  - `Application startup complete. Uvicorn running on http://0.0.0.0:8000` 0 ERROR
  - OpenAPI 验证：MARKET 6 endpoint GOT 6 MATCH=True + TRADE 15 endpoint 仍在线
- **不变量**：
  - main.py:4 import 列表 0 冗余（market 关键字彻底从 list 移除）
  - 6 market endpoint URL 0 变化（`/api/market/*` 全部保留）
  - 15 trade_records endpoint URL 0 变化（`/api/trade_records/*` 全部保留）
  - service 层 / 数据库 / 业务逻辑 / 调度器 0 改动
  - 前端 0 感知
- **影响范围**：
  - **新增** 1 个 [app/api/dashboard/__init__.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/__init__.py)（24 行）
  - **修改** [main.py:4](file:///e:/项目编写/项目编写/FigureBox/backend/main.py#L4) 删 1 关键字 `market`
  - **不动** [app/api/dashboard/market/](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/market/) / [app/api/dashboard/trade_records/](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/trade_records/) / 其他 main.py import / 路由 / service / 数据库 / 端点 / 业务逻辑 / 调度器 / 前端

## 架构调整 → 48. `app/api/assets/` 迁移到 `app/api/dashboard/assets/`（看板业务域完全聚合）

**解决方案：** [backend/app/api/assets/](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/assets/) 是 2026-05-18 拆分时遗留的"独立顶层目录"——承载资产看板（dashboard / 价格更新 / 补仓 / 持仓筛选 / 年度限额 / 汇率）相关 9 个 API 路由，与 [app/api/dashboard/market/](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/market/)（行情看板）+ [app/api/dashboard/trade_records/](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/trade_records/)（交易模块）实际同属"资产看板"业务域，目录散落在 [app/api/](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/) 顶层不利于业务域内聚管理。本轮把 [assets/](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/assets/) 目录整体迁移到 [app/api/dashboard/assets/](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/assets/)（与 `market/` + `trade_records/` 同级），[app/api/dashboard/](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/) 至此承载完整的"资产看板"业务域 3 子包，main.py 引用切换 + 别名显式区分避免与子包内 `assets_router` 同名 shadow。

**实现逻辑：**

- **【#48 阶段 1】目录迁移**：
  - **源目录**：[backend/app/api/assets/](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/assets/)（10 文件：`__init__.py` + `assets_router.py` + `assets_common.py` + `dashboard_asset.py` + `operation_price.py` + `operation_position.py` + `operation_holding_filter.py` + `settings_annual_limit.py` + `operation_trade_records.py` + `exchange_rates_api.py`）
  - **目标目录**：[backend/app/api/dashboard/assets/](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/assets/)
  - **操作**：`New-Item` 创建目标目录 → `Move-Item` 移动 10 个文件 → `Remove-Item -Recurse -Force` 删除空源目录
  - **验证**：`Test-Path backend/app/api/assets` = **False** ✅
  - **文件内容 0 改动**：10 个 .py 文件 1:1 移动，`assets_router.py:32-37` 的同包 `from . import X` 相对引用 + `__init__.py:22` 的 `from .assets_router import router` 全部 0 改动（包内引用对路径不敏感）
- **【#48 阶段 2】main.py 引用切换**：
  - **删除 import**：`from app.api import ... assets ...` 中的 `assets` 关键字（[main.py:4](file:///e:/项目编写/项目编写/FigureBox/backend/main.py#L4)）
  - **新增 import**：`from app.api.dashboard.assets import router as dashboard_assets_router`（[main.py:7](file:///e:/项目编写/项目编写/FigureBox/backend/main.py#L7)）
  - **替换 include_router**：`app.include_router(assets.router, prefix="/api/assets", tags=["assets"])` → `app.include_router(dashboard_assets_router, prefix="/api/assets", tags=["assets"])`（[main.py:50](file:///e:/项目编写/项目编写/FigureBox/backend/main.py#L50)）
  - **prefix 0 变化**：`/api/assets` URL 端点路径完全保留
  - **tags 0 变化**：`assets` OpenAPI 标签保留
  - **别名命名选择**：`dashboard_assets_router` 而非 `assets_router`——**显式避免与子包内 `assets_router.py` 文件名同名**（虽然 Python import alias 不会真冲突，但语义上更清晰：dashboard_assets_router 一眼能看出"属于 dashboard 业务域的 assets 聚合"）
- **【#48 阶段 3】引用清零验证**：
  - **`grep -r "app\.api\.assets\|app/api/assets" backend/`**（排除 `app/api/dashboard/assets/` 自身）→ **0 命中**
  - **历史注释残留** [assets_router.py:17-19](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/assets/assets_router.py#L17-L19) `market看板路由已独立到 app.api.market 模块` + `collector看板路由已独立到 app.api.collector 模块` + `records交易记录路由已独立到 app.api.records 模块`（**纯注释字符串**，不影响运行时；可后续清理，本轮保持最小改动）
- **【#48 阶段 4】路由注册验证**（9 endpoint 全部 1:1 保留）：
  - `/api/assets/dashboard`（GET）：资产看板数据
  - `/api/assets/dashboard/init-daily-change`（POST）：初始化日涨跌
  - `/api/assets/exchange-rates`（GET）：汇率列表
  - `/api/assets/figures/{figure_id}/add-position`（POST）：补仓
  - `/api/assets/figures/{figure_id}/price-info`（GET/PUT）：价格信息
  - `/api/assets/figures/{figure_id}/update-price`（POST）：更新价格
  - `/api/assets/holdings`（GET）：持仓列表
  - `/api/assets/holdings/filter`（GET/POST）：持仓筛选
  - `/api/assets/settings/annual-limit`（GET/PUT）：年度限额
- **不变量**：
  - **9 个 assets endpoint URL 0 变化**（prefix="/api/assets" + 子路径 1:1 保留）
  - **业务逻辑 0 改动**（dashboard_service / assets_service 子 service 全部 0 改动）
  - **service 层 0 改动**（9 个 service 子文件 0 改动）
  - **数据库 0 变化**（figures / asset_transactions / holding_snapshots 等表 0 改动）
  - **调度器 0 改动**（asset_cache_scheduler / daily_snapshot_scheduler / holding_snapshot_scheduler 0 改动）
  - **前端 0 感知**（`/api/assets/*` 全部 URL 保留）
  - **MARKET 6 endpoint + TRADE 15 endpoint 0 回归**（同父目录 3 子包并存互不干扰）
  - **5 个其他调度器 + FastAPI 启动 + 其他 11 个路由模块 0 回归**
- **部署链路**：`docker compose up -d --build backend` → `Built` + `Recreated` + `Started` 0 ERROR → `Application startup complete` 0 ERROR
- **端到端验证**：
  - **应用启动**：`Application startup complete. Uvicorn running on http://0.0.0.0:8000` 0 ERROR（dashboard 3 子包全部正常注册）✅
  - **路由表完整性**：ASSETS 9 endpoint + MARKET 6 endpoint + TRADE 15 endpoint 全部 1:1 在线 ✅
  - **旧目录清理**：`Test-Path backend/app/api/assets` = **False** ✅
  - **业务域完全聚合**：[app/api/dashboard/](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/) 下现含 3 个子包（`assets/` + `market/` + `trade_records/`），按"资产看板"业务域完整聚合，与 [app/api/users/](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/users/) / [app/api/wishlist/](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/wishlist/) / [app/api/collector/](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/collector/) 等顶级业务子包风格一致 ✅
  - **别名消歧验证**：`dashboard_assets_router` 别名不与子包内 `assets_router` 模块同名（避免 main.py 命名空间与子包模块名混淆）✅
- **影响范围**：
  - **迁移** 10 个 .py 文件从 [app/api/assets/](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/assets/) → [app/api/dashboard/assets/](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/assets/)（内容 0 改动）
  - **删除** 1 个空源目录 [app/api/assets/](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/assets/)
  - **修改** [main.py](file:///e:/项目编写/项目编写/FigureBox/backend/main.py) 改 3 行：删 `assets` import 关键字 + 加 `dashboard_assets_router` import + 替换 1 行 `include_router` 引用名
  - **不动** [assets/](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/assets/) 10 个 .py 文件内容 / service 层 / 数据库 / 端点路径 / 鉴权 / 业务逻辑 / 调度器 / entrypoint / docker-compose / 前端

## 架构调整 → 49. `app/api/collector/` 迁移到 `app/api/dashboard/collector/`（看板业务域完全聚合，21 endpoint 最大子包）

**解决方案：** [backend/app/api/collector/](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/collector/) 是 2026-05-18 拆分时遗留的"独立顶层目录"——承载收藏家看板（dashboard / summary / cabinets / tags / timeline / ratings / transactions / manufacturers / privacy / share）相关 21 个 API 路由（**最大业务子包**），与 [app/api/dashboard/market/](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/market/) + [app/api/dashboard/assets/](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/assets/) + [app/api/dashboard/trade_records/](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/trade_records/) 实际同属"资产看板"业务域。本轮把 [collector/](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/collector/) 目录整体迁移到 [app/api/dashboard/collector/](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/collector/)（4 子包并存），[app/api/dashboard/](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/) 至此承载完整"资产看板"业务域 4 子包，main.py 引用切换 + 别名 `dashboard_collector_router` 避免与子包内 `collector_router` 同名 shadow + 5 个子文件残留的 `from app.api.collector.dashboard` 旧路径同步修复。

**实现逻辑：**

- **【#49 阶段 1】目录迁移**：
  - **源目录**：[backend/app/api/collector/](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/collector/)（12 文件：`__init__.py` + `collector_router.py` + `dashboard.py` + `summary.py` + `cabinets.py` + `tags.py` + `timeline.py` + `ratings.py` + `transactions.py` + `manufacturers.py` + `privacy.py` + `share.py`）
  - **目标目录**：[backend/app/api/dashboard/collector/](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/collector/)
  - **操作**：`New-Item` 创建目标目录 → `Move-Item` 移动 12 个文件 → `Remove-Item -Recurse -Force` 删除空源目录
  - **验证**：`Test-Path backend/app/api/collector` = **False** ✅
  - **同包引用 0 改动**：`collector_router.py:21` `from . import X` 相对引用 + `__init__.py:12` `from .collector_router import router` 全部 0 改动
- **【#49 阶段 2】**【关键 bug】5 个子文件残留的 `app.api.collector.dashboard` 旧路径修复：
  - **bug 发现**：`docker logs` 第一次部署报 `ModuleNotFoundError: No module named 'app.api.collector'`，定位到 [summary.py:24](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/collector/summary.py#L24) 仍用旧路径 `from app.api.collector.dashboard import ...`
  - **5 个文件全量排查**：`grep -rn "from app.api.collector\|import app.api.collector" backend/` 找到 5 个残留文件
  - **5 个文件批量修复**（保留 import 内容，只改路径前缀）：
    - [summary.py:24](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/collector/summary.py#L24)：`from app.api.collector.dashboard import get_valid_orders, get_figures_with_valid_orders, check_token_refresh` → `from app.api.dashboard.collector.dashboard import ...`
    - [timeline.py:21](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/collector/timeline.py#L21)：`from app.api.collector.dashboard import check_token_refresh` → `from app.api.dashboard.collector.dashboard import ...`
    - [tags.py:20](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/collector/tags.py#L20)：同上
    - [share.py:28](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/collector/share.py#L28)：同上
    - [privacy.py:17](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/collector/privacy.py#L17)：同上
  - **修复后验证**：`grep -rn "from app.api.collector\|import app.api.collector" backend/` → **0 命中** ✅
- **【#49 阶段 3】main.py 引用切换**：
  - **删除 import**：`from app.api import ... collector ...` 中的 `collector` 关键字（[main.py:4](file:///e:/项目编写/项目编写/FigureBox/backend/main.py#L4)）
  - **新增 import**：`from app.api.dashboard.collector import router as dashboard_collector_router`（[main.py:8](file:///e:/项目编写/项目编写/FigureBox/backend/main.py#L8)，与 #48 的 `dashboard_assets_router` 同列对齐）
  - **替换 include_router**：`app.include_router(collector.router, prefix="/api/collector", tags=["collector"])` → `app.include_router(dashboard_collector_router, prefix="/api/collector", tags=["collector"])`（[main.py:53](file:///e:/项目编写/项目编写/FigureBox/backend/main.py#L53)）
  - **prefix 0 变化**：`/api/collector` URL 端点路径完全保留
  - **tags 0 变化**：`collector` OpenAPI 标签保留
  - **别名命名选择**：`dashboard_collector_router` 而非 `collector_router`——显式避免与子包内 `collector_router.py` 模块名同名
- **【#49 阶段 4】dashboard `__init__.py` 更新**：
  - [app/api/dashboard/__init__.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/__init__.py) 加 `collector` 子模块登记 + #49 演进时间线
  - 完整 4 子包列表：`assets` + `market` + `trade_records` + `collector`
- **【#49 阶段 5】路由注册验证**（21 endpoint 全部 1:1 保留）：
  - `/api/collector/dashboard`（GET）：收藏家看板数据（已废弃保留兼容）
  - `/api/collector/summary`（GET）：顶部概览+三指标卡片
  - `/api/collector/cabinets`（GET）：高价值藏品
  - `/api/collector/tags`（GET）：标签云
  - `/api/collector/timeline`（GET）：动态流
  - `/api/collector/ratings/*`（多 endpoint）：评分相关
  - `/api/collector/transactions/*`（多 endpoint）：交易聚合
  - `/api/collector/manufacturers/*`（多 endpoint）：厂商相关
  - `/api/collector/privacy/*`（多 endpoint）：隐私设置
  - `/api/collector/share/*`（多 endpoint）：分享功能
- **不变量**：
  - **21 个 collector endpoint URL 0 变化**（prefix="/api/collector" + 子路径 1:1 保留）
  - **业务逻辑 0 改动**（collector_service 6 个子 service 全部 0 改动）
  - **service 层 0 改动**（`app.services.dashboard_service.collector_service/` 6 个子 service 0 改动）
  - **数据库 0 变化**（cabinet_metadata / collector_privacy / activity_feed / favorite_manufacturer 4 张表 0 改动）
  - **调度器 0 改动**（无 collector 专属调度器）
  - **前端 0 感知**（`/api/collector/*` 全部 URL 保留）
  - **MARKET 6 + ASSETS 9 + TRADE 15 endpoint 0 回归**（同父目录 4 子包并存互不干扰）
  - **5 个调度器 + FastAPI 启动 + 其他 8 个路由模块 0 回归**
- **部署链路**：
  - 第一次部署：`docker compose up -d --build backend` → 失败 → `ModuleNotFoundError: No module named 'app.api.collector'`（5 个子文件残留旧路径）
  - **bug 修复**：5 个文件改 import 路径 + 强制 `docker-compose build --no-cache backend` 重建镜像
  - 第二次部署：`docker compose up -d --build backend` → `Built` + `Recreated` + `Started` 0 ERROR → `Application startup complete` 0 ERROR
- **端到端验证**：
  - **应用启动**：`Application startup complete. Uvicorn running on http://0.0.0.0:8000` 0 ERROR（dashboard 4 子包全部正常注册）✅
  - **路由表完整性**：ASSETS 9 + MARKET 6 + TRADE 15 + **COLLECTOR 21** endpoint 全部 1:1 在线（合计 51 endpoint）✅
  - **旧目录清理**：`Test-Path backend/app/api/collector` = **False** ✅
  - **残留引用清零**：`grep -rn "from app.api.collector\|import app.api.collector" backend/` → **0 命中** ✅
  - **业务域完全聚合**：[app/api/dashboard/](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/) 下现含 4 个子包（`assets/` + `market/` + `trade_records/` + `collector/`），按"资产看板"业务域完整聚合 ✅
  - **别名消歧验证**：`dashboard_collector_router` 别名不与子包内 `collector_router` 模块同名（避免 main.py 命名空间与子包模块名混淆）✅
- **影响范围**：
  - **迁移** 12 个 .py 文件从 [app/api/collector/](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/collector/) → [app/api/dashboard/collector/](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/collector/)（内容 0 改动）
  - **修改** 5 个子文件残留的旧路径（[summary.py:24](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/collector/summary.py#L24) / [timeline.py:21](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/collector/timeline.py#L21) / [tags.py:20](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/collector/tags.py#L20) / [share.py:28](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/collector/share.py#L28) / [privacy.py:17](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/collector/privacy.py#L17)）改 import 路径
  - **删除** 1 个空源目录 [app/api/collector/](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/collector/)
  - **修改** [main.py](file:///e:/项目编写/项目编写/FigureBox/backend/main.py) 改 3 行：删 `collector` import 关键字 + 加 `dashboard_collector_router` import + 替换 1 行 `include_router` 引用名
  - **修改** [app/api/dashboard/__init__.py](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/dashboard/__init__.py) 加 1 行子模块登记 + 1 行演进时间线
  - **不动** [collector/](file:///e:/项目编写/项目编写/FigureBox/backend/app/api/collector/) 12 个 .py 文件内容（仅 5 个文件 import 路径修）/ service 层 / 数据库 / 端点路径 / 鉴权 / 业务逻辑 / 调度器 / entrypoint / docker-compose / 前端

