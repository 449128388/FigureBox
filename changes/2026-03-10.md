# 2026-03-10 更新日志

## 1. 修复"名称"字段校验问题

### 问题描述
- 在"名称"字段中输入包含中文字符的内容（如"我回复挂号费的"）时，前端错误提示"名称不能只包含特殊字符"
- 原因：emoji过滤的正则表达式范围设置错误，包含了中文字符的范围，导致中文字符被错误过滤

### 解决方案

#### 1. 代码修改
**文件**: `frontend/src/views/Figures.vue` (第396行)
- 修正 emoji 过滤的正则表达式：
  ```javascript
  // 原代码（错误）
  const emojiPattern = /[\u{1F600}-\u{1F64F}\u{1F300}-\u{1F5FF}\u{1F680}-\u{1F6FF}\u{1F1E0}-\u{1F1FF}\u{2702}-\u{27B0}\u{24C2}-\u{1F251}]/gu
  
  // 修正后（正确）
  const emojiPattern = /[\u{1F600}-\u{1F64F}\u{1F300}-\u{1F5FF}\u{1F680}-\u{1F6FF}\u{1F1E0}-\u{1F1FF}\u{2600}-\u{26FF}\u{2700}-\u{27BF}\u{1F900}-\u{1F9FF}]/gu
  ```

#### 2. 验证逻辑优化
- **原始问题**: 错误的正则表达式范围导致中文字符被当作emoji过滤，过滤后变成空字符串
- **修复后**: 使用标准的emoji字符范围，只过滤真正的emoji字符，保留中文字符
- **适用场景**: 适用于所有使用 `validateField` 方法验证的字段

### 验证规则
- **长度校验**: 1-100 位字符（对于"名称"字段）
- **字符校验**: 允许中文、英文、日文、数字、空格，常见符号
- **特殊处理**: 
  - 自动过滤 emoji 字符
  - 当输入的内容全部为 emoji 时，提示"不能只包含特殊字符"
  - 保持其他验证规则不变

### 技术实现
- 修正了 emoji 过滤的正则表达式，使用标准的 emoji 字符范围
- 确保中文字符不被错误过滤
- 保持了原有的验证逻辑和错误提示

### 影响范围
- 所有使用 `validateField` 方法进行验证的字段，包括：
  - 名称
  - 入手途径
  - 原型
  - 涂装
  - 原画
  - 作品
  - 制造商
  - 比例
  - 材质
  - 尺寸

### 优化效果
- 修复了中文字符被错误过滤的问题
- 确保了"我回复挂号费的"等中文内容能正确通过验证
- 保持了对emoji的正确过滤
- 提高了验证的准确性和用户体验

## 2. 修复后端"名称"字段校验问题

### 问题描述
- 使用包含中文字符的请求内容（如"该大佛"）时，后端返回错误："名称长度不能少于1个字符"
- 原因：后端emoji过滤的正则表达式范围设置错误，包含了中文字符的范围，导致中文字符被错误过滤

### 解决方案

#### 1. 代码修改
**文件**: `backend/app/schemas/figure.py` (第12行)
- 修正 emoji 过滤的正则表达式：
  ```python
  # 原代码（错误）
  EMOJI_PATTERN = re.compile(r'[\U0001F600-\U0001F64F\U0001F300-\U0001F5FF\U0001F680-\U0001F6FF\U0001F1E0-\U0001F1FF\U00002702-\U000027B0\U000024C2-\U0001F251]+')
  
  # 修正后（正确）
  EMOJI_PATTERN = re.compile(r'[\U0001F600-\U0001F64F\U0001F300-\U0001F5FF\U0001F680-\U0001F6FF\U0001F1E0-\U0001F1FF\U00002600-\U000026FF\U00002700-\U000027BF\U0001F900-\U0001F9FF]+')
  ```

#### 2. 验证逻辑优化
**文件**: `backend/app/schemas/figure.py` (第26-28行)
- 在 `validate_field` 函数中添加检查逻辑：
  ```python
  # 检查过滤后是否为空（如果原始值不为空但过滤后为空，说明输入的全是emoji）
  if len(value) == 0:
      raise ValueError(f'{field_name}不能只包含特殊字符')
  ```

### 验证规则
- **长度校验**: 1-100 位字符（对于"名称"字段）
- **字符校验**: 允许中文、英文、日文、数字、空格，常见符号
- **特殊处理**: 
  - 自动过滤 emoji 字符
  - 当输入的内容全部为 emoji 时，提示"不能只包含特殊字符"
  - 保持其他验证规则不变

### 技术实现
- 修正了 emoji 过滤的正则表达式，使用标准的 emoji 字符范围
- 确保中文字符不被错误过滤
- 添加了对全emoji输入的检测
- 保持了原有的验证逻辑和错误提示

### 影响范围
- 所有使用 `validate_field` 函数进行验证的字段，包括：
  - 名称
  - 入手途径
  - 原型
  - 涂装
  - 原画
  - 作品
  - 制造商
  - 比例
  - 材质
  - 尺寸

### 优化效果
- 修复了中文字符被错误过滤的问题
- 确保了"该大佛"等中文内容能正确通过后端验证
- 保持了对emoji的正确过滤
- 提高了后端验证的准确性和一致性
- 与前端验证逻辑保持一致

## 3. 优化"原型"字段校验体验

### 变更原因
- 提升用户填写"原型"字段时的实时反馈体验
- 当"原型"字段不符合输入限制时，提供直观的视觉提示
- 避免用户提交表单后才收到错误提示

### 变更内容

#### 1. 数据层变更
**文件**: `frontend/src/views/Figures.vue` (第282行)
- 在 `data()` 中添加 `prototypeError: ''` 字段用于存储错误信息

#### 2. 模板层变更
**文件**: `frontend/src/views/Figures.vue` (第170-179行)
- 为"原型"输入框添加动态类绑定 `:class="{ 'error-input': prototypeError }"`
- 添加 `@input="validatePrototypeOnInput"` 事件监听实时验证
- 在输入框下方添加错误信息显示 `<div v-if="prototypeError" class="error-message">`

#### 3. 方法层变更
**文件**: `frontend/src/views/Figures.vue` (新增第499-519行)
- 新增 `validatePrototypeOnInput()` 方法实现实时验证：
  - 空值允许通过（非必填字段）
  - 调用通用验证方法检查长度（1-40位）和字符类型
  - 验证失败时更新 `prototypeError` 显示具体错误信息
  - 验证通过时清空错误信息

- 修改 `validateForm()` 方法：
  - 表单提交时先调用 `validatePrototypeOnInput()` 验证原型
  - 如果原型验证失败，阻止表单提交
  - 从验证列表中移除原型字段（已在前面单独验证）

### 验证规则
- **长度校验**: 1-40 位字符
- **字符校验**: 允许中文、英文、日文、数字、空格，常见符号（如 /、×、（）、&）
- **特殊处理**: 自动过滤 emoji 字符，非必填字段

### 用户体验优化
- **实时反馈**: 用户输入时即时验证，无需等待提交
- **视觉提示**: 输入框边框变红，直观显示错误状态
- **文字提示**: 下方显示具体错误原因（如"原型长度不能超过40个字符"）
- **自动清理**: 输入时自动过滤 emoji 等特殊字符

## 4. 优化"涂装"字段校验体验

### 变更原因
- 提升用户填写"涂装"字段时的实时反馈体验
- 当"涂装"字段不符合输入限制时，提供直观的视觉提示
- 避免用户提交表单后才收到错误提示

### 变更内容

#### 1. 数据层变更
**文件**: `frontend/src/views/Figures.vue` (第282行)
- 在 `data()` 中添加 `paintingError: ''` 字段用于存储错误信息

#### 2. 模板层变更
**文件**: `frontend/src/views/Figures.vue` (第180-189行)
- 为"涂装"输入框添加动态类绑定 `:class="{ 'error-input': paintingError }"`
- 添加 `@input="validatePaintingOnInput"` 事件监听实时验证
- 在输入框下方添加错误信息显示 `<div v-if="paintingError" class="error-message">`

#### 3. 方法层变更
**文件**: `frontend/src/views/Figures.vue` (新增第522-542行)
- 新增 `validatePaintingOnInput()` 方法实现实时验证：
  - 空值允许通过（非必填字段）
  - 调用通用验证方法检查长度（1-40位）和字符类型
  - 验证失败时更新 `paintingError` 显示具体错误信息
  - 验证通过时清空错误信息

- 修改 `validateForm()` 方法：
  - 表单提交时先调用 `validatePaintingOnInput()` 验证涂装
  - 如果涂装验证失败，阻止表单提交
  - 从验证列表中移除涂装字段（已在前面单独验证）

### 验证规则
- **长度校验**: 1-40 位字符
- **字符校验**: 允许中文、英文、日文、数字、空格，常见符号（如 /、×、（）、&）
- **特殊处理**: 自动过滤 emoji 字符，非必填字段

### 用户体验优化
- **实时反馈**: 用户输入时即时验证，无需等待提交
- **视觉提示**: 输入框边框变红，直观显示错误状态
- **文字提示**: 下方显示具体错误原因（如"涂装长度不能超过40个字符"）
- **自动清理**: 输入时自动过滤 emoji 等特殊字符

## 5. 优化"原画"字段校验体验

### 变更原因
- 提升用户填写"原画"字段时的实时反馈体验
- 当"原画"字段不符合输入限制时，提供直观的视觉提示
- 避免用户提交表单后才收到错误提示

### 变更内容

#### 1. 数据层变更
**文件**: `frontend/src/views/Figures.vue` (第282行)
- 在 `data()` 中添加 `originalArtError: ''` 字段用于存储错误信息

#### 2. 模板层变更
**文件**: `frontend/src/views/Figures.vue` (第190-199行)
- 为"原画"输入框添加动态类绑定 `:class="{ 'error-input': originalArtError }"`
- 添加 `@input="validateOriginalArtOnInput"` 事件监听实时验证
- 在输入框下方添加错误信息显示 `<div v-if="originalArtError" class="error-message">`

#### 3. 方法层变更
**文件**: `frontend/src/views/Figures.vue` (新增第557-577行)
- 新增 `validateOriginalArtOnInput()` 方法实现实时验证：
  - 空值允许通过（非必填字段）
  - 调用通用验证方法检查长度（1-40位）和字符类型
  - 验证失败时更新 `originalArtError` 显示具体错误信息
  - 验证通过时清空错误信息

- 修改 `validateForm()` 方法：
  - 表单提交时先调用 `validateOriginalArtOnInput()` 验证原画
  - 如果原画验证失败，阻止表单提交
  - 从验证列表中移除原画字段（已在前面单独验证）

### 验证规则
- **长度校验**: 1-40 位字符
- **字符校验**: 允许中文、英文、日文、数字、空格，常见符号（如 /、×、（）、&）
- **特殊处理**: 自动过滤 emoji 字符，非必填字段

### 用户体验优化
- **实时反馈**: 用户输入时即时验证，无需等待提交
- **视觉提示**: 输入框边框变红，直观显示错误状态
- **文字提示**: 下方显示具体错误原因（如"原画长度不能超过40个字符"）
- **自动清理**: 输入时自动过滤 emoji 等特殊字符

## 6. 优化"作品"字段校验体验

### 变更原因
- 提升用户填写"作品"字段时的实时反馈体验
- 当"作品"字段不符合输入限制时，提供直观的视觉提示
- 避免用户提交表单后才收到错误提示

### 变更内容

#### 1. 数据层变更
**文件**: `frontend/src/views/Figures.vue` (第282行)
- 在 `data()` 中添加 `workError: ''` 字段用于存储错误信息

#### 2. 模板层变更
**文件**: `frontend/src/views/Figures.vue` (第200-209行)
- 为"作品"输入框添加动态类绑定 `:class="{ 'error-input': workError }"`
- 添加 `@input="validateWorkOnInput"` 事件监听实时验证
- 在输入框下方添加错误信息显示 `<div v-if="workError" class="error-message">`

#### 3. 方法层变更
**文件**: `frontend/src/views/Figures.vue` (新增第586-606行)
- 新增 `validateWorkOnInput()` 方法实现实时验证：
  - 空值允许通过（非必填字段）
  - 调用通用验证方法检查长度（1-80位）和字符类型
  - 验证失败时更新 `workError` 显示具体错误信息
  - 验证通过时清空错误信息

- 修改 `validateForm()` 方法：
  - 表单提交时先调用 `validateWorkOnInput()` 验证作品
  - 如果作品验证失败，阻止表单提交
  - 从验证列表中移除作品字段（已在前面单独验证）

### 验证规则
- **长度校验**: 1-80 位字符
- **字符校验**: 允许中文、英文、日文、数字、空格，常见符号（如 /、×、（）、&）
- **特殊处理**: 自动过滤 emoji 字符，非必填字段

### 用户体验优化
- **实时反馈**: 用户输入时即时验证，无需等待提交
- **视觉提示**: 输入框边框变红，直观显示错误状态
- **文字提示**: 下方显示具体错误原因（如"作品长度不能超过80个字符"）
- **自动清理**: 输入时自动过滤 emoji 等特殊字符

## 7. 修复中文字符被自动删除的问题

### 问题描述
- 在输入框中输入中文时，中文字符会被自动删除
- 原因：emoji过滤的正则表达式语法问题，导致中文字符被错误过滤

### 解决方案

#### 1. 代码修改
**文件**: `frontend/src/views/Figures.vue` (第424行)
- 修正 emoji 过滤的正则表达式：
  ```javascript
  // 原代码（问题）
  const emojiPattern = /[\u{1F600}-\u{1F64F}\u{1F300}-\u{1F5FF}\u{1F680}-\u{1F6FF}\u{1F1E0}-\u{1F1FF}\u{2600}-\u{26FF}\u{2700}-\u{27BF}\u{1F900}-\u{1F9FF}]/gu
  
  // 修正后（正确）
  const emojiPattern = /[\uD83C-\uDBFF\uDC00-\uDFFF]/g
  ```

### 技术实现
- 使用UTF-16代理对表示方式来过滤emoji字符
- 确保中文字符不会被错误过滤
- 保持对emoji的正确过滤功能

### 影响范围
- 所有使用 `validateField` 方法进行验证的字段，包括：
  - 名称
  - 入手途径
  - 原型
  - 涂装
  - 原画
  - 作品
  - 制造商
  - 比例
  - 材质
  - 尺寸

### 优化效果
- 修复了中文字符被自动删除的问题
- 确保用户可以正常输入中文字符
- 保持了对emoji的正确过滤
- 提高了用户体验

## 8. 优化"制造商"字段校验体验

### 变更原因
- 提升用户填写"制造商"字段时的实时反馈体验
- 当"制造商"字段不符合输入限制时，提供直观的视觉提示
- 避免用户提交表单后才收到错误提示

### 变更内容

#### 1. 数据层变更
**文件**: `frontend/src/views/Figures.vue` (第282行)
- 在 `data()` 中添加 `manufacturerError: ''` 字段用于存储错误信息

#### 2. 模板层变更
**文件**: `frontend/src/views/Figures.vue` (第254-263行)
- 为"制造商"输入框添加动态类绑定 `:class="{ 'error-input': manufacturerError }"`
- 添加 `@input="validateManufacturerOnInput"` 事件监听实时验证
- 在输入框下方添加错误信息显示 `<div v-if="manufacturerError" class="error-message">`

#### 3. 方法层变更
**文件**: `frontend/src/views/Figures.vue` (新增第615-635行)
- 新增 `validateManufacturerOnInput()` 方法实现实时验证：
  - 空值允许通过（非必填字段）
  - 调用通用验证方法检查长度（1-60位）和字符类型
  - 验证失败时更新 `manufacturerError` 显示具体错误信息
  - 验证通过时清空错误信息

- 修改 `validateForm()` 方法：
  - 表单提交时先调用 `validateManufacturerOnInput()` 验证制造商
  - 如果制造商验证失败，阻止表单提交
  - 从验证列表中移除制造商字段（已在前面单独验证）

### 验证规则
- **长度校验**: 1-60 位字符
- **字符校验**: 允许中文、英文、日文、数字、空格，常见符号（如 /、×、（）、&）
- **特殊处理**: 自动过滤 emoji 字符，非必填字段

### 用户体验优化
- **实时反馈**: 用户输入时即时验证，无需等待提交
- **视觉提示**: 输入框边框变红，直观显示错误状态
- **文字提示**: 下方显示具体错误原因（如"制造商长度不能超过60个字符"）
- **自动清理**: 输入时自动过滤 emoji 等特殊字符

## 9. 优化"比例"字段校验体验

### 变更原因
- 提升用户填写"比例"字段时的实时反馈体验
- 当"比例"字段不符合输入限制时，提供直观的视觉提示
- 避免用户提交表单后才收到错误提示

### 变更内容

#### 1. 数据层变更
**文件**: `frontend/src/views/Figures.vue` (第282行)
- 在 `data()` 中添加 `scaleError: ''` 字段用于存储错误信息

#### 2. 模板层变更
**文件**: `frontend/src/views/Figures.vue` (第264-273行)
- 为"比例"输入框添加动态类绑定 `:class="{ 'error-input': scaleError }"`
- 添加 `@input="validateScaleOnInput"` 事件监听实时验证
- 在输入框下方添加错误信息显示 `<div v-if="scaleError" class="error-message">`

#### 3. 方法层变更
**文件**: `frontend/src/views/Figures.vue` (新增第644-664行)
- 新增 `validateScaleOnInput()` 方法实现实时验证：
  - 空值允许通过（非必填字段）
  - 调用通用验证方法检查长度（1-20位）和字符类型
  - 验证失败时更新 `scaleError` 显示具体错误信息
  - 验证通过时清空错误信息

- 修改 `validateForm()` 方法：
  - 表单提交时先调用 `validateScaleOnInput()` 验证比例
  - 如果比例验证失败，阻止表单提交
  - 从验证列表中移除比例字段（已在前面单独验证）

### 验证规则
- **长度校验**: 1-20 位字符
- **字符校验**: 允许中文、英文、日文、数字、空格，常见符号（如 /、×、（）、&）
- **特殊处理**: 自动过滤 emoji 字符，非必填字段

### 用户体验优化
- **实时反馈**: 用户输入时即时验证，无需等待提交
- **视觉提示**: 输入框边框变红，直观显示错误状态
- **文字提示**: 下方显示具体错误原因（如"比例长度不能超过20个字符"）
- **自动清理**: 输入时自动过滤 emoji 等特殊字符

## 10. 优化"材质"字段校验体验

### 变更原因
- 提升用户填写"材质"字段时的实时反馈体验
- 当"材质"字段不符合输入限制时，提供直观的视觉提示
- 避免用户提交表单后才收到错误提示

### 变更内容

#### 1. 数据层变更
**文件**: `frontend/src/views/Figures.vue` (第282行)
- 在 `data()` 中添加 `materialError: ''` 字段用于存储错误信息

#### 2. 模板层变更
**文件**: `frontend/src/views/Figures.vue` (第274-283行)
- 为"材质"输入框添加动态类绑定 `:class="{ 'error-input': materialError }"`
- 添加 `@input="validateMaterialOnInput"` 事件监听实时验证
- 在输入框下方添加错误信息显示 `<div v-if="materialError" class="error-message">`

#### 3. 方法层变更
**文件**: `frontend/src/views/Figures.vue` (新增第673-693行)
- 新增 `validateMaterialOnInput()` 方法实现实时验证：
  - 空值允许通过（非必填字段）
  - 调用通用验证方法检查长度（1-50位）和字符类型
  - 验证失败时更新 `materialError` 显示具体错误信息
  - 验证通过时清空错误信息

- 修改 `validateForm()` 方法：
  - 表单提交时先调用 `validateMaterialOnInput()` 验证材质
  - 如果材质验证失败，阻止表单提交
  - 从验证列表中移除材质字段（已在前面单独验证）

### 验证规则
- **长度校验**: 1-50 位字符
- **字符校验**: 允许中文、英文、日文、数字、空格，常见符号（如 /、×、（）、&）
- **特殊处理**: 自动过滤 emoji 字符，非必填字段

### 用户体验优化
- **实时反馈**: 用户输入时即时验证，无需等待提交
- **视觉提示**: 输入框边框变红，直观显示错误状态
- **文字提示**: 下方显示具体错误原因（如"材质长度不能超过50个字符"）
- **自动清理**: 输入时自动过滤 emoji 等特殊字符

## 11. 优化"尺寸"字段校验体验

### 变更原因
- 提升用户填写"尺寸"字段时的实时反馈体验
- 当"尺寸"字段不符合输入限制时，提供直观的视觉提示
- 避免用户提交表单后才收到错误提示

### 变更内容

#### 1. 数据层变更
**文件**: `frontend/src/views/Figures.vue` (第282行)
- 在 `data()` 中添加 `sizeError: ''` 字段用于存储错误信息

#### 2. 模板层变更
**文件**: `frontend/src/views/Figures.vue` (第284-293行)
- 为"尺寸"输入框添加动态类绑定 `:class="{ 'error-input': sizeError }"`
- 添加 `@input="validateSizeOnInput"` 事件监听实时验证
- 在输入框下方添加错误信息显示 `<div v-if="sizeError" class="error-message">`

#### 3. 方法层变更
**文件**: `frontend/src/views/Figures.vue` (新增第702-722行)
- 新增 `validateSizeOnInput()` 方法实现实时验证：
  - 空值允许通过（非必填字段）
  - 调用通用验证方法检查长度（1-50位）和字符类型
  - 验证失败时更新 `sizeError` 显示具体错误信息
  - 验证通过时清空错误信息

- 修改 `validateForm()` 方法：
  - 表单提交时先调用 `validateSizeOnInput()` 验证尺寸
  - 如果尺寸验证失败，阻止表单提交
  - 从验证列表中移除尺寸字段（已在前面单独验证）

### 验证规则
- **长度校验**: 1-50 位字符
- **字符校验**: 允许中文、英文、日文、数字、空格，常见符号（如 /、×、（）、&）
- **特殊处理**: 自动过滤 emoji 字符，非必填字段

### 用户体验优化
- **实时反馈**: 用户输入时即时验证，无需等待提交
- **视觉提示**: 输入框边框变红，直观显示错误状态
- **文字提示**: 下方显示具体错误原因（如"尺寸长度不能超过50个字符"）
- **自动清理**: 输入时自动过滤 emoji 等特殊字符

## 12. 优化添加手办页面初始化逻辑

### 变更原因
- 提升用户体验，确保每次打开添加手办页面时表单都是全新的状态
- 避免保留上一次新增失败的数据，防止用户混淆
- 提供更清晰的表单交互流程

### 变更内容

#### 1. 方法层变更
**文件**: `frontend/src/views/Figures.vue` (新增第818-864行)
- 新增 `resetForm()` 方法：
  - 重置所有错误信息字段（如 `nameError`、`purchaseMethodError` 等）
  - 重置表单数据为初始状态
  - 重置标签页为默认的"基础"标签页

- 新增 `openAddForm()` 方法：
  - 先调用 `resetForm()` 重置表单
  - 再设置 `showAddForm = true` 显示表单

- 修改 `addFigure()` 方法：
  - 成功添加手办后调用 `resetForm()` 重置表单

#### 2. 模板层变更
**文件**: `frontend/src/views/Figures.vue` (第6行)
- 修改添加手办按钮的点击事件：`@click="openAddForm"`

### 功能效果
- 每次点击"添加手办"按钮时，表单都会自动重置为初始状态
- 不会保留上一次填写的数据或错误信息
- 提供了更干净、更可预测的表单交互体验

### 用户体验优化
- **一致性**: 每次打开表单都是相同的初始状态
- **清晰性**: 避免了旧数据的干扰
- **可靠性**: 确保表单验证和提交逻辑的正确性

## 13. 优化入手价格币种选择功能

### 变更原因
- 解决"定价"和"入手价格"币种绑定的问题
- 用户需要能够独立选择入手价格的币种
- 避免定价选择日元后，入手价格币种也被强制改为日元

### 变更内容

#### 1. 数据模型变更
**文件**: `backend/app/models/figure.py` (第16行)
- 新增 `purchase_currency` 字段用于存储入手价格的币种

**文件**: `backend/app/schemas/figure.py` (第54行、第167行)
- 在 `FigureBase` 类中添加 `purchase_currency: str = "CNY"` 字段
- 在 `FigureUpdate` 类中添加 `purchase_currency: str | None = None` 字段

#### 2. 前端数据层变更
**文件**: `frontend/src/views/Figures.vue` (第346行)
- 在 `newFigure` 数据结构中添加 `purchase_currency: 'CNY'` 字段

**文件**: `frontend/src/views/Figures.vue` (第841行)
- 在 `resetForm()` 方法中添加 `purchase_currency: 'CNY'` 字段重置

#### 3. 前端模板层变更
**文件**: `frontend/src/views/Figures.vue` (第100行)
- 修改"入手价格"后面的币种选择器绑定：`v-model="newFigure.purchase_currency"`

#### 4. 前端显示层变更
**文件**: `frontend/src/views/Figures.vue` (第30行)
- 修改手办列表中入手价格的币种显示：`getCurrencySymbol(figure.purchase_currency)`

#### 5. 表单提交处理
**文件**: `frontend/src/views/Figures.vue` (第880行)
- 在 `addFigure()` 方法中添加 `purchase_currency` 字段的处理

### 功能效果
- "定价"和"入手价格"的币种可以独立选择
- 定价选择日元时，入手价格仍可选择人民币或其他币种
- 手办列表中正确显示入手价格的币种

### 用户体验优化
- **灵活性**: 用户可以根据实际情况独立设置定价和入手价格的币种
- **准确性**: 避免了币种绑定导致的显示错误
- **一致性**: 表单提交和列表显示都使用正确的币种信息

## 14. 删除"入手形式"中的"请选择"选项

### 变更原因
- 简化用户选择流程，去除冗余的"请选择"占位选项
- 用户可以直接从实际选项中进行选择
- 提升表单交互的简洁性

### 变更内容

#### 1. 前端模板层变更
**文件**: `frontend/src/views/Figures.vue` (第123-131行)
- 删除"入手形式"选择器中的 `<el-option value="" label="请选择" />` 选项
- 保留其他实际选项：其他、预定、现货、二手、散货、国产

### 功能效果
- "入手形式"下拉框不再显示"请选择"选项
- 用户可以直接从实际有效的选项中进行选择
- 简化了用户的选择流程

### 用户体验优化
- **简洁性**: 去除了冗余的占位选项，界面更加简洁
- **直观性**: 用户可以直接看到所有可用的选项
- **效率性**: 减少了用户的选择步骤

## 15. 新增手办管理页面翻页功能

### 变更原因
- 提升大量手办数据的浏览体验
- 减少页面加载时间，提高性能
- 提供灵活的分页选项，满足不同用户的需求

### 变更内容

#### 1. 数据层变更
**文件**: `frontend/src/views/Figures.vue` (第338-340行)
- 添加分页相关状态变量：
  - `currentPage: 1` - 当前页码
  - `pageSize: 15` - 每页条数（默认15条）
  - `pageSizes: [15, 30, 45, 60]` - 分页选项

#### 2. 计算属性变更
**文件**: `frontend/src/views/Figures.vue` (第372-382行)
- 添加分页处理计算属性：
  - `paginatedFigures()` - 计算当前页显示的手办数据
  - `totalFigures()` - 计算总数据量

#### 3. 模板层变更
**文件**: `frontend/src/views/Figures.vue` (第18行)
- 修改手办列表循环，使用 `paginatedFigures` 计算属性

**文件**: `frontend/src/views/Figures.vue` (第306-317行)
- 添加Element Plus分页组件，配置：
  - 双向绑定当前页码和每页条数
  - 提供15、30、45、60条/页的选项
  - 显示总数、每页条数选择、页码导航等功能

#### 4. 方法层变更
**文件**: `frontend/src/views/Figures.vue` (第952-961行)
- 添加分页相关方法：
  - `handleSizeChange(val)` - 处理每页条数变化
  - `handleCurrentChange(val)` - 处理页码变化

#### 5. 样式层变更
**文件**: `frontend/src/views/Figures.vue` (第1623-1629行)
- 添加分页组件样式，确保在右下角显示

### 功能效果
- 手办管理页面底部显示分页组件
- 支持15条/页、30条/页、45条/页、60条/页的切换
- 默认显示15条/页
- 支持页码导航、跳转和总数显示

### 用户体验优化
- **性能提升**: 减少一次性加载的数据量，提高页面响应速度
- **灵活性**: 用户可以根据自己的需求选择合适的每页显示条数
- **直观性**: 清晰的分页界面，方便用户浏览和导航
- **一致性**: 与Element Plus的整体设计风格保持一致

## 16. 优化手办管理页面显示顺序

### 变更原因
- 提升用户体验，让最新添加的手办优先显示
- 方便用户快速查看最近添加的手办信息
- 符合用户的使用习惯和预期

### 变更内容

#### 1. 计算属性变更
**文件**: `frontend/src/views/Figures.vue` (第385-395行)
- 修改 `paginatedFigures()` 计算属性：
  - 添加按id排序的逻辑，最新添加的手办在前面（id自增）
  - 排序后再进行分页处理

### 功能效果
- 手办管理页面按添加时间排序，最新添加的手办在最前面
- 一开始添加的手办在最后面
- 排序逻辑基于id字段（自增），确保顺序的准确性

### 用户体验优化
- **直观性**: 用户打开页面即可看到最新添加的手办
- **便捷性**: 无需手动排序，系统自动按时间顺序展示
- **一致性**: 符合用户对时间顺序的认知习惯