# 2026-04-14 更新日志

## 功能更新

### 前端组件注释添加

为了提高代码可维护性，为以下目录下的所有Vue组件添加了详细的文件头注释：

#### Dashboard 目录
- `components/collector/ActivityFeed.vue` - 收藏家模式动态流组件
- `components/collector/CollectorHeader.vue` - 收藏家模式头部组件
- `components/collector/CollectorOverview.vue` - 收藏家模式概览组件
- `components/collector/TagCloud.vue` - 收藏家模式标签云组件
- `components/collector/ValuableCollections.vue` - 收藏家模式价值收藏组件
- `components/reseller/assets/AssetOverview.vue` - 资产概览组件
- `components/reseller/assets/ChartSection.vue` - 资产分布和收益曲线组件
- `components/reseller/assets/HoldingsList.vue` - 持仓列表组件
- `components/reseller/assets/IndexComparison.vue` - 指数对比组件
- `components/reseller/assets/PriceUpdateDialog.vue` - 修改市场价弹窗
- `components/reseller/assets/ProfitAnalysis.vue` - 盈亏分析组件
- `components/reseller/market/KlineSection.vue` - K线图组件
- `components/reseller/market/MarketIndex.vue` - 市场指数组件
- `components/reseller/market/ResearchSection.vue` - 研究板块组件
- `components/reseller/market/SectorRanking.vue` - 板块排名组件
- `components/reseller/market/WatchlistSection.vue` - 关注列表组件
- `components/reseller/trade/ProfitAnalysis.vue` - 交易盈亏分析组件
- `components/reseller/trade/QuickActions.vue` - 快速操作组件
- `components/reseller/trade/TradeFlow.vue` - 交易流水组件
- `components/reseller/trade/TradeStats.vue` - 交易统计组件
- `components/reseller/AssetView.vue` - 倒狗模式资产模块组件
- `components/reseller/MarketView.vue` - 倒狗模式市场模块组件
- `components/reseller/TradeView.vue` - 倒狗模式交易模块组件

#### FigureDetail 目录
- `components/FigureAuthorInfo.vue` - 手办作者信息组件
- `components/FigureBasicInfo.vue` - 手办基本信息组件
- `components/FigureHeader.vue` - 手办详情页头部组件
- `components/FigureImages.vue` - 手办图片展示组件
- `components/FigureOrders.vue` - 手办订单信息组件
- `components/FigureSpecInfo.vue` - 手办规格信息组件
- `components/FigureTags.vue` - 手办标签组件

#### Figures 目录
- `components/form/FormAuthorTab.vue` - 手办表单作者信息标签页组件
- `components/form/FormBasicTab.vue` - 手办表单基础信息标签页组件
- `components/form/FormSpecsTab.vue` - 手办表单规格信息标签页组件
- `components/form/FormTagsTab.vue` - 手办表单标签管理标签页组件
- `components/form/ImageUpload.vue` - 图片上传组件
- `components/FigureForm.vue` - 手办表单组件
- `components/FigureItem.vue` - 手办卡片组件
- `components/FiguresHeader.vue` - 手办列表头部组件
- `components/FiguresList.vue` - 手办列表组件
- `components/ImportFiguresDialog.vue` - 导入手办数据弹窗组件
- `components/Pagination.vue` - 分页组件
- `components/SearchFilters.vue` - 搜索筛选组件
- `Figures.vue` - 手办管理主页面

#### Orders 目录
- `components/OrderForm.vue` - 订单表单组件
- `components/OrderItem.vue` - 订单项组件
- `components/OrdersList.vue` - 订单列表组件
- `Orders.vue` - 订单管理主页面

### 注释规范

所有组件注释包含以下内容：
- 组件名称和功能说明
- 详细的功能描述
- 组件依赖关系
- 维护提示和注意事项

## Bug修复

### 导入手办功能修复

**问题描述：**
在手办管理页面中使用导入手办功能时，前端页面提示"导入失败"，但后端接口返回 200 状态码。

**问题原因：**
`ImportFiguresDialog.vue` 组件中使用了 `await emit('import', previewData.value)` 来等待导入结果，但 Vue 的 `$emit` 是同步的，不会返回 Promise 或等待父组件处理完成，导致导入结果无法正确获取。

**修复内容：**
- `frontend/src/views/Figures/components/ImportFiguresDialog.vue`
  - 修改 `handleImport` 方法，使用 Promise 包装 emit，通过回调函数机制等待父组件处理完成
  
- `frontend/src/views/Figures.vue`
  - 修改 `handleImport` 方法，接收回调函数参数并在处理完成后调用回调返回结果

**修复后行为：**
- 导入手办功能正常工作
- 导入成功时显示成功状态和导入统计
- 导入失败时显示错误信息

### 导入功能修复（续）

**问题描述：**
修复导入功能后，前端仍然提示"导入失败"，但后端返回 200 状态码。

**问题原因：**
- 前端使用了错误的字段名：`imported_count` 和 `orders_count`，但后端返回的是 `imported_figures` 和 `imported_orders`
- 后端在跳过已存在的手办时，仍然增加了 `updated_figures` 计数，这是不正确的

**修复内容：**
- `frontend/src/views/Figures/composables/useImportFigures.js`
  - 修正字段名，使用 `imported_figures` 和 `imported_orders`
  
- `backend/app/services/figure_import_service.py`
  - 移除 `updated_figures` 计数逻辑，因为现在不会更新已存在的手办

### 导入功能修复（最终）

**问题描述：**
前端提示 "Cannot read properties of undefined (reading 'success')"，但后端返回 200 状态码。

**问题原因：**
- axios 响应拦截器已经返回了 `response.data`，所以 `response` 已经是数据对象，再访问 `.data` 就会是 undefined
- 后端返回的字段名与前端期望的不一致

**修复内容：**
- `frontend/src/views/Figures/composables/useImportFigures.js`
  - 移除 `.data` 访问，因为 axios 响应拦截器已经返回了数据
  - 修正字段名，使用 `imported_count` 和 `orders_count` 与后端保持一致

### 导入逻辑优化（续）

**问题描述：**
导入 `figures_2026-04-13.json` 文件时，显示 "成功导入 0 个新手办，更新 0 个手办，导入 0 个订单"，但文件中包含 3 个手办和 6 个订单。

**问题原因：**
- 手办导入逻辑通过 `name` 字段检查是否存在，如果存在则跳过
- 订单导入逻辑通过 `id` 字段检查是否存在，如果存在则跳过
- 数据库中可能已经存在同名手办或相同ID的订单

**修复内容：**
- `backend/app/services/figure_import_service.py`
  - 修改 `import_figure` 方法：总是创建新的手办，不检查是否存在
  - 修改 `import_orders` 方法：总是创建新的订单，不检查是否存在

**修复后行为：**
- 导入时总是创建新的手办和订单记录
- 不会因为已存在同名手办或相同ID订单而跳过导入
- 正确统计导入的手办和订单数量

### 导入逻辑优化

**修改内容：**
修改 `backend/app/services/figure_import_service.py` 中的导入逻辑：

1. **手办导入逻辑 (`import_figure` 方法)**：
   - 如果手办名称已存在，直接跳过，不进行任何更新
   - 只创建新的手办记录

2. **订单导入逻辑 (`import_orders` 方法)**：
   - 如果订单ID已存在，直接跳过，不进行任何更新
   - 只为新手办创建新的订单记录

**修改原因：**
- 避免导入时意外覆盖现有手办和订单数据
- 确保导入操作只进行增量添加，不影响已有数据
- 提高数据安全性，防止误操作导致数据丢失

### 导入响应数据处理修复

**问题描述：**
导入手办功能仍然显示"导入失败"，虽然后端返回 200 状态码。

**问题原因：**
`useImportFigures.js` 中的响应处理代码直接访问 `response.imported_count`，但 axios 返回的响应数据在 `response.data` 属性中，导致无法正确获取导入结果。

**修复内容：**
- `frontend/src/views/Figures/composables/useImportFigures.js`
  - 修改响应数据访问方式，从 `response.imported_count` 改为 `response.data.imported_count`
  - 修改成功判断，从固定 `success: true` 改为 `response.data.success`

**修复后行为：**
- 正确获取并显示导入结果统计
- 根据后端返回的 success 字段判断导入是否成功

## 后端模型注释添加

为了提高后端代码的可维护性，为所有数据模型添加了详细的字段注释：

### 修改的文件

- `backend/app/models/figure.py` - 手办模型
  - 添加类级文档字符串，说明模型功能
  - 为每个字段添加详细注释，包括字段含义、数据类型、使用场景
  - 添加关联关系说明

- `backend/app/models/order.py` - 订单模型
  - 添加类级文档字符串，说明订单状态和业务流程
  - 为每个字段添加详细注释
  - 说明定金+尾款的付款模式

- `backend/app/models/asset.py` - 资产相关模型
  - AssetPriceHistory: 资产价格历史记录
  - AssetAlert: 资产预警设置
  - AssetTransaction: 资产交易记录
  - StockIndexCache: 股票指数缓存
  - StockIndexHistory: 股票指数历史
  - AssetValueCache: 资产市值缓存
  - 为每个模型添加详细的功能说明
  - 为每个字段添加业务含义注释

- `backend/app/models/user.py` - 用户模型
  - 添加用户认证和权限说明
  - 为每个字段添加安全相关注释

- `backend/app/models/tag.py` - 标签模型
  - 添加多对多关联说明
  - 说明标签的使用场景

- `backend/app/models/database.py` - 数据库配置
  - 添加数据库连接配置说明
  - 说明 SQLAlchemy 引擎和会话工厂的使用
  - 添加 get_db 依赖函数的详细文档

### 注释规范

所有模型注释包含以下内容：
- 模型/类级文档字符串，说明整体功能
- 字段级注释，说明字段含义、数据类型、业务场景
- 关联关系说明
- 特殊业务逻辑说明（如订单状态、预警类型等）

## 部署更新

- 执行 `docker-compose up -d --build` 重新部署服务，确保所有前端组件注释更新、后端模型注释和导入功能修复生效
