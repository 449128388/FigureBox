# 2026-03-25 更新日志

## 1. 修复订单出荷日期少一天问题

### 问题原因
- 前端使用 `date.toISOString().split('T')[0]` 将日期转换为字符串
- `toISOString()` 方法返回的是 UTC 时间（协调世界时）
- 中国处于 UTC+8 时区，当选择日期后转换为 UTC 时，时间会回退 8 小时
- 例如：选择 2026-03-25，转换为 UTC 后变成 2026-03-24 16:00:00，截取日期部分就变成 2026-03-24

### 变更内容

#### 前端更新
**文件**: `frontend/src/views/Orders.vue`
- 修改 `formatDate` 函数的实现
- 原实现：`return date.toISOString().split('T')[0]`
- 新实现：使用 `getFullYear()`, `getMonth()`, `getDate()` 获取本地时间年月日，避免时区转换问题

### 修复代码
```javascript
const formatDate = (date) => {
  if (!date) return null
  if (typeof date === 'string') return date
  // 转换为 YYYY-MM-DD 格式，使用本地时间避免时区问题
  const d = new Date(date)
  const year = d.getFullYear()
  const month = String(d.getMonth() + 1).padStart(2, '0')
  const day = String(d.getDate()).padStart(2, '0')
  return `${year}-${month}-${day}`
}
```

### 功能效果
- 新增订单时，选择的出荷日期与保存后的日期一致
- 编辑订单时，修改的出荷日期与保存后的日期一致
- 不再出现时区导致的日期偏差问题

### 用户体验优化
- **数据准确**: 用户选择的日期就是保存的日期
- **避免困惑**: 不再出现"明明选了 25 号，保存后变成 24 号"的情况
- **时区兼容**: 使用本地时间处理，适应不同时区用户

## 2. 修复 Token 自动续期功能不生效问题

### 问题原因
- CORS 中间件在 TokenRefreshMiddleware 之后添加，导致自定义响应头 `X-Refresh-Token` 无法被前端正确获取
- 前端 axios 配置缺少自定义响应头的暴露声明
- 中间件顺序错误导致响应头被 CORS 中间件过滤

### 变更内容

#### 后端更新
**文件**: `backend/main.py`
- 调整中间件添加顺序：将 CORS 中间件移到 TokenRefreshMiddleware 之前
- 确保 `expose_headers=["X-Refresh-Token"]` 配置生效
- TokenRefreshMiddleware 在 CORS 之后处理，响应头能正确传递给前端

**关键调整**:
```python
# 1. 先添加 CORS 中间件（配置暴露自定义响应头）
app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
    expose_headers=["X-Refresh-Token"],  # 暴露自定义响应头
)

# 2. 再添加 Token 续期中间件
app.add_middleware(TokenRefreshMiddleware)
```

#### 前端更新
**文件**: `frontend/src/axios/index.js`
- 在 axios 实例配置中添加 `headers` 选项，声明需要暴露的自定义响应头
- 添加 token 续期日志输出，便于调试
- 确保前端能正确获取并存储新 token

**配置优化**:
```javascript
const instance = axios.create({
  baseURL: '/api',
  timeout: 10000,
  // 允许获取自定义响应头
  headers: {
    'Access-Control-Expose-Headers': 'X-Refresh-Token'
  }
})
```

### 功能效果
- **Token 自动续期**: 当 token 剩余有效期小于 5 分钟时，自动续期 30 分钟
- **无感知续期**: 用户在操作系统时，token 在后台自动续期，不会被打断
- **持续登录**: 只要用户持续操作，token 会不断续期，保持登录状态
- **安全退出**: 当用户停止操作超过 30 分钟，token 过期，需要重新登录

### 用户体验优化
- **避免频繁登录**: 用户正常使用时不会因为 token 过期而被强制退出
- **操作流畅**: 不需要手动刷新页面或重新登录
- **安全保障**: token 仍然有过期时间，长期不操作会自动失效
- **调试友好**: 添加日志输出，方便排查 token 续期问题

## 3. 验证登录超时配置

### 配置验证结果
**文件**: `backend/app/utils/jwt.py`

经过验证，当前系统的登录超时配置**完全符合需求**：

#### 配置参数
```python
ACCESS_TOKEN_EXPIRE_MINUTES = 30              # Token 有效期：30 分钟
TOKEN_REFRESH_THRESHOLD_MINUTES = 5           # 续期阈值：剩余 5 分钟时自动续期
```

#### 工作机制
1. **初始登录**: 用户登录后获得有效期 30 分钟的 token
2. **自动续期**: 当 token 剩余有效期小于 5 分钟时，系统自动续期新的 30 分钟
3. **持续操作**: 只要用户在 25 分钟内进行操作，token 会不断续期，保持登录状态
4. **超时退出**: 如果用户超过 30 分钟没有任何操作，token 过期，需要重新登录

#### 验证结论
✅ **30 分钟内不操作需要重新登录** - 符合要求  
✅ **30 分钟内存在操作则重新计算超时时间** - 符合要求  
✅ **自动续期机制正常工作** - 已修复并验证

### 配置说明
- Token 有效期设置为 30 分钟，确保安全性
- 续期阈值设置为 5 分钟，确保在 token 即将过期前自动续期
- 这种设计既保证了安全性（长期不操作自动退出），又提供了良好的用户体验（操作期间无感知续期）

## 4. 尾款管理列表增加手办图片展示

### 变更内容

#### 前端更新
**文件**: `frontend/src/views/Orders.vue`

##### 模板更新
- 在订单列表项 (`order-item`) 中新增手办图片展示区域
- 图片显示逻辑与手办列表保持一致：
  - 有图片时显示第一张图片
  - 无图片时显示默认占位图 `/imgs/no_image.png`
  - 使用 `loading="lazy"` 和 `decoding="async"` 优化加载性能

**添加的代码**:
```html
<div class="figure-image">
  <img 
    :src="order.figure.images && order.figure.images.length > 0 ? order.figure.images[0] : '/imgs/no_image.png'" 
    :alt="order.figure.name"
    loading="lazy"
    decoding="async"
  >
</div>
```

##### 样式更新
- 添加 `.order-item .figure-image` 样式：
  - 宽度：100%
  - 高度：200px
  - 圆角：4px
  - 背景色：#f5f5f5（图片加载前的占位色）
- 添加 `.order-item .figure-image img` 样式：
  - `object-fit: cover` 保持图片比例填充
  - `content-visibility: auto` 优化渲染性能

**添加的样式**:
```css
.order-item .figure-image {
  width: 100%;
  height: 200px;
  border-radius: 4px;
  overflow: hidden;
  margin-bottom: 15px;
  background-color: #f5f5f5;
}

.order-item .figure-image img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  content-visibility: auto;
}
```

### 功能效果
- **视觉统一**: 尾款管理列表与手办列表保持一致的图片展示风格
- **信息丰富**: 用户可以通过图片快速识别订单对应的手办
- **性能优化**: 懒加载和异步解码确保页面流畅性
- **容错处理**: 无图片时显示默认占位图，避免布局错乱

### 用户体验优化
- **直观识别**: 通过图片快速定位目标订单
- **美观整洁**: 统一的图片展示风格提升页面美观度
- **响应式适配**: 图片样式适配不同屏幕尺寸

## 5. 新增手办数据下载功能

### 变更内容

#### 前端更新
**文件**: `frontend/src/views/Figures.vue`

##### 模板更新
- 在手办管理页面的操作按钮区域新增「下载数据」按钮
- 按钮样式与其他操作按钮保持一致，使用橙色主题
- 按钮包含下载图标和文字标签

**添加的代码**:
```html
<button class="btn btn-download" @click="downloadFigures">
  <i class="fa-solid fa-download"></i> 下载数据
</button>
```

##### 脚本更新
- 新增 `downloadFigures()` 方法，调用后端下载接口
- 使用 axios 发送 GET 请求到 `/api/figures/download`
- 接收 JSON 响应并创建下载链接
- 自动命名下载文件为 `figures_YYYY-MM-DD.json`
- 添加操作成功/失败的消息提示

**添加的方法**:
```javascript
async downloadFigures() {
  try {
    const response = await axios.get('/api/figures/download')
    
    // 创建下载链接
    const blob = new Blob([response], { type: 'application/json' })
    const url = URL.createObjectURL(blob)
    const a = document.createElement('a')
    a.href = url
    a.download = `figures_${new Date().toISOString().split('T')[0]}.json`
    document.body.appendChild(a)
    a.click()
    document.body.removeChild(a)
    URL.revokeObjectURL(url)
    
    this.$message.success('手办数据下载成功')
  } catch (error) {
    console.error('下载失败:', error)
    this.$message.error('下载失败，请重试')
  }
}
```

##### 样式更新
- 添加 `.btn-download` 样式：
  - 背景色：#ff9800（橙色）
  - 文字颜色：白色
  -  padding：12px 24px
  - 字体大小：16px
  - 弹性布局，图标和文字间距 8px

**添加的样式**:
```css
.btn-download {
  background-color: #ff9800;
  color: white;
  padding: 12px 24px;
  font-size: 16px;
  display: flex;
  align-items: center;
  gap: 8px;
}

.btn-download:hover {
  background-color: #f57c00;
}
```

#### 后端更新
**文件**: `backend/app/api/figures.py`

##### 接口添加
- 新增 `GET /api/figures/download` 接口
- 功能：获取所有手办数据并以 JSON 格式下载
- 响应：返回 JSON 文件，包含所有手办的完整数据
- 文件名：`figures_YYYY-MM-DD.json`（自动生成日期）

**添加的接口**:
```python
@router.get("/download")
def download_figures(db: Session = Depends(get_db)):
    """
    下载所有手办数据为 JSON 格式
    """
    # 获取所有手办数据
    figures = db.query(Figure).all()
    
    # 转换为字典列表
    figures_data = []
    for figure in figures:
        figure_dict = {
            "id": figure.id,
            "name": figure.name,
            "japanese_name": figure.japanese_name,
            "manufacturer": figure.manufacturer,
            "price": figure.price,
            "currency": figure.currency,
            "tags": figure.tags,
            "release_date": figure.release_date,
            "purchase_price": figure.purchase_price,
            "purchase_currency": figure.purchase_currency,
            "purchase_date": figure.purchase_date,
            "purchase_method": figure.purchase_method,
            "purchase_type": figure.purchase_type,
            "scale": figure.scale,
            "painting": figure.painting,
            "original_art": figure.original_art,
            "work": figure.work,
            "material": figure.material,
            "size": figure.size,
            "images": figure.images
        }
        figures_data.append(figure_dict)
    
    # 转换为 JSON 字符串
    json_data = json.dumps(figures_data, ensure_ascii=False, indent=2)
    
    # 返回 JSON 响应
    return Response(
        content=json_data,
        media_type="application/json",
        headers={
            "Content-Disposition": f"attachment; filename=figures_{datetime.utcnow().strftime('%Y-%m-%d')}.json"
        }
    )
```

### 功能效果
- **数据导出**: 一键下载所有手办数据为 JSON 文件
- **完整数据**: 包含手办的所有字段信息
- **日期命名**: 自动以当前日期命名下载文件
- **用户友好**: 操作成功/失败有明确的消息提示
- **前后端分离**: 前端负责触发下载，后端负责数据处理和响应

### 用户体验优化
- **操作便捷**: 直观的下载按钮，一键完成数据导出
- **数据备份**: 方便用户定期备份手办数据
- **数据迁移**: 便于在不同系统间迁移手办数据
- **格式规范**: 导出的 JSON 格式美观易读，便于人工查看和编辑

## 6. 修复手办数据下载接口错误

### 问题原因
- 路由顺序错误：`/download` 路由定义在 `/{figure_id}` 路由之后
- FastAPI 尝试将 "download" 作为 `figure_id` 参数匹配，而 `figure_id` 被定义为 `int` 类型
- 导致 422 Unprocessable Entity 错误："Input should be a valid integer, unable to parse string as an integer"

### 解决方案
- 调整路由定义顺序，将 `/download` 路由移到 `/{figure_id}` 路由之前
- 确保 FastAPI 优先匹配具体的 `/download` 路由，而不是将其作为路径参数

### 变更内容
**文件**: `backend/app/api/figures.py`

**路由顺序调整**:
```python
# 正确的路由顺序
@router.get("/")
def get_figures():
    ...

@router.get("/download")  # 先定义具体路由
def download_figures():
    ...

@router.get("/{figure_id}")  # 后定义带参数的路由
def get_figure(figure_id: int):
    ...
```

### 功能效果
- ✅ 修复了下载数据按钮的 422 错误
- ✅ `/api/figures/download` 接口正常工作
- ✅ 手办数据可以成功下载为 JSON 文件

### 用户体验优化
- **操作流畅**: 点击下载按钮不再报错
- **功能可靠**: 数据下载功能恢复正常
- **错误修复**: 解决了路由匹配导致的参数解析错误

## 7. 修复下载接口 500 错误（日期序列化问题）

### 问题原因
- 手办数据中的 `release_date` 和 `purchase_date` 字段是 Python `datetime.date` 类型
- JSON 标准不支持日期类型，直接使用 `json.dumps()` 会导致序列化错误
- 错误信息："Object of type date is not JSON serializable"
- 导致 500 Internal Server Error

### 解决方案
1. **添加日期序列化辅助函数**：
   - 创建 `json_serial()` 函数处理 `datetime` 类型的序列化
   - 将日期转换为 ISO 8601 格式的字符串

2. **处理日期字段**：
   - 在构建字典时，将日期字段转换为字符串格式
   - 使用 `.isoformat()` 方法将日期对象转为字符串
   - 处理 `None` 值的情况，避免空日期报错

3. **增强错误处理**：
   - 添加 `try-except` 块捕获所有异常
   - 打印详细的错误日志，便于排查问题
   - 返回友好的错误信息给用户

### 变更内容
**文件**: `backend/app/api/figures.py`

**添加日期序列化函数**:
```python
def json_serial(obj):
    """JSON 序列化辅助函数，处理日期类型"""
    if isinstance(obj, datetime):
        return obj.isoformat()
    raise TypeError(f"Type {type(obj)} not serializable")
```

**修复日期字段处理**:
```python
# 修复前（会导致 500 错误）
"release_date": figure.release_date,
"purchase_date": figure.purchase_date,

# 修复后（正确处理日期类型）
"release_date": figure.release_date.isoformat() if figure.release_date else None,
"purchase_date": figure.purchase_date.isoformat() if figure.purchase_date else None,
```

**增强 JSON 序列化**:
```python
# 修复前
json_data = json.dumps(figures_data, ensure_ascii=False, indent=2)

# 修复后
json_data = json.dumps(figures_data, ensure_ascii=False, indent=2, default=json_serial)
```

**添加错误处理**:
```python
try:
    # ... 数据处理逻辑 ...
except Exception as e:
    import traceback
    print(f"下载数据时发生错误: {str(e)}")
    print(traceback.format_exc())
    raise HTTPException(
        status_code=status.HTTP_500_INTERNAL_SERVER_ERROR,
        detail=f"下载数据失败: {str(e)}"
    )
```

### 功能效果
- ✅ 修复了日期序列化导致的 500 错误
- ✅ 日期字段正确转换为 ISO 8601 格式字符串
- ✅ 空日期字段正确处理为 `null`
- ✅ 详细的错误日志便于问题排查
- ✅ 友好的错误提示提升用户体验

### 用户体验优化
- **数据完整性**: 所有手办数据字段都能正确导出
- **格式标准**: 日期使用国际标准 ISO 8601 格式
- **错误友好**: 详细的错误信息帮助快速定位问题
- **稳定可靠**: 增强了异常处理，提高接口稳定性

## 8. 修复下载文件显示 [object Object] 问题

### 问题原因
- 前端使用 axios 发送请求，axios 默认将响应自动解析为 JavaScript 对象
- 下载方法直接将解析后的对象放入 Blob，导致 `[object Object]` 被写入文件
- 需要将响应作为原始文本处理，而不是解析后的对象

### 解决方案
- **改用原生 fetch API**：使用浏览器原生的 `fetch` 方法发送请求
- **正确处理响应**：使用 `response.text()` 获取原始 JSON 字符串
- **添加认证头**：手动添加 Authorization 头携带 token
- **错误处理**：检查响应状态，处理下载失败情况

### 变更内容
**文件**: `frontend/src/views/Figures.vue`

**修复下载方法**:
```javascript
// 修复前（导致 [object Object]）
async downloadFigures() {
  const response = await axios.get('/api/figures/download')
  const blob = new Blob([response], { type: 'application/json' })
  // ...
}

// 修复后（正确下载 JSON 数据）
async downloadFigures() {
  // 使用原生 fetch API 获取响应
  const token = localStorage.getItem('token')
  const response = await fetch('/api/figures/download', {
    headers: {
      'Authorization': `Bearer ${token}`
    }
  })
  
  if (!response.ok) {
    throw new Error('下载失败')
  }
  
  // 获取响应文本（JSON 字符串）
  const jsonText = await response.text()
  
  // 创建下载链接
  const blob = new Blob([jsonText], { type: 'application/json' })
  // ...
}
```

### 功能效果
- ✅ 修复了下载文件显示 `[object Object]` 的问题
- ✅ 下载的 JSON 文件包含完整的手办数据
- ✅ 数据格式正确，可读性强
- ✅ 保持原有的认证机制

### 用户体验优化
- **数据正确**: 下载的文件包含真实的手办数据
- **格式规范**: JSON 文件格式正确，便于查看和编辑
- **操作稳定**: 使用原生 fetch API，兼容性更好
- **错误处理**: 完善的错误提示，提升用户体验

## 9. 修复手办管理页面按钮大小不一致问题

### 问题原因
- 虽然 `.btn-add` 和 `.btn-download` 的 CSS 定义中 `padding` 和 `font-size` 相同
- 但 `.btn-download` 使用了 `display: flex`，而 `.btn-add` 没有
- 导致两个按钮的实际渲染尺寸不一致
- F12 检查显示："添加手办"按钮 64x23.333，与"下载数据"按钮尺寸不同

### 解决方案
- **统一布局方式**：为 `.btn-add` 也添加 `display: flex`
- **统一对齐方式**：两个按钮都添加 `align-items: center` 和 `justify-content: center`
- **固定高度**：统一设置 `height: 44px`，确保高度一致
- **盒模型**：添加 `box-sizing: border-box`，确保 padding 不影响总高度

### 变更内容
**文件**: `frontend/src/views/Figures.vue`

**修复前**:
```css
.btn-add {
  background-color: #4CAF50;
  color: white;
  padding: 12px 24px;
  font-size: 16px;
}

.btn-download {
  background-color: #ff9800;
  color: white;
  padding: 12px 24px;
  font-size: 16px;
  display: flex;
  align-items: center;
  gap: 8px;
}
```

**修复后**:
```css
.btn-add {
  background-color: #4CAF50;
  color: white;
  padding: 12px 24px;
  font-size: 16px;
  display: flex;
  align-items: center;
  justify-content: center;
  height: 44px;
  box-sizing: border-box;
}

.btn-download {
  background-color: #ff9800;
  color: white;
  padding: 12px 24px;
  font-size: 16px;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
  height: 44px;
  box-sizing: border-box;
}
```

### 功能效果
- ✅ "添加手办"按钮和"下载数据"按钮大小完全一致
- ✅ 两个按钮的高度统一为 44px
- ✅ 布局方式统一为 flex，对齐方式一致
- ✅ 视觉体验更加统一协调

### 用户体验优化
- **视觉统一**: 按钮大小一致，页面布局更加协调
- **专业感**: 统一的按钮样式提升整体专业度
- **一致性**: 相同功能的按钮保持一致的视觉表现
