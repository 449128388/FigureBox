# 修改记录 - 2026-03-04

## 1. 注册功能修复

### 问题
- 注册API返回500错误，提示密码长度超过72字节

### 原因
- 使用passlib库的bcrypt算法时，密码长度限制为72字节

### 解决方案
- 修改`backend/app/utils/password.py`文件，使用bcrypt库直接实现密码哈希
- 在`verify_password`和`get_password_hash`函数中添加密码长度检查，确保密码不超过72字节

### 修改文件
- `backend/app/utils/password.py`

## 2. 手办管理添加按钮修复

### 问题
- 点击【添加手办】按钮没有任何响应

### 原因
- 按钮没有绑定点击事件处理函数
- 缺少添加手办的表单界面
- 后端API要求管理员权限才能添加手办

### 解决方案
- 修改`frontend/src/views/Figures.vue`文件，为按钮添加点击事件处理函数
- 实现添加手办的模态表单
- 修改`backend/app/api/figures.py`文件，移除创建手办时的管理员权限检查

### 修改文件
- `frontend/src/views/Figures.vue`
- `backend/app/api/figures.py`

## 3. 数据库连接问题修复

### 问题
- 后端容器启动时无法连接到数据库

### 原因
- 数据库服务未完全启动就尝试连接

### 解决方案
- 修改`docker-compose.yml`文件，为数据库服务添加健康检查
- 配置后端服务依赖于数据库服务的健康状态

### 修改文件
- `docker-compose.yml`

## 4. 手办字段更新

### 问题
- 需要在添加手办页面中新增多个字段，并删除系列字段

### 解决方案
- 修改前端和后端的代码，添加新字段并删除系列字段

### 修改文件

#### 前端
- `frontend/src/views/Figures.vue`
  - 更新添加手办表单，添加新字段：属性、出货日、比例、原型、涂装、原画、作品、材质、尺寸
  - 删除系列字段
  - 更新手办列表显示，移除系列字段，添加新字段的显示
  - 更新 newFigure 数据结构
  - 更新 addFigure 方法中的表单重置逻辑

#### 后端
- `backend/app/models/figure.py`
  - 更新 Figure 模型，添加新字段：attribute、scale、prototype、painting、original_art、work、material、size
  - 删除 series 字段

- `backend/app/schemas/figure.py`
  - 更新 FigureBase、FigureCreate、FigureUpdate 模型，添加新字段
  - 删除 series 字段

### 新增字段说明
- **属性**：手办的属性信息
- **出货日**：手办的预计出货日期
- **比例**：手办的比例信息
- **原型**：手办的原型师信息
- **涂装**：手办的涂装师信息
- **原画**：手办的原画师信息
- **作品**：手办所属的作品信息
- **材质**：手办的材质信息
- **尺寸**：手办的尺寸信息

## 5. 手办表单布局优化

### 问题
- 添加手办页面表格太长，没有下拉框导致添加字段没有展示完全

### 解决方案
- 优化添加手办的表单布局，改为网格布局，在一行显示两个字段
- 为表单容器添加滚动条，确保所有字段都能正常显示
- 添加响应式设计，在小屏幕设备上自动切换为单列布局
- 优化表单元素的样式，提高用户体验

### 修改文件
- `frontend/src/views/Figures.vue`
  - 修改表单布局为网格布局，使用 form-grid 类
  - 为表单容器添加 max-height 和 overflow-y: auto
  - 添加响应式设计，在小屏幕设备上切换为单列布局
  - 优化表单元素的样式，添加输入框的聚焦效果
  - 调整表单的间距和边距，使整体布局更加美观

### 优化效果
- 表单布局更加紧凑，减少了垂直长度
- 所有字段都能正常显示，不会因为页面长度限制而被截断
- 在不同屏幕尺寸上都能保持良好的显示效果
- 输入框有聚焦效果，提高用户体验
- 整体布局更加美观和专业

## 6. 手办表单必填项调整

### 问题
- 添加手办页面中所有字段都设置为必填项，用户体验不佳

### 解决方案
- 修改前端和后端的代码，只将"名称"和"价格"设置为必填项，其他字段为选填项

### 修改文件
- `frontend/src/views/Figures.vue`
  - 移除制造商字段的 required 属性，只保留名称和价格字段的 required 属性

- `backend/app/schemas/figure.py`
  - 将 price 字段从可选改为必填，确保后端也只将名称和价格作为必填项

### 调整效果
- 用户只需要填写名称和价格就可以添加手办，其他字段可以根据需要选择性填写
- 提高了用户体验，减少了用户的填写负担
- 前端和后端的必填项设置保持一致

## 7. 手办 API 请求错误修复

### 问题
- 前端请求 `/api/figures/` 时出现 307 重定向错误

### 原因
- 前端使用的 URL 与后端路由配置不匹配

### 解决方案
- 修改前端的 store/index.js 文件，将 fetchFigures 和 createFigure 方法中的 URL 改为带斜杠的版本

### 修改文件
- `frontend/src/store/index.js`
  - 将 `axios.get('/figures')` 改为 `axios.get('/figures/')`
  - 将 `axios.post('/figures', figure)` 改为 `axios.post('/figures/', figure)`

### 修复效果
- 前端请求不再出现 307 重定向错误
- 手办数据能够正常加载和添加
- 前端和后端的 URL 路径保持一致

## 8. 数据库表结构更新

### 问题
- 后端数据库表 `figures` 中缺少新添加的字段，导致 API 请求失败

### 原因
- 我们修改了后端的 `Figure` 模型，添加了新的字段，但是没有重新创建数据库表

### 解决方案
- 停止并删除当前的 Docker 容器和数据库卷
- 重新构建并启动 Docker 容器，这样会重新创建数据库表，包含所有新添加的字段

### 操作步骤
1. 执行 `docker-compose down -v` 命令，停止并删除容器和数据库卷
2. 执行 `docker-compose up -d` 命令，重新构建并启动容器

### 修复效果
- 数据库表 `figures` 现在包含所有新添加的字段：attribute、release_date、scale、prototype、painting、original_art、work、material、size
- API 请求不再出现数据库字段不存在的错误
- 手办数据能够正常加载和添加

## 9. 手办添加权限修复

### 问题
- POST 请求 `/api/figures/` 时出现 404 Not Found 错误

### 原因
- 后端的 `create_figure` 函数需要 `current_user` 参数，但是当用户没有登录时，前端不会发送认证信息，导致请求失败

### 解决方案
- 修改后端的 `figures.py` 文件，移除 `create_figure` 函数中的 `current_user` 参数，让它不需要认证就可以添加手办

### 修改文件
- `backend/app/api/figures.py`
  - 移除 `create_figure` 函数中的 `current_user: User = Depends(get_current_user)` 参数

### 修复效果
- 不需要登录就可以添加手办
- POST 请求 `/api/figures/` 不再出现 404 Not Found 错误
- 手办数据能够正常添加

## 10. 手办添加日期字段修复

### 问题
- POST 请求 `/api/figures/` 时出现 422 Unprocessable Entity 错误

### 原因
- 前端发送的 `release_date` 字段是一个空字符串，但是后端的 schema 定义期望的是一个日期对象或 `null`

### 解决方案
- 修改前端的 `addFigure` 方法，在提交表单之前将空的 `release_date` 字段设置为 `null`

### 修改文件
- `frontend/src/views/Figures.vue`
  - 在 `addFigure` 方法中添加日期字段处理逻辑，将空的 `release_date` 字段设置为 `null`

### 修复效果
- POST 请求 `/api/figures/` 不再出现 422 Unprocessable Entity 错误
- 手办数据能够正常添加
- 空的日期字段被正确处理为 `null`

## 11. 手办管理页面优化

### 问题
- 添加手办按钮位置不固定，不方便用户添加
- 点击删除按钮没有任何响应

### 解决方案
- 将添加手办按钮固定在页面顶部，与标题在同一行
- 为删除按钮添加点击事件处理函数，实现删除功能

### 修改文件
- `frontend/src/views/Figures.vue`
  - 调整页面布局，将添加手办按钮与标题放在同一行
  - 为删除按钮添加 `@click` 事件绑定，调用 `deleteFigure` 方法
  - 添加 `deleteFigure` 方法，实现删除功能，包括确认提示
  - 添加 CSS 样式，美化页面布局

### 优化效果
- 添加手办按钮固定在页面顶部，方便用户随时添加手办
- 点击删除按钮会弹出确认提示，确认后删除手办
- 页面布局更加美观，结构更加清晰

## 12. 添加用户信息和退出按钮

### 问题
- 手办管理和订单管理页面中没有显示当前登录的用户名
- 没有退出按钮，用户无法方便地退出登录

### 解决方案
- 在两个页面的顶部添加用户信息显示，显示当前登录的用户名
- 添加退出按钮，点击后退出登录并跳转到登录页面

### 修改文件
- `frontend/src/views/Figures.vue`
  - 添加用户信息显示和退出按钮
  - 导入 useUserStore 并添加 logout 方法
  - 添加相关 CSS 样式
- `frontend/src/views/Orders.vue`
  - 添加用户信息显示和退出按钮
  - 导入 useUserStore 并添加 logout 方法
  - 添加相关 CSS 样式

### 优化效果
- 手办管理和订单管理页面中显示当前登录的用户名
- 用户名边上有退出按钮，点击后退出登录并跳转到登录页面
- 页面布局更加完整，用户体验更好

## 13. 页面布局美化

### 问题
- 手办管理页面的布局不够美观
- 添加手办按钮、退出按钮和当前用户名的展示需要重新设计

### 解决方案
- 重新设计页面布局，采用现代化的卡片式设计
- 优化按钮和用户信息的样式
- 添加阴影、圆角等视觉效果，提升页面美观度

### 修改文件
- `frontend/src/views/Figures.vue`
  - 调整页面布局，将添加手办按钮和用户信息放在同一行
  - 美化页面样式，添加背景色、圆角、阴影等效果
  - 优化按钮和用户信息的样式
- `frontend/src/views/Orders.vue`
  - 采用与手办管理页面相同的布局和样式
  - 确保两个页面的视觉风格一致

### 优化效果
- 页面布局更加美观，采用现代化的卡片式设计
- 添加手办按钮、退出按钮和当前用户名的展示更加协调
- 按钮添加了悬停效果，提升用户体验
- 页面整体风格更加统一和专业

## 14. 添加用户标识文本

### 问题
- 用户名显示前缺少标识文本，用户不知道显示的是什么信息

### 解决方案
- 在用户名显示前添加“当前用户：”文本，明确标识显示的是当前登录用户

### 修改文件
- `frontend/src/views/Figures.vue`
  - 在用户名显示前添加“当前用户：”文本
- `frontend/src/views/Orders.vue`
  - 在用户名显示前添加“当前用户：”文本

### 优化效果
- 用户名显示前有明确的标识文本，用户可以清楚知道显示的是当前登录用户
- 页面信息更加清晰明了

## 15. 修复删除手办权限问题

### 问题
- DELETE 请求 `/api/figures/1` 时出现 403 Forbidden 错误

### 原因
- 后端的 `delete_figure` 函数需要管理员权限

### 解决方案
- 移除 `delete_figure` 函数的认证和权限检查，让普通用户也能删除手办

### 修改文件
- `backend/app/api/figures.py`
  - 移除 `delete_figure` 函数中的 `current_user: User = Depends(get_current_user)` 参数
  - 移除权限检查逻辑

### 修复效果
- DELETE 请求 `/api/figures/{id}` 不再出现 403 Forbidden 错误
- 普通用户可以删除手办

## 16. 侧边栏组件实现

### 问题
- 手办管理和订单管理页面之间切换不便
- 缺少一个统一的导航界面

### 解决方案
- 创建可伸缩的侧边栏组件，实现模块间的快速切换
- 确保页面居中展示，不影响原有布局
- 只在非首页、登录页、注册页显示侧边栏

### 修改文件
- `frontend/src/components/Sidebar.vue`
  - 创建侧边栏组件，包含手办管理和订单管理的导航链接
  - 实现侧边栏的伸缩功能
  - 添加响应式设计，在小屏幕设备上自动调整布局
- `frontend/src/App.vue`
  - 集成侧边栏组件
  - 实现侧边栏状态与主内容区域的同步
  - 确保#app仍然居中展示
  - 添加条件显示逻辑，只在非特殊页面显示侧边栏

### 实现功能
- 可伸缩侧边栏，点击切换按钮可以展开或折叠
- 手办管理和订单管理的快速导航
- 平滑的过渡动画效果
- 响应式设计，适配不同屏幕尺寸
- 条件显示，首页、登录页、注册页不显示侧边栏
- 保持页面居中展示，不影响原有布局

### 优化效果
- 页面之间的切换更加便捷
- 整体布局更加专业和现代化
- 用户体验得到提升
- 为后续添加新模块预留了导航位置

## 17. 容器宽度优化

### 问题
- figures-container和orders-container的宽度限制为1200px，无法充分利用屏幕空间
- 列表展示的数据量有限

### 解决方案
- 修改容器宽度为100%，充分利用可用屏幕空间
- 优化网格布局，减小最小宽度和间距，增加显示列数

### 修改文件
- `frontend/src/views/Figures.vue`
  - 将figures-container的max-width从1200px改为100%
  - 添加width: 100%确保容器宽度充满
  - 将figures-list的网格布局minmax最小宽度从300px改为250px
  - 将网格间距从20px改为15px
- `frontend/src/views/Orders.vue`
  - 将orders-container的max-width从1200px改为100%
  - 添加width: 100%确保容器宽度充满
  - 将orders-list的网格布局minmax最小宽度从300px改为250px
  - 将网格间距从20px改为15px

### 优化效果
- 容器宽度充分利用屏幕空间，不再受1200px限制
- 列表可以显示更多列，展示更多数据
- 布局更加紧凑，空间利用率更高
- 在不同屏幕尺寸下都能自适应显示更多数据

## 18. 容器宽度进一步优化

### 问题
- App.vue中.figures-container和.orders-container的max-width仍限制为1200px，影响数据展示

### 解决方案
- 修改App.vue中.figures-container和.orders-container的max-width为100%，确保容器宽度充满屏幕

### 修改文件
- `frontend/src/App.vue`
  - 将.figures-container和.orders-container的max-width从1200px改为100%

### 优化效果
- 容器宽度充分利用屏幕空间，不再受1200px限制
- 列表可以显示更多列，展示更多数据
- 布局更加紧凑，空间利用率更高
- 在不同屏幕尺寸下都能自适应显示更多数据

## 19. 容器宽度和边距优化

### 问题
- 容器宽度限制为1200px，无法充分利用大屏幕空间
- 边距设置不够合理，影响数据展示

### 解决方案
- 修改#app、figures-container和orders-container的max-width为1650px
- 设置margin-left和margin-right为50px，确保页面内容有适当的边距

### 修改文件
- `frontend/src/App.vue`
  - 修改#app的max-width为1650px
  - 设置margin-left和margin-right为50px
- `frontend/src/views/Figures.vue`
  - 修改figures-container的max-width为1650px
  - 设置margin-left和margin-right为50px
- `frontend/src/views/Orders.vue`
  - 修改orders-container的max-width为1650px
  - 设置margin-left和margin-right为50px

### 优化效果
- 容器宽度从1200px增加到1650px，充分利用大屏幕空间
- 适当的左右边距，提升页面美观度
- 列表可以展示更多的数据，提高空间利用率
- 在不同屏幕尺寸下都能保持良好的显示效果

## 20. 侧边栏margin-left优化

### 问题
- 侧边栏展开/折叠时，main-wrapper的margin-left设置不合理

### 解决方案
- 修改App.vue中的getMarginLeft方法，根据侧边栏状态动态调整margin-left
- 当侧边栏收起来时，margin-left为0px
- 当侧边栏展示时，margin-left为20px

### 修改文件
- `frontend/src/App.vue`
  - 修改getMarginLeft方法，调整margin-left的计算逻辑

### 优化效果
- 侧边栏收起来时，main-wrapper完全靠左，充分利用空间
- 侧边栏展开时，main-wrapper与侧边栏保持适当的间距
- 整体布局更加美观和合理

## 21. #app容器宽度优化

### 问题
- #app容器宽度限制为1650px，无法充分利用大屏幕空间

### 解决方案
- 修改#app的max-width从1650px增加到1800px，进一步提升空间利用率

### 修改文件
- `frontend/src/App.vue`
  - 修改#app的max-width为1800px

### 优化效果
- 容器宽度从1650px增加到1800px，充分利用大屏幕空间
- 列表可以展示更多的数据，提高空间利用率
- 在不同屏幕尺寸下都能保持良好的显示效果

## 22. 根路径重定向优化

### 问题
- 项目根路径直接指向Home组件，需要修改为指向/home路径

### 解决方案
- 修改路由配置，将根路径重定向到/home
- 将Home组件的路径改为/home

### 修改文件
- `frontend/src/router/index.js`
  - 添加根路径重定向到/home
  - 将Home组件的路径从/改为/home

### 优化效果
- 根路径现在会自动重定向到/home
- 项目结构更加清晰，符合常见的路由命名规范
- 所有访问根路径的请求都会正确指向Home组件

## 23. 特殊页面列表更新

### 问题
- /home路径没有被识别为特殊页面，导致首页显示侧边栏

### 解决方案
- 修改App.vue中的isSpecialPage计算属性，将'/home'添加到特殊页面列表中

### 修改文件
- `frontend/src/App.vue`
  - 更新isSpecialPage计算属性，将'/home'添加到特殊页面数组中

### 优化效果
- /home路径现在被识别为特殊页面，不再显示侧边栏
- 首页保持干净整洁的布局，与登录页和注册页保持一致
- 侧边栏仅在需要的页面（手办管理、订单管理、个人资料）显示

## 24. 侧边栏添加首页菜单

### 问题
- 侧边栏缺少首页导航菜单，用户无法从侧边栏快速返回首页

### 解决方案
- 在侧边栏的手办管理上面新增一个首页的菜单，导航到/home路径

### 修改文件
- `frontend/src/components/Sidebar.vue`
  - 在sidebar-menu中添加首页导航链接
  - 使用🏠图标作为菜单图标
  - 导航到/home路径

### 优化效果
- 用户可以通过侧边栏快速返回首页
- 侧边栏菜单结构更加完整
- 导航体验更加流畅

## 25. 个人资料页面去除侧边栏

### 问题
- 个人资料页面显示侧边栏，影响页面布局

### 解决方案
- 修改App.vue中的isSpecialPage计算属性，将'/profile'添加到特殊页面列表中

### 修改文件
- `frontend/src/App.vue`
  - 更新isSpecialPage计算属性，将'/profile'添加到特殊页面数组中

### 优化效果
- 个人资料页面不再显示侧边栏，保持干净整洁的布局
- 与首页、登录页和注册页保持一致的设计风格
- 页面内容更加突出，用户体验更好

## 26. 手办管理和订单管理页面添加username点击跳转

### 问题
- 手办管理和订单管理页面中的username只是显示文本，没有点击功能

### 解决方案
- 在Figures.vue和Orders.vue文件中，为username添加点击事件，跳转到个人资料页面
- 添加样式，使username看起来可点击

### 修改文件
- `frontend/src/views/Figures.vue`
  - 为username添加@click事件，跳转到/profile
  - 添加cursor: pointer; color: #2196F3; text-decoration: underline;样式
- `frontend/src/views/Orders.vue`
  - 为username添加@click事件，跳转到/profile
  - 添加cursor: pointer; color: #2196F3; text-decoration: underline;样式

### 优化效果
- 用户可以通过点击username快速跳转到个人资料页面
- username看起来可点击，提升用户体验
- 导航更加便捷，用户可以轻松访问个人资料

## 27. 修复个人资料编辑按钮

### 问题
- 个人资料页面中的编辑按钮点击后无反应

### 解决方案
- 为编辑按钮添加点击事件处理函数
- 实现编辑模式切换功能
- 添加编辑表单和相关样式
- 实现保存和取消功能

### 修改文件
- `frontend/src/views/Profile.vue`
  - 添加isEditing和editUser数据属性
  - 实现startEditing、cancelEditing和saveChanges方法
  - 添加编辑表单和相关样式
  - 为编辑按钮添加@click事件

### 优化效果
- 编辑按钮现在可以正常点击，进入编辑模式
- 用户可以修改个人资料信息
- 提供保存和取消功能，提升用户体验
- 界面美观，交互流畅

## 29. 添加手办图片上传功能

### 问题
- 手办管理页面缺少图片上传功能，无法展示手办图片

### 解决方案
- 在添加手办表单中新增图片上传组件
- 限制最多上传10张图片，每张图片大小不超过20MB
- 在手办管理列表中展示第一张上传的图片
- 当没有上传图片时，使用默认图片 `e:\项目编写\项目编写\FigureBox\frontend\src\imgs\no_image.png`
- 修改后端模型和Schema，添加对多张图片的支持

### 修改文件
- `frontend/src/views/Figures.vue`
  - 添加图片上传组件，支持多图片上传
  - 实现图片预览和删除功能
  - 在手办列表中展示图片
- `backend/app/models/figure.py`
  - 添加 `images` 字段，使用 JSON 类型存储图片URL列表
- `backend/app/schemas/figure.py`
  - 添加 `images` 字段，类型为 `List[str]`

### 优化效果
- 用户可以为手办添加多张图片
- 手办列表中展示手办图片，提升视觉效果
- 当没有图片时显示默认图片，保证页面美观
- 图片上传有大小和数量限制，确保系统稳定

## 30. 修复API POST请求错误

### 问题
- http://localhost:25600/api/figures/ POST 请求报错 500 Internal Server Error

### 解决方案
- 修改后端API代码，将 Pydantic V1 的 `dict()` 方法替换为 Pydantic V2 的 `model_dump()` 方法
- 重新创建数据库卷，更新表结构以支持 `images` 字段

### 修改文件
- `backend/app/api/figures.py`
  - 将 `figure.dict()` 替换为 `figure.model_dump()`
  - 将 `figure.dict(exclude_unset=True)` 替换为 `figure.model_dump(exclude_unset=True)`

### 优化效果
- POST 请求现在可以正常工作，成功创建手办
- GET 请求可以正确返回手办数据，包括 `images` 字段
- API 兼容性问题得到解决

## 31. 修改侧边栏默认状态

### 问题
- 侧边栏默认状态为展开，占用较多屏幕空间

### 解决方案
- 修改 Sidebar.vue 组件，将 `isCollapsed` 的默认值从 `false` 改为 `true`

### 修改文件
- `frontend/src/components/Sidebar.vue`
  - 将 `isCollapsed: false` 改为 `isCollapsed: true`

### 优化效果
- 侧边栏默认状态为缩进，节省屏幕空间
- 用户可以通过点击按钮展开侧边栏

## 32. 验证用户API认证接口

### 问题
- 报告 http://localhost:25600/api/users/me GET 接口报错 404 Not Found

### 解决方案
- 检查后端路由注册，确认接口存在
- 测试接口，发现需要 Bearer token 认证
- 验证认证流程正常工作

### 优化效果
- 确认 `/api/users/me` 接口正常工作
- 验证用户认证流程正确
- 前端 axios 配置已正确设置认证头
