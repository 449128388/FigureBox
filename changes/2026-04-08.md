# 2026-04-08 更新日志

## 今日更新

- **修复资产分布饼图显示问题**：
  - 问题：控制台显示 `pieChart.value` 为 null，DOM 元素未渲染完成
  - 原因：return 语句中缺少 `pieChart` 和 `profitChart` 的暴露，导致模板中的 ref 无法正确绑定
  - 解决方案：
    - 在 return 语句中添加 `pieChart` 和 `profitChart` 的暴露
    - 使用 setTimeout 延迟初始化，确保 DOM 完全渲染
    - 添加双重 setTimeout 作为备用方案
  - 修改文件：
    - `frontend/src/views/Dashboard.vue`

- **清理调试代码**：
  - 删除所有用于排查饼图问题的 console.log 调试语句
  - 修改文件：
    - `frontend/src/views/Dashboard.vue`
