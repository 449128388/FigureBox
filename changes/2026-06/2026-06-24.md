# 2026年6月24日 更新日志

## 修复

### 1. 动态流 BUY 事件详情页新增操作按钮

**解决方案：** 动态流 BUY 事件详情页（等待补款状态）缺少操作按钮，用户无法直接跳转到订单管理进行补款操作。

**实现逻辑：**
- **查看订单详情按钮**：在 BUY 事件详情新增"查看订单详情"按钮，点击后跳转到尾款管理页面（`/orders`）
- **去补款按钮**：在 BUY 事件详情新增"去补款"按钮，点击后跳转到尾款管理页面并自动打开对应订单的编辑表单
- 按钮仅在 `detailData.status === '等待补款'` 时显示
- 跳转逻辑通过路由参数 `editOrderId` 传递，尾款管理页面通过 `watch` 监听 `filteredOrders` 加载完成后自动触发编辑
- 按钮样式参考 `activity_feed_component.html` 中的 `detail-btn` / `detail-btn-primary` 样式

### 结果
- 等待补款的订单在动态流详情中可一键跳转到尾款管理
- 点击"去补款"自动打开编辑表单，简化操作路径

---

### 2. activity_feed 表全面采用 Append-only 设计

**解决方案：** activity_feed 表中存在修改已有记录的操作（`update_buy_event`、`update_sell_event`），违反了动态流作为不可变事件日志的设计原则。同时手办标签变更时仅记录增量差异而非全量快照。

**实现逻辑：**
- **移除所有更新操作**：删除 `update_buy_event` 和 `update_sell_event` 方法，改为追加新记录的 `record_buy_update_event` 和 `record_sell_update_event`
- **编辑订单时**：不再修改原有的 BUY/SELL 事件，而是创建一条新的同类型事件记录，保留完整历史快照
- **手办标签变更**：从增量差异（diff）记录改为全量快照记录，每次标签变更时记录当前全部标签

### 结果
- activity_feed 表成为不可变的事件日志，历史记录永不修改
- 每次编辑订单都会创建新的事件快照，完整记录变更历史
- 标签事件记录当前全部标签而非仅增量

---

### 3. 动态流 BUY 事件编辑时标题反映具体变更内容

**解决方案：** 编辑尾款管理中的订单时，`record_buy_update_event` 生成的事件标题始终为"入手「XXXX」"，未反映定金/尾款的金额或币种实际变更情况。且原实现仅支持单个字段变更，多个字段同时变更时标题不完整。

**实现逻辑：**
- `record_buy_update_event` 新增 `old_deposit`、`old_deposit_currency`、`old_balance`、`old_balance_currency` 四个旧值参数
- 独立检测定金组（金额或币种任一变化）和尾款组（金额或币种任一变化），分别生成描述字符串
- 仅定金变化 → `「XXXX」定金发生变动，定金 {旧符号}{旧值} 修改为 {新符号}{新值}`
- 仅尾款变化 → `「XXXX」尾款发生变动，尾款 {旧符号}{旧值} 修改为 {新符号}{新值}`
- 定金和尾款同时变化 → `「XXXX」订单定金和尾款发生变动，定金 {旧符号}{旧值} 修改为 {新符号}{新值}, 尾款 {旧符号}{旧值} 修改为 {新符号}{新值}`
- `order_crud_service.py` 的 `update_order` 调用时传入已记录的旧值

### 结果
- 支持定金/尾款金额、币种任意组合变更的标题展示
- 多个字段同时变更时标题完整描述所有变更点

---

### 4. 动态流 BUY 事件列表缺少关键字段颜色标记

**解决方案：** 动态流全部动态列表中，BUY 事件的标题仅有手办名称高亮，金额数字（定金/尾款数值）为普通文本，缺少视觉区分。

**实现逻辑：**
- `formatEventTitle` 中新增 BUY 事件专属处理分支
- 金额数字（含币种符号 $/¥/€ 等）使用红色标记，与 SELL 事件的价格高亮一致

### 结果
- BUY 事件列表中金额数字带红色标记，与 SELL/OUT/TAG 等事件视觉风格统一

---

### 5. 修复订单状态变更为"已支付"时动态流未记录尾款付清事件

**解决方案：** 在尾款管理中将订单状态从"未支付"变更为"已支付"时，动态流仍然记录为 BUY 事件且标题为"入手「XXXX」，等待补款"，但实际尾款已付清，应记录为 FULL_PAY 事件。

**实现逻辑：**
- **后端**：
  - `record_full_pay_event` 新增 `deposit_paid`、`character`、`scale`、`maker` 参数，`status` 改为"等待出荷"
  - `update_order` 捕获变更前状态，当最终状态为"已支付"时调用 `record_full_pay_event` 记录尾款付清事件，其他状态（未支付/已完成/已取消）仍使用 `record_buy_update_event`
  - FULL_PAY 事件标题：`「XXXX」尾款已付清，等待出荷`
- **前端**：FULL_PAY 详情页更新为与 HTML 参考一致，新增定金金额、本次尾款字段，状态标签改为绿色

### 结果
- 订单变为"已支付"时动态流正确记录 FULL_PAY 事件
- 详情页完整展示定金、尾款、累计支付等信息

---

### 6. 动态流 FULL_PAY 事件详情新增订单进度追踪

**解决方案：** 动态流 FULL_PAY 事件详情页缺少"订单进度追踪"展示，用户无法直观了解当前订单在完整生命周期（定金→尾款→出荷→入库）中的进度位置。

**实现逻辑：**
- 在 FULL_PAY 详情中新增"订单进度追踪"区块
- 进度包含四个阶段：定金已付（completed）、尾款已付清（current）、工厂出荷（pending）、入库（pending）
- 定金已付使用已购圆点样式，尾款已付清使用当前步骤样式（绿色高亮），后续步骤为灰色
- CSS 样式参考 `activity_feed_same_figure.html` 的 status-timeline 系列样式

### 结果
- FULL_PAY 事件详情页展示订单进度追踪，用户可直观了解订单当前所处阶段

---

### 7. 动态流详情弹窗尺寸放大适配新增内容

**解决方案：** FULL_PAY 事件详情新增订单进度追踪后，弹窗内容增多，原有尺寸（600px宽 / 85vh高）偏小，需要适当放大。

**实现逻辑：**
- 弹窗宽度从 `92%` → `95%`，最大宽度从 `600px` → `720px`
- 最大高度从 `85vh` → `90vh`

### 结果
- 详情弹窗有更充裕的空间展示订单进度追踪信息

---

### 8. 修复订单进度追踪中工厂出荷日期为空

**解决方案：** FULL_PAY 事件详情页的订单进度追踪中"工厂出荷"日期始终显示为"待确认"，原因是 `record_full_pay_event` 未将 `due_date`（出荷日期）写入 detail_data。

**实现逻辑：**
- `record_full_pay_event` 新增 `due_date` 参数，写入 detail_data
- `order_crud_service.py` 中调用时传入 `db_order.due_date`
- 前端展示规则：有 due_date 时显示"预计 YYYY-MM-DD"，无 due_date 时显示"待确认"

### 结果
- 工厂出荷日期正确显示订单的出荷日期

---

### 9. 修复 FULL_PAY 事件详情页数据样式不统一

**解决方案：** FULL_PAY 事件详情页中订单编号、定金金额、本次尾款、累计支付、支付时间五个数据字段的字体颜色、行高与 BUY 事件已统一的样式不一致。

**实现逻辑：**
- 五个数据字段的 `<span>` 新增 `buy-detail-value` CSS 类，与 BUY 事件数据字段样式保持一致
- 移除"本次尾款"和"累计支付"原有的 `accent`/`green` 彩色样式类
- "当前状态"和"订单进度追踪"保持原样式不变

### 结果
- FULL_PAY 事件详情页五个数据字段样式与 BUY 事件统一

---

### 10. 修复订单状态变更为"已完成"时动态流未记录入库事件

**解决方案：** 在尾款管理中将订单状态从"未支付"变更为"已完成"时，动态流仍然记录 BUY 事件，但实际已确认收货，应记录为 IN_STOCK 事件。

**实现逻辑：**
- `update_order` 动态流同步新增 `elif db_order.status == "已完成"` 分支，调用 `record_in_stock_event`
- `record_in_stock_event` 新增 `target_id`、`target_type` 参数，detail_data 新增 `status: "完成入库"`
- 前端 IN_STOCK 详情新增"当前状态"字段和"查看手办详情"按钮，数据字段样式与 BUY 事件统一

### 结果
- 订单变为"已完成"时动态流正确记录 IN_STOCK 事件
- 详情页展示入库信息、完成入库状态和操作按钮

---

### 11. 消除进入收藏家模式时动态流的重复请求

**解决方案：** 进入收藏家模式后浏览器先后请求了两个相同的 timeline 接口（`/collector/timeline` 和 `/collector/timeline?event_type=all&offset=0&limit=20`），造成重复数据传输。

**实现逻辑：**
- 根因分析：`useCollectorData.js` 的 `fetchCollectorData` 中并行请求了 `/collector/timeline`，但该数据从未被任何组件消费——动态流由 `ActivityFeed` 组件通过 `useActivityFeed` 独立加载
- 移除 `useCollectorData.js` 中的 `/collector/timeline` 请求，只保留 summary、cabinets、tags 三个请求
- 移除缓存中的 `activities` 字段及 mock 数据中的重复定义

### 结果
- 进入收藏家模式后只请求一次 timeline 接口
- 减少了一次无用的 HTTP 请求和数据传输

---

### 12. 移除收藏家模式请求失败时的模拟数据

**解决方案：** API 请求失败时 catch 块使用硬编码的模拟数据填充，导致用户无法感知后端错误。

**实现逻辑：**
- 移除 catch 块中的模拟数据对象（summary/valuable_items/tags）
- 替换为打印错误日志、显示错误提示弹窗、`collectorData` 置为 null
- 前端组件通过 `v-if="collectorData"` 判断数据是否有效

### 结果
- API 请求失败时显示错误提示，不再展示虚假的模拟数据

---

### 13. 优化收藏柜接口每个分类返回上限 20 条

**解决方案：** `/api/collector/cabinets` 接口响应体积高达 23MB，原因是 8 个分类的 items 数组无限制返回全部数据。

**实现逻辑：**
- 新增 `CABINET_ITEMS_LIMIT = 20` 常量
- 新增 `sliced_items()` 辅助函数，对 items 数组截取前 20 条
- 8 个分类的 `items` 均使用 `sliced_items()`，`count` 保持全量总数
- `meta` 底部文案使用全量 `count`，`companion_days` 基于截取后的 items 计算

### 结果
- 每个分类最多返回 20 条 items，响应体积减少 90% 以上
- 前端橱窗卡片展示不受影响（单页展示一般不超过 20 条）

---

### 14. IN_STOCK 事件详情入柜位置改为实时计算

**解决方案：** 动态流 IN_STOCK 事件的"入柜位置"字段使用 detail_data 的静态快照值，与实际收藏柜分类不一致，需要实时计算手办当前所属的藏品柜。

**实现逻辑：**
- 后端 `_get_figure_cabinets()`：根据 cabinets.py 的业务规则（持有天数、30天入库、修复标签、卖出记录、订单状态、库存数量等），实时判断手办属于哪些收藏柜
- `get_event_detail` 中 IN_STOCK 事件额外返回 `figure_cabinets` 字段
- 前端 `ActivityFeed.vue` 优先使用动态计算的 `figureCabinets`，无数据时回退到静态 `detailData.cabinet`

### 结果
- 入柜位置实时反映手办当前所属的收藏柜分类
- 支持同时属于多个收藏柜（如"最近入柜 / 复数专区"）

---

### 15. 修复订单变更为"已取消"时尾款记录和动态流事件类型错误

**解决方案：** 订单状态变更为"已取消"时，order_transactions 表可能错误地创建尾款记录，且 activity_feed 表记录为 BUY 类型而非 ORDER_CANCEL 类型。

**实现逻辑：**
- order_transactions：定金记录按独立检查（存在则跳过），尾款记录对"已取消"状态跳过不创建
- activity_feed：`create_order` 和 `update_order` 中"已取消"状态独立调用 `record_order_cancel_event`（ORDER_CANCEL 类型），不再走 BUY 事件分支

### 结果
- 已取消订单不创建尾款资金流水，定金记录不受影响
- 动态流正确记录 ORDER_CANCEL 事件，标题显示"取消「XXXX」订单"

---

### 16. 完善 ORDER_CANCEL 事件 detail_data 状态和前端详情展示

**解决方案：** ORDER_CANCEL 事件的 detail_data 缺少 `status` 字段，且前端详情页缺少手办卡片和作品/比例/制造商信息。

**实现逻辑：**
- `record_order_cancel_event` 的 detail_data 新增 `"status": "取消订单"`
- `showFigureCard` 数组新增 `'order_cancel'`，使取消事件详情页展示手办卡片
- ORDER_CANCEL 详情模板数据字段统一使用 `buy-detail-value` 样式，取消时间取 `eventDetail.created_at`，状态使用动态 `detailData.status`

### 结果
- ORDER_CANCEL 事件详情页展示手办卡片和作品/比例/制造商信息
- 状态字段正确显示"取消订单"

---

### 17. ORDER_CANCEL 事件取消原因取自 remarks、详情新增查看手办详情按钮

**解决方案：** ORDER_CANCEL 事件 detail_data 的 `cancel_reason` 始终为空，未从订单的 `remarks` 字段取值；同时详情页缺少操作按钮。

**实现逻辑：**
- `create_order` 和 `update_order` 中调用 `record_order_cancel_event` 时传入 `cancel_reason=db_order.remarks`
- ORDER_CANCEL 详情模板新增"查看手办详情"按钮

### 结果
- 取消原因正确显示订单备注内容
- 详情页可一键跳转到手办详情页

---

### 18. 修改手办市场价时动态流新增 PRICE_UPDATE 事件

**解决方案：** 在手办管理模块中修改手办的 `market_price`（市场价）后，activity_feed 表没有记录对应的价格变动事件。

**实现逻辑：**
- `collector_activity_service.py` 新增 `record_price_update_event` 方法，记录 PRICE_UPDATE 类型事件
- `figure_crud_service.py` 的 `update_figure` 中捕获变更前的 `old_market_price` 和 `old_market_currency`
- 更新后检测新旧市场价是否不同，不同则调用 `record_price_update_event`
- 事件标题格式：`「手办名」市场价更新：¥旧价 → ¥新价`

### 结果
- 修改手办市场价后动态流正确记录 PRICE_UPDATE 事件
- 详情展示原价、新价、变动金额和变动幅度百分比

---

### 19. 修复市场价不变仅币种变化时未记录动态流事件

**解决方案：** 修改市场价时仅变更币种（如 CNY→USD）或价格+币种同时变更时，activity_feed 未记录对应事件。

**实现逻辑：**
- `record_price_update_event` 新增 `old_currency` 和 `new_currency` 两个币种参数，标题分别使用旧币种符号和新币种符号
- `figure_crud_service.py` 检测条件从仅判断价格变化改为价格或币种任一变化均触发
- PRICE_UPDATE 详情页更新：使用动态货币符号替代硬编码 ¥，新增 `getCurrencySymbol()` 函数

### 场景覆盖
| 价格变化 | 币种变化 | 标题示例 |
|:---:|:---:|---------|
| ✅ | ❌ | `市场价更新：¥2000 → ¥3000` |
| ❌ | ✅ | `市场价更新：¥2000 → $2000` |
| ✅ | ✅ | `市场价更新：¥2000 → $3000` |

---

### 20. PRICE_UPDATE 事件详情新增手办卡片展示

**解决方案：** PRICE_UPDATE 事件详情页缺少手办图片和作品/比例/制造商信息，与其他事件类型展示不一致。

**实现逻辑：**
- `showFigureCard` 数组新增 `'price_update'`，使价格变动事件详情页展示手办卡片

### 结果
- PRICE_UPDATE 事件详情页正确展示手办图片和信息

---

### 21. PRICE_UPDATE 事件详情新增查看手办详情按钮

**解决方案：** PRICE_UPDATE 事件详情页缺少操作按钮，用户无法从动态流直接跳转到手办管理页。

**实现逻辑：**
- PRICE_UPDATE 详情底部新增"查看手办详情"按钮

### 结果
- PRICE_UPDATE 事件详情页可一键跳转到手办管理页

---

### 22. 修复 PRICE_UPDATE 币种变更时变动金额/幅度计算错误

**解决方案：** 市场价仅变更币种（如 ¥2000 → $2000）或价格+币种同时变更时，变动金额直接使用原数值相减，但不同币种不能直接相减。

**实现逻辑：**
- 新增 `PRICE_EXCHANGE_RATES` 汇率常量（与 asset_core_calculations.py 一致）
- `record_price_update_event` 中先将旧价和新价按汇率统一折算为人民币，再计算变动金额和变动幅度
- 前端变动金额符号固定为 `¥`（变动值始终为人民币）

### 场景示例
| 旧 | 新 | 变动金额（旧） | 变动金额（新） |
|---|---|---|---|
| ¥2000 | ¥3000 | 直减 1000 | 折算CNY 1000 |
| ¥2000 | $2000 | 直减 0（错误） | 折算CNY 12000 |
| ¥2000 | $3000 | 直减 1000（错误） | 折算CNY 19000 |

---

### 23. PRICE_UPDATE 变动金额/幅度的颜色调整为涨红跌绿

**解决方案：** 原配色涨为绿色、跌为红色不符合中国人的习惯（红色代表上涨、绿色代表下跌）。

**实现逻辑：**
- 变动金额和变动幅度颜色的三元表达式从 `>= 0 ? 'green' : 'red'` 改为 `>= 0 ? 'red' : 'green'`

### 结果
- 上涨显示红色（📈），下跌显示绿色（📉）

---

### 24. SELL 事件标题新增"已完成"和"退款/纠纷"状态支持

**解决方案：** SELL 事件标题未覆盖"已完成"和"退款/纠纷"两种状态，前端展示不准确。

**实现逻辑：**
- `record_sell_event` 和 `record_sell_update_event` 新增 `refund_amount` 参数
- 新增状态处理：
  - "已完成"：`「xxxxx」订单交易完成，买家已签收，售价 ¥X（盈利/亏损 ¥X）`
  - "退款/纠纷"：`「xxxxx」订单产生售后争议，已完成退款，退款金额 ¥X`
- detail_data 新增 `refund_amount` 字段
- 调用处传入 `refund_amount=0`

### 结果
- 各状态的 SELL 事件标题准确反映实际业务状态

---

### 25. 修复分享海报和隐私设置按钮无法响应

**解决方案：** 点击[分享海报]和[隐私设置]按钮无响应，原因是新组件的 `v-model` 绑定方式与组件内定义的 prop 名称不匹配。

**实现逻辑：**
- 根治：`SharePoster` 和 `PrivacySettings` 组件使用 `visible` prop 控制显隐
- `Dashboard.vue` 中 `v-model` 改为 `v-model:visible`，明确绑定到 `visible` prop

### 结果
- 点击按钮正确打开对应弹窗并调用后端接口

---

### 26. 分享海报新增"藏品陈列"模板

**解决方案：** 分享海报工具只提供了"资产概览"和"极简数据"两种模板，缺少参考设计中的"藏品陈列"（gallery）模板。

**实现逻辑：**
- 模板数组中新增 `gallery` 条目（名称为"藏品陈列"，描述为"手办拼图展示"）
- 新增 `topFigures` 计算属性，从收藏柜数据中提取去重的手办列表
- 新增 `poster-gallery-grid` 3列网格布局展示手办图标
- 新增特征手办展示区（名称、陪伴天数）
- 新增 gallery 背景色渐变样式和 grid/item/feature 样式

### 结果
- 分享海报支持 3 种模板：资产概览、藏品陈列、极简数据

---

### 27. 隐私设置对接海报生成 — 影响藏品总数/资产金额/名称显示

**解决方案：** 隐私设置修改后未正确影响海报生成内容，没有按用户设置的可见性规则过滤海报数据。

**实现逻辑：**
- `showStats`（藏品总数=关）：海报中藏品数量显示为 `--`，gallery 模板的副标题置空
- `showAmount`（资产金额=关）：海报中不显示任何价格/资产数据
- `showNames`（海报展示数据=仅统计）：不展示具体藏品名称
- `showAmount` 需同时满足 `海报展示数据=完整数据` 且 `资产金额=开` 才会展示

### 结果
- 隐私设置各开关正确影响海报的对应数据展示区域

---

### 28. 隐私设置新增"二维码域名/IP"配置项

**解决方案：** 海报二维码 URL 自动从请求地址获取，但当内网/外网环境不同时，自动检测的地址可能不正确，需要用户手动配置。

**实现逻辑：**
- 后端 `collector_privacy` 表新增 `share_domain` 字段，API 允许读写该字段
- `generate_share_url` 优先使用用户配置的 `share_domain` 拼接 URL：`http://{share_domain}/share/{user_id}?token=xxx`
- 未配置时回退到 `request.base_url` 自动检测
- 前端 PrivacySettings 分享设置区新增输入框，placeholder 示例 `192.168.1.100:25600`

### 结果
- 用户可手动配置二维码域名/IP，扫码后正确跳转到指定地址

---

### 29. 放大分享海报弹窗尺寸

**解决方案：** 分享海报弹窗尺寸偏小，海报预览和模板选择区域视觉上不够舒适。

**实现逻辑：**
- 弹窗最大宽度从 `480px` 扩大到 `640px`
- 海报画布从 `280x420` 扩大到 `320x480`，预览区 padding 增大
- 二维码从 `64px` 增大到 `80px`
- 标题和副标题字号略微增大

### 结果
- 分享海报弹窗整体视觉更舒适

---

### 30. 修复分享主页二维码扫码后提示"链接已失效"

**解决方案：** 扫描海报二维码进入分享主页后，API 请求失败返回 500 错误，前端误判为 403 显示"链接已失效"。

**根因：** `get_shared_profile` 中使用了 `user.nickname`，但 User 模型没有 `nickname` 字段（只有 `username`），导致 `AttributeError` 未捕获，FastAPI 返回 500。

**实现逻辑：**
- `user.nickname` 改为 `user.username`（User 模型实际字段名）

### 结果
- 扫描二维码后正确显示收藏家分享主页

---

### 31. 放大海报二维码尺寸方便扫码

**解决方案：** 海报中二维码太小，实际扫码时难以识别。

**实现逻辑：**
- 二维码生成的像素尺寸从 `128px` 提升到 `200px`，白边从 `1` 增大到 `2`，提高识别率
- CSS 显示尺寸从 `80px` 增大到 `120px`

### 结果
- 海报二维码更清晰，扫码成功率提升

---

### 32. 进一步提升海报二维码像素和显示尺寸

**解决方案：** 上一轮调整后二维码仍然较难扫出，像素和显示尺寸仍偏小。

**实现逻辑：**
- 生成像素从 `200px` 提升到 `400px`，白边从 `2` 增大到 `4`，大幅提高点阵密度
- CSS 显示尺寸从 `120px` 增大到 `180px`，接近画布宽度的一半
- 提示文字从 `10px` 增大到 `12px`

### 结果
- 海报二维码更易识别，扫码成功率显著提升

---

### 33. 修复保存海报图片分辨率低、模糊

**解决方案：** 点击"保存图片"下载的海报截图以 CSS 像素尺寸（320×480）输出，放大后模糊不清。

**实现逻辑：**
- 改用 SVG → Canvas 管线：先用 `domtoimage.toSvg` 输出无损矢量图，再绘制到 3 倍大小的 Canvas 上导出 PNG
- 内容等比例放大，而非单纯扩大画布尺寸

### 结果
- 保存的海报图片清晰度高，放大后无锯齿

---

### 34. 修复保存隐私设置导致二维码 URL 被重置

**解决方案：** 每次生成海报都调用分享链接 API，导致旧二维码立即失效（token 被替换）。

**根因：** `generate_share_url` 每次都生成新 token 并递增 `token_version`。由于 token 仅存哈希值（单向），无法复原原始值用于重建 URL，所以每次只能重新生成。

**实现逻辑：**
- `collector_privacy` 表新增 `share_token_raw` 字段，存储原始 token 值
- `generate_share_url` 检查现有 token 是否未过期，有效则直接复用 `share_token_raw`
- 仅当从未生成或已过期时才创建新 token
- `reset_share_token` 同时清空 `share_token_raw`

### 结果
- 同一张海报的二维码不会因多次生成而失效
- 仅在 30 天过期或手动重置时更新 token

---

### 35. 修复"仅好友"可见性下分享主页显示占位提示

**解决方案：** 设置"个人主页可见性=仅好友"时，分享主页底部错误显示"完整收藏家主页（需复用…）"占位提示。

**根因：** 后端 `get_shared_profile` 的三个场景条件都要求 `home_visibility == "public"`。当为 `friends_only` 时无一匹配，`summary_only` 和 `names_only` 均未设置，前端误判为"完整数据模式"。

**实现逻辑：**
- 新增 else 分支处理非公开可见性（friends_only/private），按"仅统计"级别展示数据

### 结果
- "仅好友"可见性下分享主页正确显示统计卡片和"该用户未公开藏品明细"提示

---

### 36. 分享主页对齐 dashboard 收藏家模式 - 复用组件和数据接口

**解决方案：** 海报分享页面仅显示简单统计卡片和一个占位提示，未复用 dashboard 收藏家模式的实际组件，API 返回的数据结构也未对齐。

**实现逻辑：**
- **后端** `GET /share/full/{user_id}`：
  - 返回与 dashboard 一致的完整数据结构：`summary` + `cabinets` + `tags` + `activities`
  - 内部调用与 `summary.py` / `cabinets.py` 相同的查询逻辑和模型
  - 根据隐私设置过滤数据（show_total / show_figures 等）
- **前端** `ShareProfile.vue`：
  - 新增 `GET /collector/share/full/{user_id}` 接口调用
  - 完整数据模式：直接复用 `CollectorOverview`、`CollectionCabinets`、`TagCloud`、`ActivityFeed` 四个组件
  - 仅统计/仅藏品名模式：展示统计卡片和对应提示
  - 复用路由 `/share/{userId}` 不变

### 结果
- 分享主页与 dashboard 收藏家模式展示完全一致
- 实现"一次开发，两处复用"

---

### 37. 隐私设置弹窗隐藏垂直滚动条

**解决方案：** 隐私设置弹窗因内容超出高度出现垂直滚动条，视觉上不美观。

**实现逻辑：**
- `.modal` 新增 `scrollbar-width: none`（Firefox）
- 新增 `.modal::-webkit-scrollbar { display: none }`（Chrome/Safari）
- 滚动功能正常保留，仅隐藏滚动条

### 结果
- 隐私设置弹窗无垂直滚动条，交互不受影响

---

### 38. 分享 API 拆分为多个独立端点

**解决方案：** `/share/full/{user_id}` 一个接口返回 summary/cabinets/tags/activities 全部数据与 dashboard 的多接口并行加载模式不一致。

**实现逻辑：**
- 移除 `/share/full/{user_id}` 合并端点
- 新增 5 个独立端点，每个端点自行验证 token：
  - `GET /share/profile/{user_id}` — 用户基础信息 + 隐私级别
  - `GET /share/summary/{user_id}` — 统计数据
  - `GET /share/cabinets/{user_id}` — 收藏柜
  - `GET /share/tags/{user_id}` — 标签云
  - `GET /share/activities/{user_id}` — 动态流
- 前端先串行获取 profile、再并行获取 4 个业务接口后合并数据

### 结果
- 分享页 API 与 dashboard 多接口架构对齐

---

### 39. 优化分享页按隐私级别按需请求接口

**解决方案：** 仅统计/仅藏品名模式下，前端仍发送 cabinets/tags/activities 三个不必要的 API 请求（后端虽返回空数据但浪费带宽和查询资源）。

**实现逻辑：**
- 前端先获取 profile 接口确定隐私级别
- 仅在 `summary_only` 和 `names_only` 均为 false（即完整数据模式）时，才并行请求 cabinets/tags/activities
- 仅统计/仅藏品名模式仅请求 summary 接口

### 结果
- 仅统计模式减少 3 次无效 API 请求，页面加载更快

---

### 40. 分享海报弹窗隐藏垂直滚动条

**解决方案：** 分享海报弹窗（藏品陈列/极简数据模板）因内容高度超出出现垂直滚动条，影响视觉美观。

**实现逻辑：**
- `.modal` 新增 `scrollbar-width: none`（Firefox）
- 新增 `.modal::-webkit-scrollbar { display: none }`（Chrome/Safari）

### 结果
- 分享海报弹窗无垂直滚动条，滚动交互正常

---

### 41. 隐私设置"本命厂商"字段更名为"标签云"

**解决方案：** 隐私设置中"本命厂商"字段名和描述与实际功能不匹配，实际控制的是标签云（tags）的可见性而非厂商偏好。

**实现逻辑：**
- 后端 `collector_privacy` 表字段 `show_makers` 重命名为 `show_tags`，注释从"是否展示本命厂商"改为"是否展示标签云"
- 服务层、API 层、`to_dict` 返回值同步更新为 `show_tags`
- frontend PrivacySettings 组件文案"本命厂商 → 是否展示厂商偏好信息"改为"标签云 → 是否展示手办标签信息"

### 结果
- 隐私设置项与实际功能匹配

---

### 42. 隐私设置"资产金额"选项移到"动态流"下方

**解决方案：** 隐私设置数据展示模块中"资产金额"排在"动态流"之前，资产金额属于敏感度较高的选项，放在最后更合理。

**实现逻辑：**
- PrivacySettings.vue 模板中数据展示模块顺序调整为：藏品总数 → 具体藏品列表 → 标签云 → 动态流 → 资产金额

### 结果
- 数据展示模块选项排序更合理

---

### 43. 分享页按每个隐私开关独立请求对应接口

**解决方案：** 分享页数据加载未与隐私设置的每个开关一一对应。开关关闭时仍发请求，浪费带宽；同时开关打开时也可能因条件判断过宽而漏掉数据。

**实现逻辑：**
- 前端先获取 profile 接口获得每个隐私开关状态
- 使用 `promiseMap` 动态构建请求列表，每个开关控制一个独立接口：
  - `show_total=开` → 请求 `/share/summary`
  - `show_figures=开` → 请求 `/share/cabinets`
  - `show_tags=开` → 请求 `/share/tags`
  - `show_feed=开` → 请求 `/share/activities`
- 开关关闭对应的接口完全不请求，节省网络资源
- 结果按 promiseMap 索引映射回对应数据字段

### 结果
- 每个隐私开关精准控制对应接口的请求，无多余调用无遗漏