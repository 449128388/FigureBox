# 2026-04-08 更新日志

## 今日更新

- **修复资产分布饼图显示问题**：
  - 问题：控制台显示 `pieChart.value` 为 null，DOM 元素未渲染完成
  - 原因：return 语句中缺少 `pieChart` 和 `profitChart` 的暴露，导致模板中的 ref 无法正确绑定
  - 解决方案：
    - 在 return 语句中添加 `pieChart` 和 `profitChart` 的暴露
    - 使用 setTimeout 延迟初始化，确保 DOM 完全渲染
    - 添加双重 setTimeout 作为备用方案
  - 修改文件：
    - `frontend/src/views/Dashboard.vue`

- **清理调试代码**：
  - 删除所有用于排查饼图问题的 console.log 调试语句
  - 修改文件：
    - `frontend/src/views/Dashboard.vue`

- **新增资产IP分布饼图**：
  - 功能：在资产看板中新增资产IP分布饼图，通过饼图下方的点进行切换
  - 后端：新增制造商分布统计逻辑，返回 `manufacturer_distribution` 数据
  - 前端：
    - 添加饼图切换指示器（两个圆点）
    - 支持在"风险状态分布"和"IP分布"之间切换
    - 添加切换动画和样式
    - 标题随切换动态变化
  - 修改文件：
    - `backend/app/api/assets.py`
    - `frontend/src/views/Dashboard.vue`

- **修复制造商分布统计问题**：
  - 问题：接口返回统计了2个未知厂商，但数据库中一条有明确制造商，一条没有
  - 原因：构建 holdings 列表时缺少 `manufacturer` 字段
  - 解决方案：在 holdings.append 中添加 `manufacturer: fig.manufacturer`
  - 修改文件：
    - `backend/app/api/assets.py`

- **优化饼图居中显示**：
  - 问题：饼图位置太靠近右边
  - 解决方案：将饼图的 center 属性从 `['60%', '50%']` 修改为 `['50%', '50%']`
  - 修改文件：
    - `frontend/src/views/Dashboard.vue`

- **新增资产持仓周期饼图**：
  - 功能：在资产看板中新增资产持仓周期饼图，通过饼图下方的点进行切换（3个点）
  - 后端：
    - 新增持仓周期分布统计逻辑，返回 `holding_period_distribution` 数据
    - 持仓时间分组：🆕 本月新入（30天内）、📅 1年内、🏛️ 1-2年、🦕 2年以上
  - 前端：
    - 添加第3个切换圆点
    - 支持在"风险状态分布"、"IP分布"和"持仓周期分布"之间切换
    - 标题随切换动态变化
  - 修改文件：
    - `backend/app/api/assets.py`
    - `frontend/src/views/Dashboard.vue`

- **修复后端API 500错误**：
  - 问题：`/api/assets/dashboard` 接口返回500错误
  - 原因：`UnboundLocalError: cannot access local variable 'datetime'`，在函数内部重复导入 datetime 覆盖了外部导入
  - 解决方案：删除持仓周期分布代码块中重复的 `from datetime import datetime` 导入语句
  - 修改文件：
    - `backend/app/api/assets.py`

- **新增资产仓位分层饼图**：
  - 功能：在资产看板中新增资产仓位分层饼图，通过饼图下方的点进行切换（4个点）
  - 后端：
    - 新增仓位分层分布统计逻辑，返回 `tier_distribution` 数据
    - 按单只手办市场价分层：🏠 海景房(>¥3k)、💎 中端(¥1k-3k)、🧩 入门(<¥1k)
  - 前端：
    - 添加第4个切换圆点
    - 支持在"风险状态分布"、"IP分布"、"持仓周期分布"和"仓位分层分布"之间切换
    - 标题随切换动态变化
  - 修改文件：
    - `backend/app/api/assets.py`
    - `frontend/src/views/Dashboard.vue`

- **新增饼图自动轮播功能**：
  - 功能：4个饼图之间每1分钟自动切换轮播
  - 默认展示：进入资产看板模块默认展示"资产风险状态饼图"
  - 自动轮播顺序：风险状态 → IP分布 → 持仓周期 → 仓位分层 → 风险状态（循环）
  - 用户交互：用户手动点击切换后，自动轮播会重新从当前选中的饼图开始计时
  - 生命周期管理：组件挂载时启动轮播，卸载时清除定时器
  - 修改文件：
    - `frontend/src/views/Dashboard.vue`
