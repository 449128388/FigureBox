# 修改记录 - 2026-03-05

## 1. 图片上传功能优化

### 问题
- 添加手办时，上传的图片无法查看完整内容
- 图片删除功能需要优化

### 解决方案
- 在添加手办的图片上传功能中新增组件，实现点击图片查看完整图片的功能
- 优化图片删除功能，确保用户可以方便地删除上传的图片
- 添加图片预览模态框，提供更好的用户体验

### 修改文件
- `frontend/src/views/Figures.vue`
  - 添加图片预览相关的数据属性：`showImagePreview` 和 `previewImage`
  - 实现 `viewImage` 和 `closeImagePreview` 方法
  - 修改图片上传组件，为图片添加点击事件
  - 添加图片预览模态框的HTML结构
  - 添加相关的CSS样式，包括图片悬停效果和模态框样式

### 优化效果
- 用户可以点击上传的图片查看完整的图片内容
- 图片预览模态框以半透明背景覆盖整个屏幕，居中显示完整图片
- 点击模态框外部或关闭按钮可以关闭预览
- 每张上传的图片右上角都有删除按钮，点击可以直接删除对应的图片
- 图片具有悬停效果，鼠标悬停时会轻微放大
- 响应式设计，在移动设备上也能正常工作

## 2. 图片删除按钮样式优化

### 问题
- 图片删除按钮样式简单，与整体设计风格不够协调

### 解决方案
- 优化图片删除按钮的样式，使其更加符合当前的设计风格
- 增加按钮大小，添加阴影效果和过渡动画
- 调整颜色和字体，提升视觉效果

### 修改文件
- `frontend/src/views/Figures.vue`
  - 优化 `.btn-delete-image` 样式
  - 增加按钮大小从20px到24px
  - 使用更符合Material Design风格的红色系颜色
  - 添加阴影效果和过渡动画
  - 优化悬停效果

### 优化效果
- 图片删除按钮更加醒目，符合整体设计风格
- 按钮具有立体感和动画效果
- 悬停时的反馈更加明显
- 整体视觉效果更加协调统一

## 3. 图片上传功能交互优化

### 问题
- 图片查看和删除功能的交互方式不够直观
- 按钮显示方式影响整体美观

### 解决方案
- 修改图片上传功能，将鼠标移动到图片上展示两个按钮：小眼睛（查看）和垃圾桶（删除）
- 点击小眼睛按钮查看完整图片
- 点击垃圾桶按钮删除上传的图片
- 优化悬停效果和动画过渡

### 修改文件
- `frontend/src/views/Figures.vue`
  - 修改图片上传组件的HTML结构，添加.image-actions容器
  - 替换原来的点击查看和直接显示删除按钮的方式
  - 添加.btn-view-image和.btn-delete-image按钮
  - 优化CSS样式，实现鼠标悬停时显示按钮的效果
  - 添加过渡动画和视觉效果

### 优化效果
- 鼠标移动到图片上时显示操作按钮，平时隐藏，保持界面整洁
- 小眼睛按钮用于查看完整图片，垃圾桶按钮用于删除图片，图标直观易懂
- 悬停效果流畅，提升用户体验
- 整体交互更加现代化和直观

## 4. 图片上传按钮样式优化

### 问题
- 图片上传功能中的操作按钮样式需要统一优化

### 解决方案
- 修改添加手办中图片上传功能中的小眼睛和垃圾桶的样式，使用Font Awesome图标
- 添加Font Awesome库引用
- 优化按钮样式，使其更加美观和统一

### 修改文件
- `frontend/index.html`
  - 添加Font Awesome库引用：`<link rel="stylesheet" href="https://cdn.bootcdn.net/ajax/libs/font-awesome/6.4.0/css/all.min.css">`
- `frontend/src/views/Figures.vue`
  - 修改图片上传组件，使用Font Awesome图标替代emoji
  - 更新按钮样式，使用test.html中定义的样式
  - 优化按钮的视觉效果和交互体验

### 优化效果
- 按钮使用Font Awesome图标，视觉效果更加统一和美观
- 按钮样式符合现代设计风格
- 悬停效果流畅，提升用户体验
- 整体界面更加专业和协调

## 5. 图片删除二次确认功能

### 问题
- 图片删除操作缺少二次确认，容易误操作

### 解决方案
- 修改添加手办图片上传功能中图片删除功能，增加二次确认删除的弹框功能
- 使用浏览器的confirm()方法实现简单的确认对话框

### 修改文件
- `frontend/src/views/Figures.vue`
  - 修改 `removeImage` 方法，添加确认对话框
  - 只有用户确认后才执行删除操作

### 优化效果
- 防止用户误操作删除图片
- 提升用户体验，增加操作的安全性
- 操作流程更加规范和专业

## 6. 修复413 Request Entity Too Large错误

### 问题
- 添加手办时上传10张图片，POST请求到 `/api/figures/` 时报错 413 Request Entity Too Large

### 解决方案
- 修改Nginx配置，增加 `client_max_body_size` 到250MB，以支持大文件上传
- 优化前端图片上传逻辑，确保每张图片不超过20MB的限制

### 修改文件
- `frontend/nginx.conf`
  - 添加 `client_max_body_size 250M;` 配置

### 优化效果
- 支持上传10张20MB的图片，不再出现413错误
- 上传功能更加稳定和可靠
- 满足用户的实际使用需求

## 7. 价格输入限制和出货日字体修复

### 问题
- 价格输入框可以输入负数和小数，不符合业务需求
- 出货日组件中年、月、日三个字体与其他字体不一致

### 解决方案
- 修改价格输入框，添加 `min="0"` 和 `step="1"` 属性，限制只能输入非负整数
- 为日期输入框添加样式，确保字体与其他输入框一致

### 修改文件
- `frontend/src/views/Figures.vue`
  - 修改价格输入框，添加 `min="0"` 和 `step="1"` 属性
  - 为日期输入框添加样式，设置 `font-family: inherit` 和 `font-size: 14px`

### 优化效果
- 价格输入框只能输入非负整数，符合业务需求
- 出货日组件的字体与其他输入框一致，提升视觉统一性
- 整体表单界面更加规范和专业

## 8. "价格"更名为"定价"

### 问题
- 添加手办页面中，"价格"字段名称不够准确

### 解决方案
- 将"价格"更名为"定价"，使表述更加准确

### 修改文件
- `frontend/src/views/Figures.vue`
  - 修改手办列表中显示的"价格"为"定价"
  - 修改添加表单中"价格"标签为"定价"

### 优化效果
- 字段名称更加准确和规范
- 提升用户界面的专业性

## 9. 调整添加手办表单字段顺序

### 问题
- 添加手办表单中"属性"和"制造商"字段的顺序需要调整

### 解决方案
- 调整添加手办表单中字段的顺序，将"属性"放在"制造商"前面

### 修改文件
- `frontend/src/views/Figures.vue`
  - 调整表单字段顺序，将"属性"字段移到"制造商"前面

### 优化效果
- 表单字段顺序更加合理
- 提升用户填写表单的体验

## 11. 添加币种选择功能

### 问题
- 添加手办页面中定价只支持人民币，缺少币种选择功能

### 解决方案
- 在定价输入框后增加币种选择下拉框，支持选择日元、人民币、美元、欧元
- 添加货币符号显示功能
- 优化表单布局，使定价和币种选择框并排显示

### 修改文件
- `frontend/src/views/Figures.vue`
  - 在定价输入框后添加币种选择下拉框
  - 在newFigure对象中添加currency属性，默认值为CNY
  - 添加getCurrencySymbol方法，用于显示货币符号
  - 修改手办列表中价格的显示，添加币种标识
  - 添加价格和币种容器的CSS样式

### 优化效果
- 支持多种币种选择，满足不同手办的定价需求
- 界面布局美观，操作便捷
- 货币符号显示正确，提升用户体验

## 12. 修复币种显示和默认值问题

### 问题
- 手办管理页面中的"定价"只展示"¥"，没有跟随添加手办中币种的变换而变化
- 币种选择框的默认值需要确保为"人民币"

### 解决方案
- 修改后端的Figure模型，添加currency字段，默认值为"CNY"
- 修改后端的FigureBase和FigureUpdate schema，添加currency字段
- 确保前端发送数据时包含currency字段
- 确保手办管理页面正确显示不同币种的货币符号

### 修改文件
- `backend/app/models/figure.py`
  - 添加currency字段，默认值为"CNY"
- `backend/app/schemas/figure.py`
  - 在FigureBase中添加currency字段，默认值为"CNY"
  - 在FigureUpdate中添加currency字段

### 优化效果
- 手办管理页面中的定价会根据币种显示正确的货币符号
- 币种选择框默认选择"人民币"
- 完整支持多币种定价功能

## 13. 修复 /api/figures/ GET 接口 500 错误

### 问题
- http://localhost:25600/api/figures/ GET 接口报错 500 Internal Server Error

### 解决方案
- 修改 main.py 文件，在创建表之前先删除所有表，确保表结构能够正确更新
- 重新构建并启动后端服务

### 修改文件
- `backend/main.py`
  - 在创建表之前添加 `Base.metadata.drop_all(bind=engine)` 语句

### 优化效果
- /api/figures/ GET 接口现在正常工作，返回 200 OK 状态码
- 数据库表结构正确更新，包含 currency 字段

## 14. 修改定价格式显示

### 问题
- 手办管理列表页面中的定价显示格式为"¥1"、"$3"、"€3"等，需要改为"1 元"、"1 日元"、"3 美元"、"3 欧元"的格式

### 解决方案
- 修改 getCurrencySymbol 方法，让它返回货币名称而不是符号
- 修改模板中显示定价的部分，将格式从"货币符号+价格"改为"价格+货币名称"

### 修改文件
- `frontend/src/views/Figures.vue`
  - 修改 getCurrencySymbol 方法，返回货币名称
  - 修改模板中定价的显示格式

### 优化效果
- 定价显示格式更加清晰易懂
- 统一了货币显示方式，使用中文货币名称
